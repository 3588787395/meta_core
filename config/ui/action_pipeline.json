{
  "version": "1.0",
  "description": "Action pipeline 表驱动配置：每个 action 声明 steps 列表，_execute_action 按序执行 step 函数库。差异显示于 steps 内容，非独立函数体",
  "actions": {
    "resolve_and_pass": {"steps": ["resolve", "pass"], "desc": "解析候选源并传递到下游"},
    "apply_filter": {"steps": ["filter", "propagate"], "desc": "应用转移条件过滤后传播"},
    "dzh_condition_filter": {"steps": ["dzh_filter", "propagate"], "desc": "DZH 公式条件过滤后传播"},
    "pass_pool_stocks": {"steps": ["pass"], "desc": "池股票传递（支持 move）"},
    "transfer_between_pools": {"steps": ["transfer"], "desc": "池间转移并触发高亮"},
    "remove_from_pool": {"steps": ["remove"], "desc": "从池中移除股票"}
  }
}
