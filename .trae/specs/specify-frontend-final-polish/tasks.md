# Meta Core 前端最终完善任务拆分

所有任务优先级：**High**

执行顺序：Task 1 → Task 2 → Task 3 → Task 4 → Task 5 → Task 6 → Task 7

**完成状态**：所有7个任务已完成（2026-07-25），评审通过

---

## [x] Task 1：模式状态管理重构 ✅ (评审25/25)

**目标**：修复 currentMode 局部变量问题，建立全局统一状态管理，实现仿真自动步进

**依赖**：无

**工作量预估**：8-12小时

### 子任务

- [ ] T1.1：梳理现有状态管理结构，定位所有使用 currentMode 局部变量的组件
- [ ] T1.2：在全局 Store 中定义 mode/simulationState/simulationTime 状态结构
- [ ] T1.3：实现 useSimulationMode、useSimulationTime 等统一 selector hooks
- [ ] T1.4：重构所有组件，移除局部 currentMode，改用全局 store 订阅
- [ ] T1.5：实现 Play/Pause/Step/Reset 控制逻辑，Step 一次前进1000ms
- [ ] T1.6：实现仿真时钟：Running 状态下基于 requestAnimationFrame 自动步进
- [ ] T1.7：实现 modeChanged 事件广播机制，确保模块间状态同步
- [ ] T1.8：添加状态变更日志中间件，便于调试

### 实现要点

1. 使用表驱动方式定义状态转换表：
```
stopped → (play) → running
running → (pause) → paused
paused → (play) → running
running/paused → (reset) → stopped
```

2. 仿真时间使用 performance.now() 计算delta，确保暂停/继续时间精确
3. 所有状态变更必须通过 action/dispatch，禁止直接赋值
4. 提供状态时间旅行调试支持

### Test Requirements

**Programmatic Tests（自动化测试）**：
- T1-P1：单元测试覆盖所有状态转换路径，转换合法性100%验证
- T1-P2：单元测试验证仿真时间单调性：连续步进1000次，时间严格递增无回退
- T1-P3：集成测试：Play → 等待100ms → Pause，验证时间增量在[90ms, 170ms]范围内
- T1-P4：集成测试：连续切换模式（real ↔ simulation）100次，验证无内存泄漏，无控制台错误
- T1-P5：静态代码扫描：grep 所有组件文件，`const currentMode`、`let currentMode` 匹配数为0
- T1-P6：测试 Reset 后状态完全回到初始值：time=0, state=stopped
- T1-P7：测试 Step 一次精确前进1000ms，误差≤16ms

**Human-Judgement Tests（人工验证）**：
- T1-H1：在顶部切换模式，观察所有面板（事件面板、定时器面板、股票池）模式指示同步更新
- T1-H2：点击 Play 后观察仿真时间流畅递增，无卡顿无跳变
- T1-H3：Pause 后时间立即停止，继续后从断点精确恢复
- T1-H4：Step 按钮点击一次时间前进1秒，视觉反馈清晰
- T1-H5：使用 DevTools 观察状态变化，所有 action 可追溯

### 验收关联
- 对应 AC-1

---

## Task 2：事件面板矩阵视图时间分布完善

**目标**：实现时间轴刻度、当前时间指示线、排队事件样式区分

**依赖**：Task 1 完成（全局时间状态可用）

**工作量预估**：6-8小时

### 子任务

- [ ] T2.1：矩阵视图重构：X轴改为基于仿真时间的比例定位，而非索引
- [ ] T2.2：实现时间刻度渲染：0s, 10s, 20s, ..., 300s，刻度线+文字标注
- [ ] T2.3：实现红色当前时间线（vertical line），使用 CSS animation 或 rAF 驱动
- [ ] T2.4：实现排队事件样式：虚线边框、opacity: 0.6、背景色淡
- [ ] T2.5：实现已触发事件样式：实心填充、边框完整、opacity: 1
- [ ] T2.6：实现格子悬停效果：高亮+tooltip 显示精确时间（ms）、事件类型
- [ ] T2.7：矩阵视图缩放支持：滚轮缩放时间范围（默认0-300s）

### 实现要点

1. 格子水平位置 = `(event.triggerTime / maxTime) * matrixWidth`
2. 当前时间线位置 = `(simulationTime / maxTime) * matrixWidth`，每帧更新
3. 使用 CSS transform: translateX() 移动时间线，获得GPU加速
4. 事件状态表驱动：
```
'pending' → { border: 'dashed', opacity: 0.6 }
'triggered' → { border: 'solid', opacity: 1.0 }
'cancelled' → { border: 'dotted', opacity: 0.3 }
```

### Test Requirements

**Programmatic Tests**：
- T2-P1：模拟仿真时间从0到300000ms，验证当前时间线left样式值从0%到100%线性变化
- T2-P2：验证 pending 事件元素包含 `dashed` class，triggered 事件包含 `solid` class
- T2-P3：JSDOM 测试：渲染300秒时间范围，DOM中存在0s/10s/20s/.../300s共31个刻度标签
- T2-P4：单元测试：事件位置计算函数在边界值（0, maxTime, 中间值）计算正确
- T2-P5：性能测试：矩阵包含1000个事件时，FPS≥50

**Human-Judgement Tests**：
- T2-H1：视觉检查：时间刻度均匀分布，文字清晰不重叠
- T2-H2：运行仿真，观察红色时间线平滑向右移动，无抖动
- T2-H3：观察排队事件（虚线/半透明）和已触发事件（实心）视觉差异明显
- T2-H4：鼠标悬停在事件格子上，tooltip 显示时间精确到毫秒
- T2-H5：当时间线经过事件格子瞬间，格子样式从pending切换为triggered

### 验收关联
- 对应 AC-2

---

## Task 3：事件面板散点图交互完善

**目标**：添加事件计数显示、悬浮 tooltip、点击详情面板

**依赖**：Task 1、Task 2 完成

**工作量预估**：8-10小时

### 子任务

- [ ] T3.1：散点聚合逻辑：同一时间点（±500ms窗口）的事件聚合为一个散点
- [ ] T3.2：实现计数徽标：每个散点右上角显示事件数量（>1时显示）
- [ ] T3.3：实现 tooltip 组件：悬浮显示时间、事件类型分布、股票代码列表（最多显示5个，更多显示+N）
- [ ] T3.4：实现点击交互：点击散点弹出详情 Modal/Drawer
- [ ] T3.5：详情面板展示该时间点所有事件：序号、时间、类型、股票代码、事件数据
- [ ] T3.6：详情面板中股票代码可点击，点击后跳转到股票详情视图
- [ ] T3.7：散点颜色映射：与矩阵视图使用同一配色表（表驱动）
- [ ] T3.8：tooltip 延迟100ms显示，避免鼠标快速划过时闪烁

### 实现要点

1. 配色表使用表驱动：
```javascript
const EVENT_COLORS = {
  'stock:enter': '#4CAF50',
  'stock:observe': '#2196F3',
  'stock:buy': '#FF9800',
  'stock:sell': '#F44336',
  'stock:exit': '#9E9E9E',
  'timer:trigger': '#9C27B0',
};
```

2. 散点聚合使用哈希表：key = Math.round(time / 1000) * 1000
3. tooltip 使用 portal 渲染到body，避免父容器overflow截断
4. 详情面板虚拟滚动：事件数>100时使用虚拟列表保证性能

### Test Requirements

**Programmatic Tests**：
- T3-P1：单元测试：聚合函数正确合并同一时间窗口内的事件
- T3-P2：单元测试：计数徽标数字 = 该聚合计数，数字正确
- T3-P3：组件测试：悬浮散点100ms内tooltip出现，移开后200ms内消失
- T3-P4：组件测试：点击散点后详情面板打开，包含正确数量的事件条目
- T3-P5：集成测试：点击详情中的股票代码，路由正确跳转，股票详情加载
- T3-P6：快照测试：散点图在0/100/200/300秒四个时间点的DOM快照正确
- T3-P7：测试同一时间点有50个事件时，徽标显示"50"，tooltip显示"+45更多"

**Human-Judgement Tests**：
- T3-H1：观察散点分布与矩阵视图一致，颜色对应正确
- T3-H2：鼠标悬浮在散点上，tooltip 弹出流畅，信息完整可读
- T3-H3：点击散点弹出详情，数据与事件日志一一对应
- T3-H4：点击股票代码正确跳转，返回后位置状态保持
- T3-H5：快速移动鼠标经过多个散点，无残留tooltip，无闪烁

### 验收关联
- 对应 AC-3

---

## Task 4：定时器队列时间分布图完善

**目标**：实现当前时间线、过期定时器标识、剩余时间实时倒计时

**依赖**：Task 1 完成

**工作量预估**：6-8小时

### 子任务

- [ ] T4.1：定时器队列视图重构：从列表视图改为时间线/甘特图视图
- [ ] T4.2：实现绿色当前时间线（区别于矩阵视图的红色）
- [ ] T4.3：实现定时器条目水平条：起点=注册时间，终点=触发时间
- [ ] T4.4：实现过期定时器样式：灰色背景、文字删除线、opacity: 0.5
- [ ] T4.5：实现待触发定时器样式：彩色边框、醒目背景、显示剩余时间
- [ ] T4.6：剩余时间实时倒计时：每秒更新，格式"XXs后触发"
- [ ] T4.7：实现点击定时器条目高亮：在矩阵/散点图上高亮关联事件
- [ ] T4.8：添加过滤器：按状态（等待/已触发/过期）、按类型筛选定时器

### 实现要点

1. 定时器状态表：
```
'waiting' → { style: 'active', showCountdown: true }
'triggered' → { style: 'success', showCountdown: false }
'expired' → { style: 'expired', showCountdown: false } // 异常情况：错过触发
'cancelled' → { style: 'cancelled', showCountdown: false }
```

2. 剩余时间 = timer.targetTime - simulationTime，每秒重新计算
3. 时间线位置计算与矩阵视图复用同一工具函数
4. 高亮联动：点击定时器 → 派发 `timerHighlight` 事件 → 矩阵/散点图响应

### Test Requirements

**Programmatic Tests**：
- T4-P1：单元测试：定时器状态计算函数在各种时间输入下状态正确
- T4-P2：当 simulationTime > targetTime+100ms 时，定时器标记为 expired 样式
- T4-P3：当 simulationTime < targetTime 时，剩余时间显示正确，误差≤100ms
- T4-P4：集成测试：定时器触发瞬间，样式从waiting变为triggered
- T4-P5：测试300秒仿真结束后，所有定时器要么triggered要么expired，无waiting状态
- T4-P6：测试点击定时器条目，派发的事件包含正确timerId
- T4-P7：性能测试：队列中有200个定时器时，倒计时更新流畅

**Human-Judgement Tests**：
- T4-H1：观察绿色当前时间线与仿真时钟同步移动
- T4-H2：过期定时器（灰色删除线）与等待中定时器视觉区分明显
- T4-H3：等待中的定时器倒计时每秒更新，数字准确
- T4-H4：点击定时器条目，矩阵/散点图对应事件高亮，联动正确
- T4-H5：过滤器功能正常，筛选结果准确

### 验收关联
- 对应 AC-4

---

## Task 5：仿真完整事件链验证与修复

**目标**：仿真连续运行≥300秒无异常，验证股票完整生命周期流转

**依赖**：Task 1-4 完成

**工作量预估**：10-14小时

### 子任务

- [ ] T5.1：编写仿真自动化验证脚本（validation-runner.js）
- [ ] T5.2：实现事件监听收集器：记录所有触发的事件到日志数组
- [ ] T5.3：实现定时器监听：记录所有定时器的注册/触发/取消
- [ ] T5.4：实现股票生命周期追踪器：记录每只股票的状态变更序列
- [ ] T5.5：运行300秒仿真，收集完整数据
- [ ] T5.6：验证事件不丢失：注册事件数=触发事件数，无遗漏
- [ ] T5.7：验证定时器准确性：每个setTimer对应唯一一次回调，无重复无遗漏
- [ ] T5.8：验证股票流转：入池→观察→买入→持有→卖出→出池，每步状态转换合法
- [ ] T5.9：验证数量守恒：各池数量之和与总入池/出池数一致
- [ ] T5.10：内存泄漏检测：对比0秒和300秒内存快照，Detached DOM节点数不增长
- [ ] T5.11：修复发现的所有bug：事件丢失、定时器错乱、状态不一致等
- [ ] T5.12：生成验证报告：包含各项指标、通过/失败状态、异常列表

### 实现要点

1. 股票状态机（表驱动）：
```
'outside' → [ENTER] → 'pool'
'pool' → [OBSERVE] → 'watching'
'watching' → [BUY] → 'holding'
'holding' → [SELL] → 'sold'
'sold' → [EXIT] → 'outside'
```
任何不在此表的转换均为非法，立即报错。

2. 验证脚本使用 puppeteer/playwright 启动浏览器，自动运行仿真
3. 内存检测使用 Chrome DevTools Protocol 获取 Performance.metrics
4. 事件日志使用环形缓冲区，避免长时运行内存溢出

### Test Requirements

**Programmatic Tests（核心自动化验证）**：
- T5-P1：运行验证脚本，连续执行3次300秒仿真，每次均0 console error
- T5-P2：事件触发率100%：注册事件数 N，实际触发 N 次，误差0
- T5-P3：定时器准确率100%：注册定时器 M，正确回调 M 次，无重复无遗漏
- T5-P4：股票流转完成率≥95%：≥95%的入池股票最终走到outside状态
- T5-P5：非法状态转换数为0：无股票跳出状态机定义的合法路径
- T5-P6：数量守恒验证：每10秒检查一次，买入+持有+卖出 = 总入池 - 已出池，恒成立
- T5-P7：内存增长<50MB：300秒结束时JS Heap Used较开始时增长<50MB
- T5-P8：Detached DOM节点数在300秒时<10个，无持续增长趋势
- T5-P9：验证脚本输出结构化JSON报告，包含所有指标数值
- T5-P10：异常边界测试：仿真运行中多次Pause/Play/Reset/Step，无错误

**Human-Judgement Tests**：
- T5-H1：目视观察300秒仿真运行，界面流畅无卡死
- T5-H2：观察股票在各池间流转，符合策略逻辑（无凭空出现/消失的股票）
- T5-H3：查看验证报告，所有P0/P1指标通过
- T5-H4：手动随机Pause/Resume/Step多次，状态始终一致
- T5-H5：检查事件面板/定时器面板无明显异常（如负时间、幽灵事件）

### 验收关联
- 对应 AC-5

---

## Task 6：界面一致性与简洁性优化

**目标**：移除冗余控件、实现状态保存、添加快捷键支持，提升整体专业感

**依赖**：Task 5 完成（功能稳定后再做UI优化）

**工作量预估**：6-8小时

### 子任务

- [ ] T6.1：UI控件盘点：列出所有按钮/菜单/面板入口，标记冗余项
- [ ] T6.2：移除重复的模式切换按钮，保留单一入口（顶部工具栏）
- [ ] T6.3：移除无用的调试面板入口、废弃的功能按钮
- [ ] T6.4：统一间距规范：所有组件遵循 4px/8px/16px 栅格
- [ ] T6.5：统一配色：提取色板到CSS变量，所有硬编码颜色替换为变量
- [ ] T6.6：统一字体：标题/正文/数字使用统一字体栈
- [ ] T6.7：实现面板状态持久化：位置、大小、展开/折叠状态存入 localStorage
- [ ] T6.8：实现快捷键系统：
  - Space：播放/暂停
  - S：步进（仿真模式下）
  - R：重置
  - E：切换事件面板显示/隐藏
  - T：切换定时器面板显示/隐藏
  - ?: 显示快捷键帮助
- [ ] T6.9：实现底部状态栏：显示模式、仿真时间、FPS、股票总数、事件总数
- [ ] T6.10：快捷键提示：菜单项/按钮tooltip中显示快捷键

### 实现要点

1. 快捷键使用事件委托，在document级别监听keydown
2. 输入框聚焦时暂停快捷键响应（避免输入时触发）
3. localStorage key 使用命名空间：`metacore:panel:state:*`
4. FPS 统计基于 rAF 帧间隔计算，每秒更新一次
5. UI 控件缩减目标：较优化前减少≥20%

### Test Requirements

**Programmatic Tests**：
- T6-P1：DOM节点数对比：优化后DOM元素总数较优化前减少≥20%
- T6-P2：单元测试：快捷键映射表完整，所有定义的快捷键都有对应处理函数
- T6-P3：集成测试：按Space键切换播放/暂停，状态正确变更
- T6-P4：集成测试：按E键切换事件面板，display属性正确切换
- T6-P5：持久化测试：调整面板大小/位置→刷新页面→状态恢复一致
- T6-P6：测试输入框聚焦时快捷键不触发（如在搜索框按Space不暂停）
- T6-P7：CSS变量测试：所有color值引用var(--*)，无硬编码#xxxxxx颜色（白名单除外）
- T6-P8：localStorage写入测试：面板状态变更后500ms内持久化

**Human-Judgement Tests**：
- T6-H1：整体视觉检查：间距统一、配色协调、无视觉错位
- T6-H2：检查无冗余按钮/控件，界面清爽专业
- T6-H3：所有快捷键响应准确，无冲突
- T6-H4：按?显示快捷键帮助，说明清晰
- T6-H5：刷新页面后面板布局与关闭前一致
- T6-H6：状态栏信息准确，FPS读数合理
- T6-H7：菜单项tooltip显示对应快捷键，提示明确

### 验收关联
- 对应 AC-6

---

## Task 7：设计文档更新

**目标**：更新 DESIGN.md 和 DESIGN0.md，与最终实现完全一致

**依赖**：Task 1-6 全部完成

**工作量预估**：4-6小时

### 子任务

- [ ] T7.1：更新 DESIGN.md 状态管理章节：全局Store结构、状态转换图、selector API
- [ ] T7.2：更新 DESIGN.md 组件树：最终组件层级、props接口、事件通信
- [ ] T7.3：更新 DESIGN.md 事件流图：所有事件类型、发送方、接收方、数据格式
- [ ] T7.4：更新 DESIGN.md 可视化架构：矩阵/散点图/定时器时间线的实现原理
- [ ] T7.5：更新 DESIGN0.md 数据表结构：Stock、Event、Timer 等数据模型
- [ ] T7.6：更新 DESIGN0.md 事件类型定义：所有事件type、payload结构
- [ ] T7.7：更新 DESIGN0.md 定时器生命周期：注册→等待→触发/取消的完整流程
- [ ] T7.8：补充本次新增API文档：快捷键、状态持久化、验证脚本
- [ ] T7.9：检查并更新所有代码示例，确保与实际代码一致
- [ ] T7.10：扫描文档移除所有"TODO"、"待实现"、"WIP"等标记
- [ ] T7.11：交叉校验：随机抽取5个组件/API，验证文档描述与代码100%匹配
- [ ] T7.12：添加本次完善的变更记录（Changelog）

### 文档要求

1. 架构图使用 Mermaid 语法，可直接渲染
2. 数据结构使用 TypeScript 接口定义
3. 事件类型使用表格列出：type、payload、发起方、接收方、说明
4. 代码示例从实际代码中复制，保持一致
5. 所有图表清晰、标注完整

### Test Requirements

**Programmatic Tests**：
- T7-P1：文档扫描：grep "TODO\|待实现\|WIP\|FIXME"，结果为0
- T7-P2：文档链接检查：所有内部锚点链接有效，无死链
- T7-P3：Mermaid图表语法校验：所有```mermaid块可正常渲染
- T7-P4：TypeScript接口校验：文档中所有interface在代码中存在定义
- T7-P5：事件类型校验：文档列出的所有事件type在代码中均有dispatch对应

**Human-Judgement Tests**：
- T7-H1：架构师审阅：状态管理描述与实际Store结构一致
- T7-H2：新人测试：新人仅凭文档可搭建开发环境并理解整体架构
- T7-H3：组件文档完整：每个主要组件都有职责、props、事件说明
- T7-H4：Mermaid图渲染正常，布局清晰可读
- T7-H5：Changelog完整记录本次所有重要变更

### 验收关联
- 对应 AC-7

---

## 任务完成标准总览

| 任务 | 完成标志 | 对应AC |
|------|----------|--------|
| Task 1 | 自动化测试T1-P1~T1-P7全部通过，人工验证T1-H1~T1-H5无问题 | AC-1 |
| Task 2 | T2-P1~T2-P5通过，T2-H1~T2-H5通过 | AC-2 |
| Task 3 | T3-P1~T3-P7通过，T3-H1~T3-H5通过 | AC-3 |
| Task 4 | T4-P1~T4-P7通过，T4-H1~T4-H5通过 | AC-4 |
| Task 5 | T5-P1~T5-P10通过，300秒验证报告所有指标绿灯，T5-H1~T5-H5通过 | AC-5 |
| Task 6 | T6-P1~T6-P8通过，UI控件减少≥20%，T6-H1~T6-H7通过 | AC-6 |
| Task 7 | T7-P1~T7-P5通过，双工程师抽查匹配率100%，T7-H1~T7-H5通过 | AC-7 |
| 最终评审 | 双工程师独立评分，综合≥98分，所有维度≥90分 | AC-8 |
