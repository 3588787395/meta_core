# 股票池深度重构 - 验证清单

## 核心架构验证

- [ ] 最新tick表（latest_tick）独立维护，是唯一真相源
- [ ] 水位线（latest_tick_ts）不变时，零计算开销
- [ ] 运行前/运行时严格分离：编译期一次性计算，运行时只读
- [ ] 执行顺序从 edge.params._order 读取，不是拓扑排序
- [ ] 拓扑只用于界面展示和合法性校验，不是执行顺序
- [ ] 脏标记传播机制正确：源节点脏 → 出边检查 → 执行 → 目标节点脏

## 表驱动验证

- [ ] 时间触发：8 start + 3 cx = 11 条规则，覆盖 24 种组合
- [ ] 过滤条件：6 求值器 + 10 比较器 = 16 条规则，覆盖 60 种组合
- [ ] 传播模式：3 种模式（copy/move/overwrite）表驱动
- [ ] 节点角色：5 种角色行为表驱动
- [ ] 没有硬编码的类型判断 if 链
- [ ] 配置表总数：核心 ≤ 8 张，全部 ≤ 16 张

## 代码质量验证

- [ ] engine.py ≤ 800 行（当前 3504 行，减少 75%+）
- [ ] 核心循环函数 ≤ 150 行
- [ ] 每个函数职责单一
- [ ] 每个功能都有明确的"读表→计算→写表"对应
- [ ] 模块边界清晰：数据层/计算层/事件层/后处理层

## 功能正确性验证

- [ ] 所有现有单元测试通过
- [ ] 所有现有集成测试通过
- [ ] 行为等价性：新引擎 vs 旧引擎，相同输入相同输出
- [ ] 实盘模式正常工作
- [ ] 回放模式正常工作
- [ ] 仿真模式正常工作

## 性能验证

- [ ] 空转场景（水位线不变）：性能提升 ≥ 100x
- [ ] 单条边触发场景：性能提升 ≥ 10x
- [ ] 全量计算场景：性能提升 ≥ 5x
- [ ] 内存占用不增加

## 前端验证

- [ ] 综合设置表格正确展示执行顺序
- [ ] 执行顺序可拖拽调整，调整后实时生效
- [ ] 画布上正确显示执行顺序编号
- [ ] 三模式切换清晰，无混乱
- [ ] 设计模式/运行模式切换正确

## 架构清晰度验证

- [ ] 数据层与计算层分离
- [ ] K线合成独立于股票池引擎
- [ ] 后处理（PK/分析/面板/预警）独立于核心循环
- [ ] 事件系统独立，可单独测试
- [ ] 三模式共享同一核心引擎，差异只在三个插件
