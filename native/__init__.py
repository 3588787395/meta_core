"""native package — pipeline handlers and builtins.

I12：builtins_gate/builtins_injector/builtins_pre_tick/builtins_refresh 别名已删
（零调用方）。仅保留 pipeline 模块本身（refresh handlers 仍活跃）。
"""
from . import pipeline
