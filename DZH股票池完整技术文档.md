# 大智慧股票池完整技术文档

> 基于206个XML文件统计分析 + 大智慧股票池说明书PDF(47页) + 设计器界面截图 + 3个参考XML深度分析
> 2011年大智慧股份有限公司版

---

## 一、DZH股票池是什么

根据说明书原文："一个基本的股票池流程由**备选池→转移条件→股票池**构成"。

DZH股票池是一个**有向图执行系统**，图中有三种核心节点和连接它们的边。

### 1.1 三种核心节点

| 节点 | XML type | 作用 | 说明书原文 |
|------|----------|------|-----------|
| 备选池 | 202 | 股票数据源，决定哪些股票参与筛选 | "确定股票池的基本备选范围" |
| 转移条件 | 201 | 筛选器，用公式决定哪些股票可以通过 | "满足该指标条件的股票才能进入下一级股票池" |
| 股票状态池 | 200 | 存储池，存放经过筛选的股票 | "存储经过指标条件筛选后的股票" |

**它们是三个独立的控件，不存在任何"复合体"。**

### 1.2 辅助节点（不参与执行）

| 节点 | XML type | 作用 |
|------|----------|------|
| 废弃池 | 4 | 暂存不满足条件的股票 |
| 执行顺序 | 5 | 视觉标注，显示执行顺序编号箭头（实际执行顺序由flow元素顺序决定） |
| 容器 | 2 | 背景框、状态池框 |
| 状态列 | 3 | 纵向分隔条 |
| 文字标签 | 1 | 文字注释 |
| 流程箭头 | 6 | 装饰性箭头线 |
| 未知类型 | 203 | 极少见（仅3个），用途不明 |

### 1.3 拓扑规律

流的连接是**严格交替**的：

```
202(备选池) → 201(转移条件) → 200(状态池) → 201(转移条件) → 200(状态池) → ...
```

**不存在** 200→200 或 201→201 的直接连接。每个200必须通过201才能连接到另一个200。

---

## 二、XML文件结构

### 2.1 总体结构

```xml
<?xml version="1.0" encoding="GB2312"?>
<pool type="ss-pool" ver="1.0" mode="1" nextid="95" backcolor="16777216" warning="..." ency="...">
  <cells>
    <cell id="6" type="1" attr="89" pos="32,33,258,65" clr="221" text="..."/>
    <cell id="10" type="200" attr="0" pos="..." clr="..." text="..." hold="432000" col="2,-1,-2,-3,7,14,8,10,17,45">
      <stk label="SH600000" t="1369909255" p="10.54"/>
    </cell>
    ...
  </cells>
  <flows>
    <flow from="10" to="11" attr="0" clr="-1" count="1379" begin="3" begint="1800" end="0" endt="2147483647" interval="60"/>
    ...
  </flows>
  <trades>
    <trade .../>
  </trades>
  <opentrades>
    <trade .../>
  </opentrades>
</pool>
```

### 2.2 Pool 根属性

| 属性 | 类型 | 必填 | 含义 | 常见值 | 统计 |
|------|------|------|------|--------|------|
| `type` | string | 是 | 股票池类型标识 | `"ss-pool"` | 204/204 |
| `ver` | string | 是 | 格式版本 | `"1.0"` (201), `"1.1"` (3) | 204/204 |
| `mode` | string | 是 | 运行模式 | `"1"` | 204/204 |
| `nextid` | int | 是 | 下一个可用cell ID（自增） | 各种数字 | 204/204 |
| `backcolor` | int | 是 | 背景颜色值 | `16777216`(103), `-1`(71), `16777226`(3) | 204/204 |
| `warning` | string | 否 | 帮助/预警页面URL | `"file://#DZH2#/userdata/poolsys/startpage.html"` | 2/204 |
| `ency` | string | 否 | 加密密钥（编辑密码保护） | 长整数字符串 | 52/204 |
| `system` | string | 否 | 系统标记 | `"0"` | 1/204 |

### 2.3 backcolor 颜色说明

- `16777216` (0x01000000): 黑色背景（最常见）
- `-1` (0xFFFFFFFF): 白色/透明背景
- `16777226` (0x0100000A): 深色背景变体
- 颜色格式为 Windows GDI 整数值，高字节可能为 alpha 通道

---

## 三、Cell 类型体系

### 3.1 Cell 类型总览

| type值 | 控件名称 | 设计器图标 | 数量 | 说明 |
|--------|----------|-----------|------|------|
| 1 | 文字标签 | Aa | 1006 | 文字注释、标题、时间标签 |
| 2 | 容器 | □ | 417 | 背景容器、状态池、输出池 |
| 3 | 状态列 | ≡ | 354 | 纵向状态列容器 |
| 4 | 丢弃池 | ✕ | 71 | 暂存不满足条件的股票 |
| 5 | 执行顺序 | ①②③ | 26 | 执行顺序视觉标注 |
| 6 | 连接箭头 | → | 114 | 视觉连接线 |
| 200 | 股票状态池 | 设 | 2876 | 核心筛选条件+股票池状态 |
| 201 | 转移条件 | ○ | 2919 | 转移条件的输出端口 |
| 202 | 备选池 | ⊙ | 308 | 候选股票源 |
| 203 | 未知类型 | ? | 3 | 极少见，用途不明 |

### 3.2 Cell 通用属性

| 属性 | 适用类型 | 含义 |
|------|----------|------|
| `id` | 全部 | 唯一标识符，整数，自增分配 |
| `type` | 全部 | 控件类型编号 |
| `attr` | 全部 | 属性位标志（详见3.4节） |
| `pos` | 全部 | 位置坐标 `"x1,y1,x2,y2"`（左上角到右下角） |
| `clr` | 全部 | 颜色值（Windows GDI整数） |
| `text` | 全部 | 显示文本/名称 |

### 3.3 Cell 专有属性总览

| 属性 | 适用类型 | 含义 |
|------|----------|------|
| `attrtext` | 202 | 市场板块选择 |
| `reload` | 202 | 重新载入间隔(秒) |
| `lastload` | 202 | 上次载入时间戳 |
| `hold` | 200 | 股票保留时长(秒) |
| `col` | 200 | 列表显示列ID |
| `width` | 200 | 列宽(像素) |
| `histana` | 200 | 历史分析数据点数 |
| `wizd` | 200 | 状态说明文字 |
| `deltype` | 200 | 删除类型 |
| `endtime` | 200 | 结束时间 |
| `delstocktype` | 200 | 删除股票类型 |
| `staattr` | 200 | 状态属性标志 |
| `enter` | 200 | 入池执行动作 |
| `exit` | 200 | 出池执行动作 |
| `stocknum` | 200 | 股票数量 |
| `tmpl` | 200 | 模板 |
| `inditype` | 201 | 指标类型 |
| `crc` | 201 | 公式校验和 |
| `indi` | 201 | 公式内容(Base64) |
| `sorttype` | 201 | 排序类型 |
| `indiparam` | 201 | 指标参数 |
| `url` | 1 | 超链接URL |

### 3.4 Cell attr 位标志详解

attr 是一个32位整数，不同位代表不同属性开关。**各类型的位标志含义不同，解析时必须先判断type，再解读attr的各个位。**

#### type=1 (文字标签) attr 位标志

| 位 | 掩码 | 出现次数 | 推断含义 |
|----|------|----------|----------|
| bit0 | 0x1 | 502 | 字体加粗/强调标记 |
| bit1 | 0x2 | 386 | 字体样式标记1 |
| bit2 | 0x4 | 390 | 字体样式标记2 |
| bit3 | 0x8 | 373 | 字体样式标记3 |
| bit4 | 0x10 | 648 | 可见/显示标记（绝大多数标签有此位） |
| bit5 | 0x20 | 214 | 字体样式标记4 |
| bit6 | 0x40 | 336 | 字体样式标记5 |
| bit7 | 0x80 | 45 | 字体样式标记6 |
| bit9 | 0x200 | 16 | 空心/透明背景 |

**常见 attr 值与标签子类型对应表：**

| attr值 | 十六进制 | 子类型 | 典型用途 |
|--------|----------|--------|----------|
| 0 | 0x0 | plain | 普通文本 |
| 19 | 0x13 | section_header | 章节标题（如"选股原理"） |
| 21 | 0x15 | subtitle_like | 副标题变体 |
| 25 | 0x19 | description_like | 描述性文字 |
| 57 | 0x39 | pool_title | 股票池标题 |
| 77 | 0x4D | title | 主标题（如"大智慧超赢股票池"） |
| 85 | 0x55 | description | 描述文字 |
| 88 | 0x58 | text | 普通文本 |
| 89 | 0x59 | time_label | 时间标签（如"10:00"） |
| 90 | 0x5A | time_point | 时间点标记 |
| 92 | 0x5C | model_subtitle | 模型副标题 |
| 95 | 0x5F | subtitle | 副标题（如"超赢一号·短线模型"） |
| 98 | 0x62 | condition_label | 条件说明标签 |

#### type=2 (容器) attr 位标志

| 位 | 掩码 | 出现次数 | 推断含义 |
|----|------|----------|----------|
| bit9 | 0x200 | 87 | 显示到股票池总览 |

| attr值 | 含义 |
|--------|------|
| 0 (0x0) | 普通容器（背景框） |
| 512 (0x200) | 显示到总览的容器（状态池/输出池） |
| 89 (0x59) | 带时间标签属性的容器 |

#### type=3 (状态列) attr 位标志

| 位 | 掩码 | 出现次数 | 推断含义 |
|----|------|----------|----------|
| bit9 | 0x200 | 79 | 显示到股票池总览 |

| attr值 | 含义 |
|--------|------|
| 0 (0x0) | 普通状态列 |
| 512 (0x200) | 显示到总览的状态列 |

#### type=4 (丢弃池) attr 位标志

| 位 | 掩码 | 出现次数 | 推断含义 |
|----|------|----------|----------|
| bit7 | 0x80 | 71 | 丢弃池基本标记 |
| bit0-6 | 各种 | 13 | 样式/装饰标记 |
| bit9 | 0x200 | 3 | 显示到股票池总览 |

| attr值 | 含义 |
|--------|------|
| 128 (0x80) | 标准丢弃池 |
| 219 (0xDB) | 带装饰文字的丢弃池 |
| 640 (0x280) | 显示到总览的丢弃池 |

#### type=5 (执行顺序) attr 位标志

| 位 | 掩码 | 出现次数 | 推断含义 |
|----|------|----------|----------|
| bit9 | 0x200 | 7 | 显示到总览视图 |

| attr值 | 含义 |
|--------|------|
| 0 (0x0) | 设计视图中的执行顺序标注 |
| 512 (0x200) | 总览视图中的执行顺序标注 |

#### type=200 (股票状态池) attr 位标志

这是最复杂的 attr 编码，包含状态池的各种功能开关：

| 位 | 掩码 | 出现次数 | 含义 | 说明书对应 |
|----|------|----------|------|-----------|
| bit7 | 0x80 | 30 | 未知标志G | - |
| bit9 | 0x200 | 220 | 显示到股票池总览(容器级) | type=2容器attr=0x200表示该容器显示在总览中；type=200的"显示到总览"复选框对应bit30 |
| bit12 | 0x1000 | 758 | 不删源状态中股票 | "不删源状态中股票" |
| bit13 | 0x2000 | 18 | 先清空目的状态 | "先清空目的状态" |
| bit15 | 0x8000 | 597 | 简易中间态 | "简易中间态"——启用后状态池变为极小节点(pos缩至6×6)，仅做中转传递不展示 |
| bit23 | 0x800000 | 131 | 列表基于前一状态计算收益 | 对照实验确认：选中"列表基于前一状态计算收益"时bit23被设置 |
| bit23 | 0x800000 | 131 | 列表基于前一状态计算收益 | 对照实验确认 |
| bit24 | 0x1000000 | 358 | 列表基于前一状态计算收益(高位) | 与bit23同时出现时表示完整的前一状态计算收益功能 |
| bit27 | 0x8000000 | 993 | 记录历史轨迹 | "记录历史轨迹"——勾选=bit27置位，取消=bit27清除 |
| bit28 | 0x10000000 | 221 | 收益分析计算模式标志 | 与bit24/bit23共存，表示收益分析的计算模式 |
| bit29 | 0x20000000 | 851 | 未知标志D | - |
| bit30 | 0x40000000 | 513 | 显示到股票池总览 | 对照实验确认：选中"显示到股票池总览"时bit30被设置 |
| bit31 | 0x80000000 | 309 | 未知标志F | - |

**常见 attr 值组合：**

| attr值 | 十六进制 | 含义组合 |
|--------|----------|----------|
| 0 | 0x0 | 基本状态池，无特殊选项 |
| 512 | 0x200 | 仅"显示到总览" |
| 4096 | 0x1000 | 仅"不删源状态中股票" |
| 32768 | 0x8000 | 仅"简易中间态" |
| 134217728 | 0x8000000 | 仅"记录历史轨迹" |
| 134221824 | 0x8001000 | "记录历史轨迹" + "不删源状态中股票" |
| 536875008 | 0x20001000 | bit29 + "不删源状态中股票" |
| 671088640 | 0x28000000 | "记录历史轨迹" + bit29 |
| 1744830464 | 0x68000000 | "记录历史轨迹" + bit29 + bit30 |

#### type=201 (转移条件) attr 位标志

| 位 | 掩码 | 出现次数 | 含义 | 确认状态 |
|----|------|----------|------|---------|
| bit9 | 0x200 | 241 | 显示到股票池总览 | 推断 |
| bit11 | 0x800 | 920 | 基本条件标志 | 推断 |
| bit16 | 0x10000 | 2 | 未知 | 未确认（极罕见） |
| bit17 | 0x20000 | 1 | 未知 | 未确认（极罕见） |
| bit18 | 0x40000 | 20 | 反向转移/条件池转移 | 推断 |
| **bit19** | **0x80000** | **155** | **✅ 输出成分股** | **已确认(2026-06-10对照实验)** |
| **bit20** | **0x100000** | **2243** | **✅ 不删除状态中股票(保留源池)** | **已确认(2026-06-10对照实验)** |
| **bit21** | **0x200000** | **882** | **✅ 转移前先清空目的状态池** | **已确认(2026-06-10对照实验)** |
| bit22 | 0x400000 | 43 | 横向统计/条件池转移 | 推断 |

> **⚠️ 重要更正 (2026-06-10)**: bit19/20/21 的含义在v14.0中基于2919个XML的统计推断为"板块成员/指标条件/排序条件"，经14个单变量对照实验**全部推翻**。
> 真实含义来自转移条件设置窗口底部的三个独立复选框，三者可任意组合（位或运算）。
> bit20出现率高达76.8%的原因是DZH设计器默认勾选"不删除状态中股票"。

**常见 attr 值组合：**

| attr值 | 十六进制 | 位组合 | 含义 |
|--------|----------|--------|------|
| 0 | 0x0 | (无) | 直接公式模式，三选项全关 |
| 2048 | 0x800 | bit11 | 仅基本条件标志 |
| 524288 | 0x80000 | bit19 | 仅输出成分股 |
| **1048576** | **0x100000** | **bit20** | **仅不删除源池（最常见，因默认勾选）** |
| **2097152** | **0x200000** | **bit21** | **仅清空目的状态池** |
| 1572864 | 0x180000 | bit19+bit20 | 输出成分股 + 不删除源 |
| 2621440 | 0x280000 | bit19+bit21 | 输出成分股 + 清空目的 |
| 3145728 | 0x300000 | bit20+bit21 | 不删除源 + 清空目的 |
| 3670016 | 0x380000 | bit19+bit20+bit21 | 三选项全开 |
| 1050624 | 0x100800 | bit11+bit20 | 基本条件 + 不删除源 |
| 3147776 | 0x300800 | bit11+bit20+bit21 | 基本条件 + 不删除源 + 清空目的 |

#### type=202 (备选池) attr 位标志

| 位 | 掩码 | 出现次数 | 推断含义 |
|----|------|----------|----------|
| bit7 | 0x80 | 307 | 备选池基本标记 |
| bit9 | 0x200 | 10 | 显示到股票池总览 |
| bit0-6 | 各种 | 少量 | 样式标记 |

| attr值 | 含义 |
|--------|------|
| 128 (0x80) | 标准备选池 |
| 640 (0x280) | 显示到总览的备选池 |
| 152 (0x98) | 带额外样式标记的备选池 |

---

## 四、各Cell类型详细定义

### 4.1 Type 1: 文字标签

**用途**: 标题、说明文字、标注、时间标签、美化界面

**属性语义**:
| 属性 | 含义 |
|------|------|
| pos(x1,y1,x2,y2) | 矩形包围框 |
| clr | 文字颜色(十进制RGB) |
| attr | 字体样式标志位（见3.4节） |
| text | 文字内容, `\n` 为换行符 |
| url | 超链接URL（仅17个实例） |

**特征**: 纯展示, 不参与流程计算

**实例**:
```
超赢1号: 标题"大智慧超赢股票池"(attr=89), 模型名"超赢一号"(attr=90),
         风险提示(attr=21), 流程说明文字(attr=0)
超赢7号: 五个入池原理说明(label attr=98在每列上方), 版本号(attr=20)
金色两点半: 五个时间标签 10:00/11:00/14:00/14:20/14:30(attr=89/90),
           流程说明(attr=85/88)
```

### 4.2 Type 2: 容器

**用途**: 视觉布局容器，用于背景框、状态池容器、输出池容器

**属性语义**:
| 属性 | 含义 |
|------|------|
| pos(x1,y1,x2,y2) | 矩形包围框 |
| clr | 底色 (1579032=暗绿, 2565927=墨绿, 3092271=橄榄, 6184542=浅蓝绿, 4227327=亮蓝) |
| attr | 0=普通容器, 512=显示到股票池总览的容器 |
| text | 通常为空 |

**关键发现**:
- Type 2 是**纯视觉容器**，不参与流程计算
- attr=512 的容器对应显示到总览的状态池/输出池
- 超赢7号**没有 type 2**，证明它使用 type 3（状态列）做布局

**实例分布**:
```
超赢1号:    7个 type 2 (3个attr=512显示到总览, 4个attr=0普通容器)
超赢7号:    0个 (使用 type 3 状态列做布局)
金色两点半: 6个 type 2 (全部attr=512, 对应5个时间段容器+1个最终合并容器)
```

### 4.3 Type 3: 状态列

**用途**: 纵向布局容器，用于在股票池界面中组织状态池的列式排列

**属性语义**:
| 属性 | 含义 |
|------|------|
| pos(x1,y1,x2,y2) | 竖直条状包围框(高度远大于宽度) |
| clr | 底色 (-1=默认, 15263718=浅红) |
| attr | 0=普通状态列, 512=扩展态(最左列, 亮色强调) |
| text | 通常为空 |

**关键发现**:
- Type 3 是**纯视觉布局容器**，不参与流程计算
- 仅在超赢7号中出现 (5个: attr=512×1 + attr=0×4)
- 五个状态列**并行排列**: 从右到左按 pos x 坐标排列
  - id=141 (attr=512): x=4~139, 最左, clr=15263718(亮色强调)
  - id=140 (attr=0): x=178~284
  - id=139 (attr=0): x=306~414
  - id=138 (attr=0): x=433~634
  - id=142 (attr=0): x=656~998

### 4.4 Type 4: 丢弃池

**用途**: 暂存不满足条件的股票

**属性语义**:
| 属性 | 含义 |
|------|------|
| pos(x1,y1,x2,y2) | 矩形包围框 |
| clr | 底色 |
| attr | 128=标准丢弃池, 640=显示到总览 |
| text | 丢弃池名称 |

### 4.5 Type 5: 执行顺序

**用途**: 执行顺序的视觉标注控件，在设计界面上显示编号箭头

**操作方式**:
1. 在设计状态下，点击工具栏的"执行顺序"按钮
2. 所有连接到转移条件的边上，在边的两端点附近会出现一个数字
3. 数字代表该转移条件的执行顺序（0=最先执行，1=其次，以此类推）
4. 用鼠标点击每条转移边，第一次点击的边编号为0，第二次为1，直到所有转移边编号完成
5. 如果点击已有编号的边，该边的编号与当前编号交换（冲突时自动调整）

**重要更正：执行顺序的实际记录方式**

执行顺序**不是**通过type=5 cell记录的，而是通过`<flow>`元素在XML中的**排列顺序**记录（详见第六章）。

**type=5 cell属性**:

| 属性 | 含义 |
|------|------|
| pos(x1,y1,x2,y2) | 箭头坐标，方向由坐标反转编码：x1>x2=左向箭头，y1>y2=向上箭头 |
| attr | 0=设计视图, 512(0x200)=总览视图（双层显示） |
| text | 始终为空字符串（26个样本全部为空） |
| clr | 颜色 |

**关键发现**:
- type=5 是**视觉标注控件**，不参与执行顺序的持久化存储
- 执行顺序由`<flow>`元素在XML中的排列顺序决定
- 仅9/206个XML文件包含type=5 cell，共26个type=5 cell
- **所有26个type=5 cell的text属性均为空字符串**
- 零条flow连接到/来自type=5

### 4.6 Type 6: 连接箭头

**用途**: 连接两个控件的流程线（纯装饰）

**属性语义**:
| 属性 | 含义 |
|------|------|
| pos(x1,y1,x2,y2) | 线的起止坐标, y1==y2 则为水平线 |
| clr | 线的颜色 |
| attr | 始终为 0 |
| text | 始终为空 |

**实例**:
```
超赢1号:    id=8, pos="252,46,303,46" — 水平分隔线(标题下方)
超赢7号:    id=156, pos="268,46,316,46" — 水平分隔线
金色两点半: 无 type 6
```

### 4.7 Type 200: 股票状态池

**用途**: 存储经过筛选后进入的股票列表, 最核心的池类型

**属性语义**:
| 属性 | 含义 | 格式 | 出现次数 | 常见值 |
|------|------|------|----------|--------|
| pos(x1,y1,x2,y2) | 显示区域(矩形) | - | 2876 | - |
| clr | 底色 | GDI整数 | 2876 | - |
| attr | 池属性标志位 | 位标志(见3.4节) | 2876 | - |
| text | 池名称 | 字符串 | 2876 | - |
| hold | 股票保留时间(秒) | 整数 | 2876 | `"432000"`(5天,901次) — **值含义由deltype决定，见下方"删除时间体系"** |
| col | 显示列定义 | 逗号分隔整数 | 2876 | `"2,-1,-2,-3,7,14,8,10,17,45"` |
| width | 列宽(像素) | 逗号分隔整数 | 519 | `"55,42,42,42,42,42,42,42,42,42"` |
| histana | 历史分析数据点数 | 整数 | 363 | `"1000"`(351次) |
| wizd | 状态说明文字 | 字符串 | 87 | 自定义描述 |
| deltype | 删除类型(时间单位枚举) | 整数 | 119 | `"0"`(默认,自然日), `"1"`~`"4"` |
| endtime | 删除结束时间(绝对时间参数) | 整数 | 119 | `"0"`(相对模式), `HHMMSS`(如`150000`=15:00:00) |
| delstocktype | 删除时机模式 | 整数 | 119 | `"0"`(相对时间/入池后N单位), `"1"`(指定交易时间删除) |
| staattr | 状态属性标志 | 整数 | 45 | `"1"` |
| enter | 入池执行动作 | 位标志整数 | 21 | `"268445456"`, `"10000"` |
| exit | 出池执行动作 | 位标志整数 | 16 | `"805306368"` |
| stocknum | 股票数量 | 整数 | 8 | - |
| tmpl | 模板 | 字符串 | 1 | - |

**子元素 `<stk>`**:
```xml
<stk label="SH600000" t="1369909255" p="10.54"/>
```
- label: 股票代码(市场前缀), t: 入池时间戳, p: 入池价格

**hold 保留时长对照表（⚠️ 当 deltype=0 时，hold 值为秒数；当 deltype>0 时，hold 值的含义由 deltype 决定，见下方删除时间体系）**:

| hold值(秒) | 天数 | 含义 | 出现次数 |
|------------|------|------|----------|
| 2 | ~0天 | 即时删除 | 36 |
| 59 | ~1分钟 | 极短保留 | 30 |
| 300 | 5分钟 | 超短保留 | 37 |
| 3600 | 1小时 | 短时保留 | 37 |
| 18000 | 5小时 | 半天保留 | 25 |
| 36000 | 10小时 | 日内保留 | 60 |
| 43200 | 12小时 | 半天保留 | 66 |
| 54000 | 15小时 | 日内保留 | 162 |
| 64800 | 18小时 | 日内保留 | 304 |
| 72000 | 20小时 | 日内保留 | 29 |
| 86400 | 1天 | 1天保留 | 325 |
| 172800 | 2天 | 2天保留 | 68 |
| 259200 | 3天 | 3天保留 | 64 |
| 432000 | 5天 | 5天保留(默认) | 901 |
| 864000 | 10天 | 10天保留 | 91 |
| 1296000 | 15天 | 15天保留 | 37 |
| 1728000 | 20天 | 20天保留 | 106 |
| 2592000 | 30天 | 30天保留 | 120 |
| 4320000 | 50天 | 50天保留 | 53 |
| 43200000 | 500天 | 永久保留 | 49 |

### 4.7a Type=200 删除时间体系（✅ 已确认 — 2026-06-10 对照实验）

**基于6个对照实验XML文件的完整解密。** 每个文件对应DZH设计器中状态池属性窗口的"删除时间"设置，逐一修改选项后保存对比。

#### 三字段协作模型

```
┌─────────────────────────────────────────────────────┐
│              删除时间 = deltype + hold + endtime     │
│                                                     │
│  deltype (0~4) → 决定 hold 的单位                    │
│  hold        → 时间数量（单位由deltype决定）          │
│  endtime     → 绝对时间参数（仅delstocktype=1时有效） │
│  delstocktype(0/1) → 相对模式 vs 绝对指定时间模式    │
└─────────────────────────────────────────────────────┘
```

#### deltype 枚举 — 时间单位

| deltype值 | 界面名称 | hold含义 | 实际TTL计算 | 对照实验验证 |
|-----------|---------|---------|------------|-------------|
| **0** | 进入N天后删除 | 自然日数 | `hold` 秒（直接为秒数） | ✅ 进入5天后: hold=432000(5×86400s) |
| **1** | 进入N个交易日后删除 | 交易日数 | `hold / 86400` 个交易日后 | ✅ 进入5交易日后: hold=432000, deltype=1 |
| **2** | N小时后删除 | 小时数 | `hold / 3600` 小时后 | ✅ 5小时后: hold=18000(5×3600), deltype=2 |
| **3** | N分钟后删除 | 分钟数 | `hold / 60` 分钟后 | ✅ 5分钟后: hold=300(5×60), deltype=3 |
| **4** | N秒后删除 | 秒数 | `hold` 秒后 | ✅ 5秒后: hold=5, deltype=4 |

**关键发现**：
- **deltype=0 与 deltype=4 的区别**: deltype=0 的 hold=432000 表示"进入5天后"（自然日），而 deltype=4 的 hold=5 表示"5秒后"。两者都存储为秒数但语义不同——deltype=0 是"自然日模式"，deltype=4 是"精确秒模式"。在非交易时段（周末/节假日），两者的行为不同：deltype=0 跨越非交易日，deltype=4 精确到秒。
- **hold 值始终是秒数**: 无论 deltype 为何值，hold 字段中存储的都是换算后的总秒数。设计器界面显示的是人类可读的数量+单位，保存时统一转为秒。

#### delstocktype 枚举 — 删除时机模式

| delstocktype值 | 名称 | 行为 | endtime含义 | 对照实验验证 |
|---------------|------|------|------------|-------------|
| **0** | 相对时间模式（入池后N单位时间删除） | 从入池时刻开始计时，到期后自动删除 | 0（不使用） | ✅ 所有相对时间实验均为0 |
| **1** | 指定交易时间删除 | 在每个交易日的指定绝对时间检查并删除 | ✅ 已确认: endtime = 3600×HH − 900×MM + SS | ✅ 3个指定时间实验验证 |

**delstocktype=1 的组合效果**：
- 当 `delstocktype=1` 时，删除逻辑变为："在每个交易日的 `endtime` 指定的时刻，检查是否满足 `deltype+hold` 条件"
- 例如 `deltype=3`(分钟) + `hold=300`(5分钟) + `delstocktype=1` + `endtime=9050`(10:30:50) = 在每日10:30:50检查，超5分钟则删除

#### endtime 编码格式（✅ 已确认 — 2026-06-10 三样本验证）

**编码公式**：

```
endtime = 3600 × HH − 900 × MM + SS
        = 900 × (4×HH − MM) + SS
```

**解码函数**：

```
SS   = endtime % 100                    (末2位 = 秒)
prefix = endtime // 100                 (前缀 = 编码后的时分)
HH   = (prefix + 9*MM) // 36           (需遍历MM=0~59反查，或用下述简化方法)
```

**更实用的解码**（直接从endtim反推HH:MM:SS）：
由于公式中 HH 和 MM 耦合，推荐按以下方式解码：
```python
def decode_endtime(et: int) -> tuple:
    """将endtime解码为(HH, MM, SS)。"""
    ss = et % 100
    prefix = et // 100  # = 9*(4*HH - MM)
    # 遍历合法MM(0~59)找到使( prefix/9 + MM )能被4整除的组合
    for mm in range(60):
        remainder = prefix + 9 * mm
        if remainder >= 0 and remainder % 36 == 0:
            hh = remainder // 36
            if 0 <= hh <= 23:
                return (hh, mm, ss)
    raise ValueError(f"Invalid endtime: {et}")
```

**完整对照实验数据**：

| 文件名 | 界面设置 | endtime值 | 公式验证 | 备注 |
|--------|---------|-----------|---------|------|
| 指定8:00:00删除 | 08:00:00 | **0** | 3600×8−900×0+0=28800≠0 | ⚠️ 可能是开盘前无效时间→存为默认值0 |
| **指定10:30:50** | **10:30:50** | **9050** | 3600×10−900×30+50=**9050** ✅ | ✅ 首个非零endtime样本 |
| **指定14:30:10** | **14:30:10** | **23410** | 3600×14−900×30+10=**23410** ✅ | ✅ 下午时段验证通过 |

#### 完整对照实验数据

| 文件名 | 界面设置 | deltype | hold | endtime | delstocktype | TTL解读 |
|--------|---------|--------|------|---------|-------------|---------|
| 删除时间5分钟后删除.png/xml | "N分钟后删除" = 5 | **3** | 300 (=5×60) | 0 | 0 | 入池后5分钟(300秒)自动删除 |
| 删除时间5秒后删除.png/xml | "N秒后删除" = 5 | **4** | 5 | 0 | 0 | 入池后5秒自动删除 |
| 删除时间5小时后删除.png/xml | "N小时后删除" = 5 | **2** | 18000 (=5×3600) | 0 | 0 | 入池后5小时(18000秒)自动删除 |
| 删除时间进入5交易日后删除.png/xml | "进入N个交易日后删除" = 5 | **1** | 432000 (=5×86400) | 0 | 0 | 入池后第5个交易日删除 |
| 删除时间进入5天后删除.png/xml | "进入N天后删除" = 5 | **0** | 432000 (=5×86400) | 0 | 0 | 入池后5个自然日删除 |
| 删除时间指定交易时间8:00:00删除.png/xml | "指定交易时间" = 08:00:00 | **3** | 300 | 0 | **1** | 每日08:00:00检查，超5分钟则删除 |

#### 与 TDX 的对应关系

DZH 的 `deltype` 与 TDX 的 `ndeltype`（定义在 `schemas.py TdxPsattModel`）功能等价：

| DZH字段 | TDX字段 | 值域 | 说明 |
|---------|---------|------|------|
| deltype | ndeltype | 0~4 | 时间单位枚举（完全一致） |
| hold | ndelnum × 单位换算 | 整数 | DZH已换算为秒，TDX存数量+单位分开 |
| delstocktype | (无直接对应) | 0/1 | DZH独有：相对/绝对时间模式 |
| endtime | (无直接对应) | HHMMSS/0 | DZH独有：绝对时间参数 |

**col 列ID对照表**:

| 列ID | 含义 | 出现次数 |
|------|------|----------|
| 2 | 代码 | 2877 |
| -1 | 名称 | 2698 |
| -2 | 最新价 | 2620 |
| -3 | 涨跌幅 | 2651 |
| -5 | 涨跌额 | 356 |
| -6 | 成交量 | 9 |
| 1 | 序号 | 530 |
| 7 | 入池时间 | 2714 |
| 8 | 现价 | 2168 |
| 10 | 收益率 | 2143 |
| 14 | 入池价 | 2838 |
| 17 | 最大收益 | 2548 |
| 24 | 换手率 | 47 |
| 45 | 保留天数 | 2563 |
| 101 | DDX连续飘红天数 | 16 |
| 108 | 量比 | 13 |
| 251 | 特大单买入 | 13 |
| 285 | 大单买入 | 11 |
| 287 | BBD | 49 |
| 401 | DDX | 223 |

**enter/exit 执行动作位标志**:

**enter (入池执行动作)：**

| 值 | 十六进制 | 含义 |
|----|----------|------|
| 10000 | 0x2710 | 买入指定股数 |
| 268445456 | 0x10002710 | 买入指定股数 + 发出预警声 |
| 805306368 | 0x30000000 | 买入指定金额 |

**exit (出池执行动作)：**

| 值 | 十六进制 | 含义 |
|----|----------|------|
| 805306368 | 0x30000000 | 卖出股票 |
| 268445456 | 0x10002710 | 卖出 + 发出预警声 |
| 536880912 | 0x20002710 | 卖出 + 弹出预警提示 |
| 805316368 | 0x30002710 | 卖出 + 高亮闪烁显示 |
| 10000 | 0x2710 | 卖出指定股数 |

**动作位标志分解**:
- 0x2710 (10000): 基础买入/卖出动作
- 0x10000000: 发出预警声
- 0x20000000: 弹出预警提示
- 0x30000000: 卖出股票（基础卖出动作）

**实例分布**:
```
超赢1号:    9个 — 风险控制/热点板块/热点股票/长线资金/短线资金/异动扫描/超赢1号池/英雄榜/观察池
超赢7号:    13个 — 初步筛选/超赢备选/超赢价值A/超赢强势/超赢价值B/超赢买点/超赢追踪/超赢7号/英雄榜/观察池/强势整理/超赢分析/超赢整理
金色两点半: 9个 — 初步筛选/基准/一次考量/二次考量/预备池/金色两点半/英雄榜/观察池/全程离散
```

### 4.8 Type 201: 转移条件

**用途**: 筛选器，用公式决定哪些股票可以通过

**属性语义**:
| 属性 | 含义 | 格式 | 出现次数 | 常见值 |
|------|------|------|----------|--------|
| pos(x1,y1,x2,y2) | 小矩形, 位于两个 pool 之间的连接线位置 | - | 2919 | - |
| attr | 条件类型标志位(见3.4节+§4.8a) | 位标志 | 2919 | - |
| clr | -1 (默认, 不显示) | GDI整数 | 2919 | - |
| text | 通常为空 | 字符串 | 2919 | - |
| inditype | 指标类型 | 字符串 | 2913 | `"indi"`(2313), `"ref"`(600) |
| crc | 公式校验和 | 整数字符串 | 2913 | 各种CRC值 |
| indi | 公式内容(Base64) | Base64字符串 | 2913 | 编码后的公式（**含分析周期信息，见§4.8b**） |
| sorttype | 排序类型 | 整数 | 589 | `"1"`, `"5"`, `"7"`, `"10"`, `"20"` |
| indiparam | 指标参数 | 字符串 | 20 | - |

**inditype 说明**:
- `"indi"`: 指标条件（自定义公式）
- `"ref"`: 引用条件（引用其他指标）

**sorttype 排序类型（✅ 已确认有符号语义 2026-06-10）**:

| 值 | 含义 | UI显示 |
|----|------|--------|
| (空) | 不排序，使用指标条件判断 | "选择满足以下条件的股票" |
| 正数 N (1/5/7/10/20/200) | 排序取**前N名**(升序) | "前 N 名的股" |
| **负数 -N (-10等)** | 排序取**后N名**(降序) | "后 N 名的股" |

**三种转移条件类型（说明书第20页）**:
1. 指标是或非转移条件：满足/不满足某指标公式
2. 数值排序转移条件：按指标排序，取前N名/后N名
3. 板块成员转移：筛选特定行业/板块成员

#### §4.8a Type=201 转移选项体系（✅ 已确认 2026-06-10）

转移条件设置窗口底部有 **三个独立复选框**，控制股票通过条件后的处理方式。
三者编码在 `cell.attr` 的 bit19~bit21 中，可任意组合（位或运算）。

**对照实验数据（14个XML + 5个截图，公式均为 `CLOSE < 10;`）**:

| # | 实验文件 | ☐不删源状态中股票 | ☐转移前先清空目的状态 | ☐输出成分股 | attr值 | 变量 |
|---|---------|-------------------|---------------------|------------|--------|------|
| 1 | 直接输入条件公式日线.xml | ☐ | ☐ | ☐ | **0** | 基准(全关) |
| 2 | 输出成分股.xml | ☐ | ☐ | ✓ | **524288** (0x80000) | 仅✓输出成分股 |
| 3 | 不删源状态中股票.xml | ✓ | ☐ | ☐ | **1048576** (0x100000) | 仅✓不删除源 |
| 4 | 转移前先清空目的状态.xml | ☐ | ✓ | ☐ | **2097152** (0x200000) | 仅✓清空目的 |
| 5 | 季线/分时/1分钟/半年线/年线/多日/周线/分笔成交.xml | ✓ | ☐ | ☐ | **1048576** (不变!) | 仅改分析周期 |

**位域定义**:

| Bit | 掩码 | 复选框名称 | 默认值 | 功能说明 |
|-----|------|-----------|--------|---------|
| bit19 | 0x80000 (524288) | 输出成分股 | 关 | 当源节点是板块时，输出其成分股而非板块本身 |
| bit20 | 0x100000 (1048576) | 不删除状态中股票 | **开(默认)** | 股票通过条件后保留在源池中；关闭则从源池移除 |
| bit21 | 0x200000 (2097152) | 转移前先清空目的状态 | 关 | 将股票写入目标池前先清空目标池全部内容 |

> **注意**: "不删除状态中股票"在DZH设计器中**默认勾选**，这就是为什么2919个真实XML中76.8%(2243个)的type=201 cell都带有bit20标志。

#### §4.8b Type=201 分析周期编码（✅ 已确认 2026-06-10）

转移条件设置窗口的"分析周期"下拉框提供 **14种周期选项**：

| 周期 | 说明 |
|------|------|
| 1分钟 | 1分钟K线 |
| 5分钟 | 5分钟K线 |
| 15分钟 | 15分钟K线 |
| 30分钟 | 30分钟K线 |
| 60分钟 | 60分钟K线 |
| 日线 | 日K线（最常用，默认） |
| 周线 | 周K线 |
| 月线 | 月K线 |
| 多日(线) | 多日线 |
| 季线 | 季K线 |
| 半年线 | 半年K线 |
| 年线 | 年K线 |
| 分笔(成交) | Tick级分笔成交 |
| 分时线 | 分时走势图 |

**关键发现**: 分析周期 **不存储在任何独立的XML属性中**。14个单变量周期变更实验证实：
- 所有实验的 `attr` 值保持 **1048576** 不变
- 只有 `crc`(校验和) 和 `indi`(Base64公式blob) 随周期变化
- 结论：**分析周期编码在 `indi` 属性的Base64压缩数据内部**

> 这解释了为什么之前对206个XML的统计分析始终找不到独立的分析周期字段——它确实不存在于XML属性层，而是嵌入在公式数据中。

#### §4.8c Type=201 公式类别体系 + sorttype 有符号语义（✅ 已确认 2026-06-10）

转移条件设置窗口顶部有两个**单选按钮**决定工作模式：

| 模式 | 单选按钮文本 | sorttype | 功能 |
|------|-------------|----------|------|
| **条件筛选模式** | "选择满足以下条件的股票" | 无/空 | 股票满足指标条件即通过 |
| **排序筛选模式** | "选择指标排序" | 有值(±N) | 按指标排序后取前/后N名 |

> 切换单选按钮时，界面右侧出现不同的参数区域。条件模式显示公式编辑区；排序模式显示排序数量输入框。

**sorttype 有符号语义（7个XML对照实验确认）**:

| sorttype值 | 含义 | UI显示文字 |
|-----------|------|-----------|
| 正数 N (如1/5/10/20/200) | 取**前N名**(升序) | "前 N 名的股" |
| **负数 -N (如-10)** | 取**后N名**(降序) | "后 N 名的股" |
| 无/空 | 不排序（条件筛选模式） | — |

> **关键发现**: sorttype 使用**有符号整数**。正数=从高到低取前N名，负数=从低到高取后N名。
> 这解释了为什么之前统计中 sorttype 值域包含负数的情况。

**"选择公式"选项卡的5种公式类别**:

点击"选择公式"标签后，左侧显示5个公式类别树：

| 类别 | 图标 | 子类别示例 | 参数形式 | inditype |
|------|------|-----------|---------|----------|
| **技术指标** | 📊 | 全部/常用/自编/时间 | 指标名称+参数+周期+线号(如MA1上穿MA2) | ref |
| **条件选股** | 📋 | 条件选股条件列表(KDJ/BIAS/BOLL/DMI/KD/MA/MACD等) | 条件名称+参数+分析周期 | ref |
| **交易系统** | 🔄 | 交易系统列表(CCI等) | 系统名称 | ref |
| **基本面条件** | 💰 | 财务数据(发布日期/每股收益等) | 数据字段+比较符+阈值 | ref |
| **动态行情** | 📈 | 实时行情字段(昨收/最高/最低/涨幅/成交量等) | 字段名+比较符+阈值 | ref |

**对照实验数据（公式均为 `CLOSE < 10;` 或各类别默认）**:

| # | 实验文件 | 类别选择 | attr | inditype | sorttype | crc变化 |
|---|---------|---------|------|----------|----------|---------|
| 1 | 条件选股kdj.xml | 条件选股→KDJ(9,3,3) | 0 | ref | — | -2026069828 |
| 2 | 技术指标ma1上穿ma2.xml | 技术指标→MA1上穿MA2 | 0 | ref | — | 19717325 |
| 3 | 交易系统cci.xml | 交易系统→CCI | 0 | ref | — | 94880071 |
| 4 | 基本面条件发布日期大于5.xml | 基本面→财务数据→发布日期>5 | 0 | ref | — | 1497276158 |
| 5 | 动态行情最高大于5.xml | 动态行情→昨收>5 | 0 | ref | — | -183831478 |
| 6 | 指标排序涨幅前10名.xml | 排序→动态行情→涨幅→前10 | 0 | ref | **10** | 31320701 |
| 7 | 指标排序涨幅后10名.xml | 排序→动态行情→涨幅→后10 | 0 | ref | **-10** | 31320701 |

> **重要结论**: 公式类别选择**不影响attr值**（全部保持0）。所有信息编码在 `indi`(Base64) + `crc`(校验和) 中。
> 排序模式的两个实验(#6/#7)使用相同指标(涨幅)，crc相同(31320701)，仅sorttype符号不同(+10 vs -10)。

**股票池专用函数（说明书第19页）**:

| 函数 | 说明 |
|------|------|
| ENTERPOOLPRICE(N) | 进入之前第N个状态时的股票价格，N=0=当前状态 |
| ENTERPOOLBARS(N) | 进入之前第N个状态到目前的周期数 |
| ENTERPOOLPARAM(N) | 从外部PAR文件导入参数 |

### 4.9 Type 202: 备选池

**用途**: 定义股票池的初始备选范围（市场/板块/个股/自选组的任意组合）

**属性语义**:
| 属性 | 含义 | 格式 | 常见值 |
|------|------|------|--------|
| attr | 128=固定值(备选池标志) | 位标志 | `"128"` |
| pos(x1,y1,x2,y2) | 显示区域 | - | - |
| clr | 底色(16777227/28/25) | GDI整数 | - |
| text | 显示名称 | 字符串 | - |
| **attrtext** | **备选范围定义(Tab分隔)** | **6种格式组合** | **见§4.9a** |
| **reload** | **重载模式编码** | **5种模式枚举** | **见§4.9b** |
| lastload | 最后加载时间戳 | Unix时间戳 | `"0"`(51次), 其他为实际时间戳 |

> **⚠️ 重要更正 (2026-06-14)**: 原 §4.9 仅列出 2 种 attrtext 格式（市场 SH#* / 板块 B$#*），经 12 个对照实验 XML 验证实际存在 **6 种条目类型**。原将 reload 简单描述为"秒数"，实际是 **5 种语义化重载模式枚举**。详见 §4.9a 和 §4.9b。

#### §4.9a attrtext 格式完全解密（✅ 已确认 — 2026-06-14 对照实验）

**基于 12 个单变量/多变量对照实验 XML 的完整解密。**

##### attrtext 总体格式

`attrtext` 是 **Tab(`\t`)分隔** 的字符串，每个 Tab 分隔的条目代表一个备选来源。支持同时选择多个来源（混合个股、市场、板块、自选组）。

**UI 入口**: 双击画布上的备选池控件 → 弹出「股票池备选股设置」对话框 → 左侧三栏树形选择器勾选 → 点击「加入」→ 右侧列表显示已选项 → 「确定」保存。

##### 6 种条目类型

| # | 类型 | 格式模式 | 正则表达式 | 示例 | UI路径 |
|---|------|---------|-----------|------|--------|
| 1 | **个股** | `{SH\|SZ}{6位数字}` | `^(SH\|SZ)\d{6}$` | `SH600000`, `SZ000001`, `SH600018` | 加入个股按钮 → 弹出股票选择器 |
| 2 | **市场** | `{Exchange}#{Name}` | `^(SH\|SZ)#.+$` | `SH#上证A股`, `SZ#深证A股`, `SZ#中小企业`, `SZ#创业板` | 市场 → 上海证券交易所 → 上证A股 |
| 3 | **自选组** | `BLK-{Name}` | `^BLK-自选股\d+$` | `BLK-自选股1`, `BLK-自选股2` | 自选股 → 自选股1 |
| 4 | **概念板块** | `BLK-概念{Name}{7位ID}` | `^BLK-概念\S+\d{7}$` | `BLK-概念新材料0700008` | 板块 → 大智慧概念 → 新材料 |
| 5 | **行业板块** | `BLK-{行业名}{7位ID}` | `^BLK-(?!自选股\|概念)[^\d]+\d{7}$` | `BLK-农业0110324`, `BLK-林业0110323` | 板块 → 证监会行业 → 农业 |
| 6 | **行业经典** | `BLK-{Name}` (无数字ID) | `^BLK-(?!自选股\|概念)[^\d]+$` | `BLK-证监板块`, `BLK-基金重仓` | 板块 → 大智慧行业(经典) → 证监板块 |

> **命名规则总结**:
> - `SH`/`SZ` + 6位数字 → **个股直接引用**
> - `SH#`/`SZ#` + 名称 → **交易所市场**
> - `BLK-自选股` + 数字 → **用户自选分组**
> - `BLK-概念` + 名称 + 7位ID → **DZH概念板块**（ID为大智慧内部编号，如0700008）
> - `BLK-` + 行业名 + 7位ID → **证监会行业分类**（ID为证监会行业代码，如0110324=农业）
> - `BLK-` + 其他名称 → **大智慧经典行业分类**

##### 完整对照实验数据（12 个 XML 样本）

| # | 文件名 | attrtext 值 | 含义 | 截图验证 |
|---|--------|------------|------|---------|
| 1 | 备选池.xml (默认) | `SH600000\tSH600018` | 2只个股：浦发银行+上港集团 | ✅ 备选池.png |
| 2 | 备选池加入个股.xml | `SH600000\tSH600018` | 同上（加入个股后reload变0） | ✅ 备选池加入个股.png |
| 3 | 备选池自选股自选股1.xml | `BLK-自选股1` | 仅选择自选股1分组 | ✅ 备选池自选股自选股1.png |
| 4 | 备选池板块大智慧概念板块新材料.xml | `BLK-概念新材料0700008` | 大智慧概念→新材料 | ✅ 备选池板块大智慧概念板块新材料..png |
| 5 | 备选池板块证监会行业农业.xml | `BLK-农业0110324` | 证监会行业→农业 | ✅ 备选池板块证监会行业农业.png |
| 6 | 备选池市场上海证券交易所上证A股.xml | `SH#上证A股` | 市场→上海证券交易所→上证A股 | ✅ 备选池市场上海证券交易所上证A股.png |
| 7 | 备选池不重载.xml | `SH600000\tSH600018\tSH#上证A股` | 混合：2个股+1市场 | ✅ 备选池不重载.png |
| 8 | 备选池每隔一定时间.xml | `SH600000\tSH600018\tSH#上证A股` | 同上（仅reload不同） | ✅ 备选池每隔一定时间.png |
| 9 | 备选池每天指定时间.xml | `SH600000\tSH600018` | 2只个股（默认状态） | ✅ 备选池每天指定时间.png |
| 10 | 备选池文件载入时.xml | `SH600000\tSH600018\tSH#上证A股` | 混合（仅reload不同） | ✅ 备选池文件载入时.png |
| 11 | 备选池上证A股农业新材料.xml | `BLK-自选股1\tSH#上证A股\tBLK-概念新材料0700008` | 混合：自选组+市场+概念板块 | ✅ 备选池上证A股农业新材料.png |
| 12 | 备选池加入个股界面.xml | （同#1） | 加入个股按钮弹出的选择器 | ✅ 备选池加入个股界面.png |

##### UI 对话框详细结构

双击备选池控件弹出「**股票池备选股设置**」对话框：

**左侧树形结构（3大类）**:

| 根节点 | 一级子节点 | 二级子节点示例 | attrtext 格式 |
|--------|----------|--------------|---------------|
| **市场** | 上海证券交易所 | 上证指数/上证A股/上证B股/上证基金/上证债券/上证转债 | `SH#上证A股` 等 |
| **板块** | 大智慧行业(经典) | H股/次新股/新材料/证监板块/基金重仓 | `BLK-新材料` 等 |
| | 大智慧概念 | (动态列表) | `BLK-概念{name}{id}` |
| | 证监会行业 | 林业/畜牧业/渔业/农业/煤炭开采... | `BLK-农业0110324` 等 |
| **自选股** | 自选股1~N | 用户自定义分组 | `BLK-自选股1` 等 |

**底部操作按钮**:

| 按钮 | 功能 | 对应操作 |
|------|------|---------|
| **加入** | 将左侧树中**勾选**的项添加到右侧列表 | 多选批量添加 |
| **加入个股** | 弹出独立股票选择器（按市场/板块浏览或搜索代码） | 添加单个股票 |
| **加入文件** | 弹出文件选择器，选择 .TXT 或 .PAR 文件导入股票列表 | 从文件批量导入 |
| **删除** | 从右侧列表移除选中项 | 移除已选项 |
| **确定** | 保存设置并关闭对话框 | 写入 attrtext + reload |
| **取消** | 放弃修改并关闭 | 不保存 |

#### §4.9b reload 重载模式体系（✅ 已确认 — 2026-06-14 对照实验）

**基于 6 个单变量 reload 模式切换实验 XML 的完整解密。**

##### reload 属性的功能

控制备选池在运行过程中**何时重新从数据源获取最新股票列表**。对应 UI 对话框底部的「重新载入股票时间」5 个单选按钮。

##### 5 种重载模式编码表

| # | UI 选项 | reload 值 | 十六进制 | reload_mode | reload_param | 行为说明 |
|---|--------|----------|---------|-------------|-------------|---------|
| 1 | **每次启动** | `-57387` | `0xFFFF4C5D` | `on_startup` | `null` | 程序每次启动时加载一次，之后不再刷新 |
| 2 | **每天指定时间** | `-57387` | `0xFFFF4C5D` | `daily_time` | `HHMMSS` | 每天在指定时刻重新载入（值同每次启动，需额外参数区分） |
| 3 | **不重载** | `2147483647` | `0x7FFFFFFF` | `never` | `null` | 永不自动重载，仅在首次加载时获取 |
| 4 | **每隔一定时间** | 正整数 | 如 `5440`=0x153F | `interval` | `秒数` | 每隔 N 秒自动刷新一次（如 5440秒≈91分钟） |
| 5 | **文件载入时** | `2147483646` | `0x7FFFFFFE` | `on_file_load` | `null` | 仅当 XML 文件被重新载入到内存时刷新 |

> **⚠️ 注意**: "每次启动"和"每天指定时间"的 reload 值相同(-57387)，区别可能在于 DZH 内部是否有额外的定时参数存储在其他位置。当前实现中将 `-57387` 默认解码为 `on_startup`。

##### 实验数据验证表

| # | 文件名 | UI选择的选项 | reload 值 | 解码结果 | 截图验证 |
|---|--------|------------|----------|---------|---------|
| 1 | 备选池.xml | 默认(每次启动) | `-57387` | on_startup | ✅ 备选池.png |
| 2 | 备选池加入个股.xml | 默认(加入后不变) | `0` | on_startup(值为0→默认) | ✅ 备选池加入个股.png |
| 3 | 备选池不重载.xml | ● 不重载 | `2147483647` | never | ✅ 备选池不重载.png |
| 4 | 备选池每隔一定时间.xml | ● 每隔一定时间 [1:30:40] | `5440` | interval, param=5440 | ✅ 备选池每隔一定时间.png |
| 5 | 备选池每天指定时间.xml | ● 每天指定时间 [15:56:27] | `-57387` | daily_time/on_startup | ✅ 备选池每天指定时间.png |
| 6 | 备选池文件载入时.xml | ● 文件载入时 | `2147483646` | on_file_load | ✅ 备选池文件载入时.png |

> **关于"每隔一定时间"的时间格式**: UI 显示为 `H:MM:SS`（如截图中的 `1:30:40`），存储的 reload 值为总秒数（5440 = 1×3600 + 30×60 + 40）。"每天指定时间"的 UI 显示为 `HH:MM:SS`（如 `15:56:27`），但 reload 值仍为 `-57387`，说明具体时间可能编码在其他位置或使用相同的默认值。

### 4.10 Type 203: 未知类型

仅3个实例，用途完全不明。出现于206个XML文件中的极少数文件。

---

## 五、Flow (流程线) 属性

### 5.1 基本属性

| 属性 | 含义 | 格式 | 出现次数 | 常见值 |
|------|------|------|----------|--------|
| `from` | 起始cell ID | 整数 | 6315 | - |
| `to` | 目标cell ID | 整数 | 6315 | - |
| `attr` | 流程类型标志 | 位标志整数 | 6315 | `"0"`, `"1"`, `"8192"` |
| `clr` | 线条颜色 | GDI整数 | 6315 | `"-1"`, `"16777216"` |
| `count` | 通过股票数(运行时) | 整数 | 2552 | 各种数字 |
| `begin` | 开始时间类型 | 整数枚举 | 3399 | `"0"`, `"3"`, `"4"` |
| `begint` | 开始时间参数 | HHMMSS或秒数 | 3399 | `"0"`, `"93000"`, `"92500"` |
| `end` | 结束时间类型 | 整数枚举 | 3399 | `"0"`, `"1"` |
| `endt` | 结束时间参数 | 秒数或HHMMSS | 3399 | `"2147483647"`, `"19800"` |
| `interval` | 执行间隔(秒) | 整数 | 3399 | `"60"`, `"10"`, `"20"` |
| `mid` | 线条中点偏移 | 整数 | 845 | `"500"`, `"-500"` |

### 5.2 Flow attr 位标志

**重要：`<flow>`元素在XML中的排列顺序编码了执行顺序，排在前面的flow先执行。** 改变执行顺序时，DZH设计器重排`<flow>`元素的顺序，而不修改任何flow属性值。

| 位 | 掩码 | 出现次数 | 推断含义 |
|----|------|----------|----------|
| bit0 | 0x1 | 2916 | 删除源状态中股票（通过时从源池移除） |
| bit1 | 0x2 | 7 | 未知 |
| bit12 | 0x1000 | 441 | 不删源状态中股票（保留源） |
| bit13 | 0x2000 | 604 | 先清空目的状态 |
| bit14 | 0x4000 | 95 | 未知标志 |

**常见 attr 值：**

| attr值 | 十六进制 | 含义 |
|--------|----------|----------|
| 0 | 0x0 | 默认流程（删除源，清空目的） |
| 1 | 0x1 | 删除源状态中股票 |
| 4096 | 0x1000 | 不删源状态中股票 |
| 8192 | 0x2000 | 先清空目的状态 |
| 8193 | 0x2001 | 先清空目的状态 + 删除源 |
| 20480 | 0x5000 | bit14 + 不删源状态中股票 |

### 5.3 Flow attr 奇偶规律

- 偶数attr = 出流（从源节点流出的边）
- 奇数attr = 入流（流入目标节点的边，=出流attr+1）
- 例：出流attr=8192(0x2000) → 对应入流attr=8193(0x2001)

### 5.4 Flow attr 编码转移选项（非通过/失败路径）

**重要更正**：flow.attr 的 0x1000/0x2000 位**不是**"失败/丢弃路径"标志，而是转移选项：
- bit0 (0x1)：删除源状态中股票
- bit12 (0x1000)：不删源状态中股票
- bit13 (0x2000)：先清空目的状态
- bit14 (0x4000)：输出成份股（当源是板块时）

通过/失败路径由**flow拓扑**决定，不由 attr 位决定：
- 经过 type=201 的 flow 始终是通过路径
- 直接流向 type=4 的 flow 始终是失败/丢弃路径

**三种Flow传输模式对比：**

| 模式 | Flow attr | 行为 | 典型场景 |
|------|-----------|------|---------|
| 移动模式 | 0x2000(8192) | 股票从源池移到目标池 | 超赢1号（股票离开源池） |
| 覆盖模式 | 0x1000(4096) | 每次先清空目标池再写入 | 超赢7号（目标池始终是最新的） |
| 复制模式 | 0x5000(20480) | 股票保留在源池，同时复制到目标池 | 金色两点半（多阶段保留） |

### 5.5 Flow begin 类型枚举

| begin值 | 说明书名称 | begint格式 | 出现次数 |
|---------|-----------|-----------|----------|
| 0 | 立即开始 | 0 | 1929 |
| 1 | 延迟开始 | 秒数 | 251 |
| 2 | 开市前 | 秒数 | 72 |
| 3 | 开市后 | HHMMSS | 618 |
| 4 | 收市前 | HHMMSS | 529 |
| 5 | 收市后 | 秒数 | - |
| 6 | 指定交易日时间 | - | - |
| 7 | 指定时间 | HHMMSS | - |

### 5.6 Flow end 类型枚举（✅ 已确认 2026-06-10）

| end值 | 说明书名称 | endt格式 | 出现次数 |
|-------|-----------|----------|----------|
| 0 | 一直执行 | 2147483647(无限) 或 秒数 | 3373 |
| 1 | 执行指定时长 | 秒数 | 26 |

> **✅ 对照实验确认(2026-06-10)**: 流程属性对话框"持续时间"下拉框选择"只执行一次"时，XML编码为 **`end="1"` + `endt="1"`**。
> 同时确认 `interval` 直接存储UI中的秒数（UI显示"60秒"→XML `interval="60"`，一一对应）。

**只执行一次**：end=1, endt=1（执行一次后立即结束）

### 5.7 begint/endt 时间格式

- **HHMMSS格式**: 如 `93000` = 09:30:00, `92500` = 09:25:00, `92530` = 09:25:30
- **秒数格式**: 如 `1800` = 30分钟, `3600` = 1小时
- **特殊值**: `2147483647` (INT_MAX) = 无限/永不过期

**begint格式规则**:

| begin值 | begint格式 | 示例 |
|---------|-----------|------|
| 0 | 0（无意义） | begint=0 |
| 1, 2, 5 | 秒数 | begint=1800(30分钟) |
| 3, 4, 7 | HHMMSS | begint=93000(09:30:00), begint=140000(14:00:00) |

**常见 begint 值：**

| begint值 | 时间 | 含义 |
|----------|------|------|
| 0 | - | 立即/默认 |
| 92500 | 09:25:00 | 集合竞价结束 |
| 92530 | 09:25:30 | 集合竞价后 |
| 92800 | 09:28:00 | 开盘前 |
| 93000 | 09:30:00 | 开市时间 |
| 1800 | 30分钟 | 开市后30分钟 |
| 19800 | - | 收市相关 |

**常见 endt 值：**

| endt值 | 含义 |
|--------|------|
| 2147483647 | 一直执行（INT_MAX） |
| 19800 | 5.5小时（5小时30分钟） |
| 20100 | 5小时35分钟 |
| 120 | 2分钟 |
| 180 | 3分钟 |

### 5.8 interval 执行间隔（✅ 已确认 2026-06-10）

> **✅ 对照实验确认**: 流程属性对话框"执行时间间隔"输入框值直接以**秒**为单位存储到 `interval` 属性中。
> 实验数据：UI显示"60秒" → XML `interval="60"`，无单位转换。

| 间隔值(秒) | 含义 | 出现次数 |
|------------|------|----------|
| 5 | 5秒 | 金色两点半14:00后超高频扫描 |
| 10 | 10秒 | 388 |
| 15 | 15秒 | 110 |
| 20 | 20秒 | 172 |
| 30 | 30秒 | 153 |
| 60 | 1分钟 | 2148 |
| 300 | 5分钟 | 低频监控 |
| 600 | 10分钟 | 极低频 |

### 5.9 mid 线条中点偏移

`mid` 属性控制流程线在两个节点之间的弯曲程度和方向：
- 正值：向右弯曲
- 负值：向左弯曲
- 常见值：`500`, `-500`, `50`, `-50`

### 5.10 边类型判定规则 — 源节点决定论（✅ 全量验证 2026-06-22）

> **核心结论**：DZH 中 `<flow>` 的边类型（条件边 vs 无条件边）**100% 由源节点（from）的 cell type 决定**，与目标节点无关。

#### 判定规则

| 源节点 cell type | 含义 | attr 典型值 | 有无时间属性 | 边类型 |
|-----------------|------|------------|------------|--------|
| `type=0` | 备选池 | 8192 | ✅ interval/begin/end/count | **条件转移边** |
| `type=200` | 状态池/股票池 | 8192 | ✅ interval/begin/end/count | **条件转移边** |
| `type=202` | 数据源 | 8192 | ✅ interval/begin/end/count | **条件转移边** |
| `type=201` | 条件/公式节点 | 8193 | ❌ 仅 from/to/attr/clr + size | **无条件转移边** |

#### 验证数据

扫描 dzhpool/ 目录全部 **42 个 XML 文件、602 条 `<flow>` 元素：

| 统计项 | 数值 |
|--------|------|
| 条件边 (attr=8192) 总数 | **337 条** |
| 条件边源∈{0,200,202} | **337/337 = 100%** |
| 条件边有 interval 属性 | **337/337 = 100%** |
| 无条件边 (attr=8193) 总数 | **265 条** |
| 无条件边源=201(条件节点) | **231/265 = 87%**（其余34条为装饰性节点） |
| 无条件边有 interval 属性 | **0/265 = 0%** |

#### 属性对比

| 属性 | 条件边 (源=备选池/状态池) | 无条件边 (源=条件节点) |
|------|------------------------|---------------------|
| `from` / `to` | ✅ 必有 | ✅ 必有 |
| `attr` | ✅ 8192 (偶数) | ✅ 8193 (奇数) |
| `clr` | ✅ 线条颜色 | ✅ 线条颜色 |
| **`interval`** | ✅ **执行间隔(秒)** | ❌ **不存在** |
| **`begin`/`begint`** | ✅ **开始时间** | ❌ **不存在** |
| **`end`/`endt`** | ✅ **结束时间** | ❌ **不存在** |
| **`count`** | ✅ **通过股票数(运行时)** | ❌ **不存在** |
| `mid` | 可选(线条偏移) | 可选(线条偏移) |

#### 与 TDX 格式的对应关系

| DZH | TDX | 说明 |
|-----|-----|------|
| attr=8192 (条件边) | jgtime>0 + tran=1 (备选池→条件) 或 tran=1 (状态池→条件) | 有时间控制，move模式 |
| attr=8193 (无条件边) | jgtime=0 + tran=0 (条件→状态池) | 无时间控制，copy模式 |

> **设计含义**：条件边的"条件"不是指"满足某种条件才执行"，而是指"这条边连接到条件节点"。真正的时间触发逻辑由 interval/begin/end 控制。无条件边则是条件节点的输出——筛选结果直接 propagate 到下游状态池，无需额外时间调度。

---

## 六、执行模型

### 6.1 执行的最小单位

执行的最小单位是**转移条件（type=201）**。每个转移条件：
1. 从上游节点（备选池202或状态池200）获取股票列表
2. 用公式对每只股票求值
3. 通过的股票推入下游状态池（200或废弃池4）
4. 时序控制决定何时执行第1步

```
备选池 ──[转移条件:开盘后09:30,每60秒]──→ 状态池A ──[转移条件:收盘前14:00,每20秒]──→ 状态池B
                                                  │
                                                  └──[转移条件:立即,每60秒]──→ 废弃池
```

### 6.2 执行顺序

**执行顺序控制股票池中所有转移条件的执行先后。执行顺序是全局的，不是仅限于同一上游节点的扇出场景。**

**关键概念**：
- **转移边 = 条件边**：源端为池的边，无论目标是什么（目标可以是转移条件，也可以是另一个池）。转移边参与执行顺序编号。
- **无条件边**：源端为转移条件的边。无条件边不参与编号，不显示徽标。

**操作方式**：
1. 在设计状态下，点击工具栏的"执行顺序"按钮进入编号模式
2. 所有转移边（条件边）的两端点附近会出现一个数字徽标，显示该边的当前执行顺序编号
3. 编号从 **1** 开始（1, 2, 3, ...），数字越小执行越早
4. 用鼠标点击每条转移边，第一次点击的边编号为 1，第二次为 2，直到所有转移边编号完成
5. 如果点击已有编号的边，该边的编号与当前编号交换（冲突时自动调整）
6. 点击画布空白处退出编号模式

**XML记录方式**：

执行顺序通过`<flow>`元素在XML中的**排列顺序**记录。排在前面的flow优先执行。

**持久化规则**：连接到同一转移条件的条件边（源=池）与其对应的无条件边（源=转移条件）在`<flows>`中成对排列，保持拓扑一致性。

**验证方法**：对比`超1号.xml`和`超1号ggg.xml`（在DZH设计器中点击两条边改变执行顺序后保存），发现：
- 所有cell属性完全一致（无新增/删除/修改）
- 所有flow属性值完全一致（attr/begin/begint/end/endt/interval均未变）
- **唯一的变化是`<flow>`元素在XML中的排列顺序**

具体变化示例：
```
原始顺序:                    改变执行顺序后:
[0] 67->29 (热点股票→汇合)   [0] 16->29 (长线资金→汇合)
[1] 16->29 (长线资金→汇合)   [1] 23->29 (异动扫描→汇合)
[2] 27->29 (短线资金→汇合)   ...
[3] 23->29 (异动扫描→汇合)   [6] 27->29 (短线资金→汇合)
                              [16] 67->29 (热点股票→汇合)
```

4条流入cell id=29(超赢1号池)的flow被重新排序：长线资金从位置1→0（提升优先级），热点股票从位置0→16（降低优先级）。

**关键结论**：
- **flow的XML元素顺序 = 执行优先级顺序**
- 排在前面的flow先执行，排在后面的后执行
- flow自身的属性值（attr/begin等）不编码执行顺序
- type=5 cell是纯视觉标注控件，不参与执行顺序的记录
- 改变执行顺序时，DZH设计器重排`<flows>`中`<flow>`元素的顺序

### 6.3 五种流转模式

**模式A：串行链式**（金色两点半）
```
202 → 201 → 200 → 201 → 200 → 201 → 200 → ...
初步筛选 → 基准 → 一次考量 → 二次考量 → 预备池 → 金色两点半
```

**模式B：扇出分流**（超赢1号）
```
200(风险控制) ─┬→ 201 → 200(长线资金)
              ├→ 201 → 200(短线资金)
              └→ 201 → 200(异动扫描)
```

**模式C：扇入汇合**（超赢7号）
```
200(超赢追踪) ─┐
               ├→ 201 → 200(超赢7号)
200(超赢整理) ─┘
```

**模式D：循环反馈**（超赢7号）
```
200(超赢7号) → 201(弱化) → 200(观察池) → 201(回升) → 200(超赢7号)
```

**模式E：双源并行**（超赢1号）
```
202(沪深AB股) → 201 → 200(风险控制) ─┐
202(板块指数) → 201 → 200(热点板块) ─┤→ 201 → 200(超赢1号池)
```

### 6.4 三种参考XML的时序设计对比

| 特性 | 超赢1号 | 超赢7号 | 金色两点半 |
|------|--------|--------|-----------|
| 时间控制 | 全部立即开始，间隔60秒 | 延迟0秒开始，间隔60秒，持续5.5小时 | 分时段精确控制 |
| Flow attr | 8192(追加模式) | 4096(覆盖模式) | 20480(复杂组合) |
| 典型时序 | begin=0, interval=60 | begin=1, begint=0, end=1, endt=19800 | begin=4, begint=100000(10:00) |

金色两点半的渐进时序设计：
```
全天: 初步筛选(间隔20秒)
10:00起: 基准池(间隔20秒)
11:00起: 一次考量(间隔20秒)
13:50起: 二次考量(间隔20秒→5秒加速)
14:00起: 预备池(间隔5秒超高频)
14:20起: 最终筛选(间隔5秒超高频)
14:30: 金色两点半最终输出
```

### 6.5 执行循环

```
while 运行中:
    for 每个转移条件(type=201), 按flow元素顺序:
        if 该转移条件的定时器到了:
            上游股票 = 上游节点(202或200)的股票
            通过的 = 公式筛选(上游股票)
            if delete_source: 从上游节点移除通过的
            if keep_source: 保留上游节点中的股票
            if clear_dest_first: 清空下游节点
            下游节点(200或4).add(通过的)
```

---

## 七、界面参数（来自说明书图片）

### 7.1 备选池设置窗口（说明书第13页 + 2026-06-14 扩展）

双击备选池→弹出「股票池备选股设置」对话框。

| 界面字段 | XML属性 | 类型 | 可选值/格式 | 说明 |
|---------|---------|------|-----------|------|
| **左侧树形选择器** | attrtext | 树形复选框 | 3大分类×N个子项 | 见§4.9a的6种条目类型 |
| 市场→交易所→市场 | attrtext 条目 | 树节点 | SH#上证A股, SZ#深证A股等 | Exchange#Name 格式 |
| 板块→大智慧概念 | attrtext 条目 | 树节点 | BLK-概念{Name}{ID} | DZH 概念分类 |
| 板块→证监会行业 | attrtext 条目 | 树节点 | BLK-{行业}{ID} | CSRC 行业分类 |
| 自选股→分组 | attrtext 条目 | 树节点 | BLK-自选股N | 用户自定义分组 |
| **加入个股按钮** | attrtext 条目 | 弹窗选择器 | SH/SZ + 6位代码 | 直接添加个股 |
| **加入文件按钮** | attrtext 条目 | 文件选择器 | .TXT / .PAR 文件 | 从文件导入列表 |
| **右侧已选列表** | attrtext (组合) | 列表展示 | Tab拼接所有已选项 | 显示当前全部选择 |
| **重新载入→每次启动** | reload=`-57387` | 单选按钮 | 默认选项 | 程序启动时加载 |
| **重新载入→每天指定时间** | reload=`-57387` | 单选按钮+时间 | HH:MM:SS | 每日定时刷新 |
| **重新载入→不重载** | reload=`2147483647` | 单选按钮 | INT_MAX | 永不刷新 |
| **重新载入→每隔一定时间** | reload=正整数 | 单选按钮+时间 | H:MM:SS → 秒数 | 定间隔刷新 |
| **重新载入→文件载入时** | reload=`2147483646` | 单选按钮 | INT_MAX-1 | 文件载入时刷新 |

attrtext格式：多种格式 **Tab(`\t`)分隔** 组合，详见 §4.9a。

### 7.2 状态属性设置窗口（说明书第14-16页，图2-3-2/2-3-3/2-3-4）

双击股票状态池→弹出设置窗口。分两部分：**常规设置**和**进出执行动作**。

**常规设置（图2-3-2）：**

| 界面字段 | XML属性 | 类型 | 可选值/格式 |
|---------|---------|------|-----------|
| 显示到股票池总览 | cell.attr bit30 (0x40000000) | 复选框 | 勾选=bit30置位=显示到总览（注：type=2容器用bit9/0x200标记总览容器，与type=200的bit30是不同层级） |
| 列表基于前一状态计算收益 | cell.attr bit23 (0x800000) | 复选框 | 勾选=bit23置位 |
| 记录历史轨迹 | cell.attr bit27 (0x8000000) | 复选框 | 勾选=bit27置位(记录进出历史)；取消=bit27清除 |
| 简易中间态 | cell.attr bit15 (0x8000) | 复选框 | 勾选=状态池缩小为极小节点，仅做中转传递 |
| 股票保留天数 | hold | 数字输入(秒) | 2=即时, 64800=18h, 86400=1天, 432000=5天(默认), 2592000=30天, 43200000=永久 |
| 列表显示内容 | col | 可增删排序列表 | 列ID: 2=代码, -1=名称, -2=最新价, -3=涨跌幅, 7=入池时间, 8=现价, 10=收益率, 14=入池价, 17=最大收益, 45=保留天数, -5=涨跌额 |
| 鼠标双击股票的动作 | - | 下拉选择 | 打开走势图等 |
| 状态说明文字 | wizd | 文本输入 | 自定义备注 |

**进入/离开执行动作（图2-3-3/2-3-4）：**

| 界面字段 | XML属性 | 类型 | 可选值/格式 |
|---------|---------|------|-----------|
| 进入时执行动作 | enter | 组合选择 | 见下方动作表 |
| 离开时执行动作 | exit | 组合选择 | 见下方动作表 |

**执行动作选项：**

| 动作 | 位标志 | 说明 |
|------|--------|------|
| 买入指定股数 | 0x2710 (10000) | 基础买入 |
| 买入指定金额 | 0x30000000 | 按金额买入 |
| 买入指定仓位比例 | - | 按仓位比例买入 |
| 卖出全部 | 0x30000000 | 全部卖出 |
| 卖出指定股数 | 0x2710 | 按股数卖出 |
| 卖出指定金额 | - | 按金额卖出 |
| 发出预警声 | 0x10000000 | 声音提示 |
| 弹出预警提示 | 0x20000000 | 弹窗提示 |
| 高亮闪烁显示 | 0x30000000 | 视觉闪烁 |
| 输出到滚动预警窗 | - | 滚动预警 |

**交易统计字段（说明书第16页）：**

| 统计项 | 说明 |
|--------|------|
| 运行天数 | 股票池运行天数 |
| 总收益 | 卖出收益+浮动收益 |
| 平均收益率 | (卖出价-买入价)/买入价×100% |
| 日均收益率 | 平均收益率/平均持仓天数 |
| 最大投入资金 | 完成全部交易所需总资金量 |
| 最大投入资金计算的收益率 | 总收益/最大投入资金×100% |
| 平均持仓资金 | 平均每日持仓成本 |
| 平均持仓资金计算的收益率 | 总收益/平均持仓资金×100% |

### 7.3 转移条件设置窗口（说明书第17-20页，图2-4-2~2-4-6）

双击"设"字→弹出设置窗口。

| 界面字段 | XML属性 | 类型 | 可选值/格式 |
|---------|---------|------|-----------|
| 选择公式 | inditype+indi+crc | 按钮+编译器 | 系统预设指标 或 自编公式 |
| 插入函数 | - | 按钮 | 查找和设置函数 |
| 指标条件 | cell.attr bit20 (0x100000) | 复选框 | 满足/不满足指标公式 |
| 排序条件 | cell.attr bit21 (0x200000) | 复选框+数字 | sorttype: 1/5/7/10/20/200=取前N名 |
| 分析周期 | - | 下拉选择 | 分笔/1分/5分/15分/30分/60分/日线/周线/月线 |
| 删除源状态中股票 | flow.attr bit0 (0x1) | 复选框 | 通过的股票从源池移除 |
| 不删源状态中股票 | flow.attr bit12 (0x1000) | 复选框 | 通过的股票保留在源池 |
| 先清空目的状态 | flow.attr bit13 (0x2000) | 复选框 | 转入前先清空目标池 |
| 输出成份股 | flow.attr bit14 (0x4000) | 复选框 | 当源是板块时展开为成份股 |

### 7.4 流程时序控制窗口（说明书第20-21页，图2-5-2）

双击转移条件前面的流程箭头→弹出时序控制窗口。

| 界面字段 | XML属性 | 类型 | 可选值/格式 |
|---------|---------|------|-----------|
| 开始时间 | begin | 下拉选择(8种) | 立即开始(0)/延迟(1)/开市前(2)/开市后(3)/收市前(4)/收市后(5)/指定交易日时间(6)/指定时间(7) |
| 开始时间参数 | begint | 数字输入 | 秒数(begin=0,1,2,5) 或 HHMMSS(begin=3,4,7) |
| 持续时长 | end | 下拉选择(3种) | 一直执行(0)/执行指定时长(1)/只执行一次(end=1,endt=1) |
| 持续时长参数 | endt | 数字输入 | 秒数 或 2147483647(无限) |
| 执行间隔 | interval | 数字输入+单位 | 秒/分钟/小时 |

**快速创建流程的方法（说明书第21页小提示）：**
"单击'转移条件'图标，鼠标点住备选股票控件，拖拽鼠标至下级状态池，放开鼠标，按提示设置，就可以快速完成2.4-2.5章节的操作，不需要单独设置流程箭头。"

### 7.5 修改股票池窗口（说明书第22页，图2-7-2）

运行中修改参数的三栏界面：

| 区域 | 内容 |
|------|------|
| 左侧 | 选股流程结构图（缩略拓扑图） |
| 中间 | 每个流程的"指标条件设置"与"状态属性设置" |
| 右侧 | 每个流程的时序控制条件设置 |

---

## 八、前端画布渲染

### 8.1 画布上的控件

| 画布控件 | XML type | 渲染方式 | 工具栏图标 |
|---------|----------|---------|-----------|
| 备选池 | 202 | 蓝色圆柱图标+名称 | 圆柱体 |
| 转移条件 | 201 | 小矩形+"设"字+公式标签 | "设"字按钮 |
| 股票状态池 | 200 | 矩形+名称+股票列表 | 矩形池 |
| 废弃池 | 4 | 叉号+名称 | 叉号容器 |
| 执行顺序 | 5 | 编号标记+方向箭头 | 编号箭头 |
| 容器 | 2 | 半透明背景矩形 | 矩形框 |
| 状态列 | 3 | 纵向分隔条 | 竖条 |
| 文字标签 | 1 | 纯文字 | Aa |
| 流程箭头 | 6 | 装饰性箭头线 | 箭头线 |

### 8.2 工具栏完整按钮列表（说明书第11-12页）

| 图标 | 功能 | XML映射 |
|------|------|---------|
| 新建 | 新建空白股票池 | - |
| 打开 | 打开已有股票池 | - |
| 保存 | 保存当前股票池 | - |
| 密码 | 设置编辑密码 | pool.ency |
| 上载 | 参加设计大赛 | - |
| 撤销 | 撤销前一步 | - |
| 流程设计向导 | 引导式设计 | - |
| 流程设置 | 流程结构总览 | - |
| 运行 | 开始运行 | - |
| 选择工具 | 选中控件修改 | - |
| 备选池容器 | 新建type=202 | cell type=202 |
| 股票状态池 | 新建type=200 | cell type=200 |
| 转移条件 | 新建type=201（需拖拽连接） | cell type=201 |
| 丢弃容器 | 新建type=4 | cell type=4 |
| 流程箭头 | 连接控件 | flow |
| 执行顺序 | 设置转移条件执行先后 | cell type=5 |
| 文字与图形 | 文字注释和美化 | cell type=1 |
| 属性 | 打开属性窗口 | - |
| 颜色 | 填充颜色 | cell.clr |
| 空心 | 空心显示 | cell attr |
| 文字对齐 | 左/中/右对齐 | cell attr |
| 字体 | 字体类型和大小 | cell attr |
| 前移后移 | 叠放次序 | - |
| 对齐方式 | 控件对齐 | - |
| 独立窗口 | 多显示器 | - |
| 关闭 | 关闭股票池 | - |

### 8.3 边的渲染

边 = 从源节点到目标节点的有向线段。

说明书原文："双击'转移条件'前面的'流程箭头'，我们可以设置'流程属性'"

**两条flow的区别：**
- 转移条件**前面**的流程箭头：双击→设置"流程属性"（时序控制+转移选项）
- 转移条件**后面**的流程箭头：双击→设置线条宽度

点击边 → 属性面板显示：
- 调度参数（begin/begint/end/endt/interval）
- 转移选项（删除源/保留源/先清目标/输出成份股）

### 8.4 布局

DZH原始布局用 `pos="x1,y1,x2,y2"` 矩形坐标。导入时直接使用原始坐标定位。

---

## 九、数据流转与程序分层

### 9.1 数据流转

```
DZH XML ──[解析]──→ 内部格式(meta JSON) ──[渲染]──→ 画布
    ↑                      │                           │
    │                      ↓                           ↓
    └──[导出]──← 内部格式(meta JSON) ←──[编辑]── 属性面板
```

**解析（XML → meta）**:
- 读取XML中的cell和flow
- type=202→备选池，type=201→转移条件，type=200→状态池，type=4→废弃池
- 装饰性节点（type=1,2,3,5,6）标记为auxiliary
- **flow的XML元素顺序必须保留**——它编码了执行顺序
- flow的attr位标志解析为delete_source/keep_source/clear_dest_first/output_constituent
- 保持202→201→200→201→200的原始拓扑，不折叠

**渲染（meta → 画布）**:
- 每个节点按type选择渲染模板
- 颜色用COLORREF格式转换（r=低字节, g=中字节, b=高字节）
- 系统颜色（≥0x01000000）按节点类型回退到默认色
- 边用SVG path绘制

**导出（meta → XML）**:
- 所有节点原样输出
- 颜色值原样输出（整数）
- flow的attr位标志从delete_source/keep_source/clear_dest_first/output_constituent合成
- **flow元素顺序必须按执行顺序排列**

### 9.2 程序分层

```
┌─────────────────────────────┐
│  前端 (index.html)          │  画布渲染 + 属性编辑 + 用户交互
├─────────────────────────────┤
│  API层 (dzh_api.py)         │  导入/导出/转换的HTTP接口
├─────────────────────────────┤
│  转换层                      │
│  ├ dzh_xml_raw.py           │  XML → 原始字典（逐属性解析）
│  ├ dzh_converter.py         │  原始字典 → meta格式
│  └ dzh_exporter.py          │  meta格式 → XML字节
├─────────────────────────────┤
│  引擎层 (engine.py)         │  读表→解释执行→输出（不含领域知识）
├─────────────────────────────┤
│  配置层 (config/*.json)     │  模块定义/管道/分发/引擎
└─────────────────────────────┘
```

每层职责单一，不跨层调用。引擎层通过JSON表驱动，不含任何if-type分支。

---

## 十、stk 子元素 (股票数据)

stk 元素嵌入在 type=200 的 cell 中，记录运行时的股票数据：

| 属性 | 含义 | 格式 | 出现次数 |
|------|------|------|----------|
| `label` | 股票代码 | `"SH600000"` | 280525 |
| `t` | 入池时间戳 | Unix时间戳 | 280525 |
| `p` | 入池价格 | 浮点数字符串 | 280525 |
| `tid` | 交易ID | 整数字符串 | 17253 |

**label 代码格式：**
- `SH` + 6位数字：上海证券交易所
- `SZ` + 6位数字：深圳证券交易所

---

## 十一、trades/opentrades 元素

| 元素 | 含义 | 出现次数 |
|------|------|----------|
| `<trades>` | 已完成交易记录 | 150/206 |
| `<opentrades>` | 未平仓交易记录 | 150/206 |

两者都包含 `<trade>` 子元素，记录交易统计信息。

---

## 十二、颜色值完整参考体系（✅ 已完善 — 2026-06-18）

### 12.1 颜色格式分类

DZH股票池XML中的颜色属性（`clr`、`backcolor`、`clrtext`）统一使用**Windows COLORREF**格式存储，根据数值范围分为三类：

#### 判断逻辑与优先级

```
解析流程:
  1. 值 == -1          → 类型3: 特殊值（透明/继承/默认）
  2. 值 >= 0x01000000  → 类型1: 系统调色板索引 (index = val - 0x01000000)
  3. 0 <= val <= 0xFFFFFF → 类型2: BGR直接色 (0x00BBGGRR)
  4. 其他负数(非-1)    → 按无符号处理: val + 0x100000000
```

| 类型 | 数值范围 | 格式 | 说明 | 出现占比 |
|------|----------|------|------|----------|
| **类型1：调色板索引** | `0x01000000 ~ 0x010000FF` | `0x01` + `idx`(1字节) | 高位字节=0x01标识调色板，低8位为索引 | ~57.9% |
| **类型2：BGR直接色** | `0x00000000 ~ 0x00FFFFFF` | `0x00BBGGRR` | Windows原生COLORREF，低字节R、中字节G、高字节B | ~40.8% |
| **类型3：特殊值** | `-1` 等 | 特殊编码 | 语义依赖上下文 | ~1.3% |

> **关键注意**：BGR直接色的字节序是 **Blue-Green-Red**（而非常规RGB）。例如 `0x00FF8000` 解码为 B=255, G=128, R=0，即 CSS 表示为 `rgb(0,128,255)`（蓝色），**不是**橙色。

---

### 12.2 完整调色板映射表（20色）

DZH内置一套20色调色板，通过在COLORREF高位设置 `0x01` 标志来引用。以下映射经三重验证确认：

- ✅ **来源A**：`doc/tempdzh/颜色/颜色索引.xml` — 20个cell的clr值从16777216递增到16777235
- ✅ **来源B**：`doc/tempdzh/颜色/默认颜色.png` — DZH原生"默认颜色"对话框截图，色带顺序一致
- ✅ **来源C**：`doc/tempdzh/颜色/颜色索引.png` — 运行时渲染效果截图，20个状态池使用不同边框色
- ✅ **来源D**：`web/js/canvas.js` 第213~234行 — 前端渲染引擎的palette对象

| Index | 十进制 | Hex | CSS Hex | RGB 元组 | 中文名 | 英文名 | 视觉特征 | DZH典型用途 |
|:-----:|------:|:----:|:-------:|:--------:|--------|--------|----------|-------------|
| 0 | 16,777,216 | `0x01000000` | `#000000` | (0,0,0) | 黑色 | Black | 纯黑，无亮度 | 状态池默认背景、深色主题基底 |
| 1 | 16,777,217 | `0x01000001` | `#800000` | (128,0,0) | 暗红 / 栗色 | Maroon | 深暗红色，低饱和度 | 状态池(2)等偶数位备选色 |
| 2 | 16,777,218 | `0x01000002` | `#008000` | (0,128,0) | 深绿 | Dark Green | 暗绿色，中等明度 | 状态池(3)等序列色 |
| 3 | 16,777,219 | `0x01000003` | `#808000` | (128,128,0) | 橄榄色 | Olive | 黄绿混合，土黄色 | 状态池(4)，出现302次 |
| 4 | 16,777,220 | `0x01000004` | `#000080` | (0,0,128) | 深蓝 / 藏青 | Navy Blue | 极深蓝色，接近黑 | 状态池(5)，出现227次 |
| 5 | 16,777,221 | `0x01000005` | `#800080` | (128,0,128) | 紫色 | Purple | 品红与蓝的中间态 | 状态池(6)，出现132次 |
| 6 | 16,777,222 | `0x01000006` | `#008080` | (0,128,128) | 青色 / 孔雀绿 | Teal | 蓝绿混合，冷色调 | 状态池(7)，出现183次 |
| 7 | 16,777,223 | `0x01000007` | `#C0C0C0` | (192,192,192) | 银色 / 浅灰 | Silver | 中亮灰色，中性 | 状态池(8)，出现92次 |
| 8 | 16,777,224 | `0x01000008` | `#C0DCC0` | (192,220,192) | 淡绿灰 | Pale Green Gray | 微带绿色的浅灰 | 状态池(9)，较少使用(10次) |
| 9 | 16,777,225 | `0x01000009` | `#A6CAF0` | (166,202,240) | 淡蓝 / 天青 | Light Blue | 柔和的天蓝色 | 状态池(10)，出现183次 |
| 10 | 16,777,226 | `0x0100000A` | `#F9F7F0` | (249,247,240) | 米白 / 暖白 | Off-white | 接近白色，微暖底色 | 标题背景色，出现358次 |
| 11 | 16,777,227 | `0x0100000B` | `#A0A0A4` | (160,160,164) | 中灰 | Medium Gray | 均衡中性灰 | 备选池默认色，出现59次 |
| 12 | 16,777,228 | `0x0100000C` | `#808080` | (128,128,128) | 灰色 | Gray | 标准50%灰 | 较少使用(83次) |
| 13 | 16,777,229 | `0x0100000D` | `#FF0000` | (255,0,0) | 纯红 | Red | 标准正红色，高饱和 | 标题文字/强调色，出现1035次（第2高频） |
| 14 | 16,777,230 | `0x0100000E` | `#00FF00` | (0,255,0) | 纯绿 / 荧光绿 | Lime | 高亮绿色，醒目 | 状态池(15)，出现77次 |
| 15 | 16,777,231 | `0x0100000F` | `#FFFF00` | (255,255,0) | 纯黄 | Yellow | 明亮黄色，高可见性 | 流程线/通过标记，出现907次（第3高频） |
| 16 | 16,777,232 | `0x01000010` | `#0000FF` | (0,0,255) | 纯蓝 | Blue | 标准蓝色，全饱和 | 条件节点/信息色，出现95次 |
| 17 | 16,777,233 | `0x01000011` | `#FF00FF` | (255,0,255) | 品红 / 洋红 | Magenta | 红蓝叠加，极醒目 | 状态池(18)，出现176次 |
| 18 | 16,777,234 | `0x01000012` | `#00FFFF` | (0,255,255) | 青 / 青色 | Cyan | 绿蓝混合，最亮冷色 | 说明文字/提示，出现115次 |
| 19 | 16,777,235 | `0x01000013` | `#FFFFFF` | (255,255,255) | 纯白 | White | 全白，最大亮度 | 标题文字/高对比文字，出现53次 |

> **调色板设计规律**：Index 0~7 对应经典 VGA 16色前8色（暗色系）；Index 8~11 为DZH扩展的中性灰阶；Index 12 为标准灰；Index 13~19 对应 VGA 16色后7色（亮色系）。整体呈现「暗→中灰→亮」的三段式排列。

---

### 12.3 常用BGR直接色表（Top 20）

以下数据基于 `dzhpool/` 目录下 **206个XML文件、共14,409条clr记录** 的统计结果。排除类型1（调色板索引）和类型3（特殊值=-1）后，按出现频率排序：

| 排名 | 十进制 | Hex | BGR分解 | CSS RGB | 出现次数 | 占比 | 典型使用场景 |
|:---:|------:|:----:|:--------:|:--------:|--------:|:---:|:------------|
| 1 | 4,227,327 | `0x004080FF` | (64,128,255) | `rgb(255,128,64)` 🟠 | 238 | 1.65% | **通过/成功流程线**（橙色调，type=6连接箭头首选） |
| 2 | 33,023 | `0x000080FF` | (0,128,255) | `rgb(255,128,0)` 🟧 | 97 | 0.67% | **条件节点填充色**（纯橙色，type=201输出节点） |
| 3 | 109,815 | `0x0001ACF7` | (1,172,247) | `rgb(247,172,1)` 🟡 | 48 | 0.33% | **自定义流程线色**（金黄色变体） |
| 4 | 4,432,866 | `0x0043A3E2` | (67,163,226) | `rgb(226,163,67)` 🟨 | 46 | 0.32% | **浅金/沙金色节点**（柔和暖色调） |
| 5 | 16,744,448 | `0x00FF8000` | (255,128,0) | `rgb(0,128,255)` 🔵 | 39 | 0.27% | **蓝色条件节点**（type=201/202默认蓝底） |
| 6 | 221 | `0x000000DD` | (0,0,221) | `rgb(221,0,0)` 🔴 | 39 | 0.27% | **深红连接线/箭头**（type=6线条色） |
| 7 | 8,421,631 | `0x008080FF` | (128,128,255) | `rgb(255,128,128)` 🔴🌕 | 34 | 0.24% | **淡红/鲑鱼色**（容器或标签背景） |
| 8 | 30,186 | `0x000075EA` | (0,117,234) | `rgb(234,117,0)` 🟠 | 28 | 0.19% | **深橙色流程线**（比#1更深的橙色） |
| 9 | 608,189 | `0x000947BD` | (9,71,189) | `rgb(189,71,9)` 🟤 | 27 | 0.19% | **棕褐色**（深色系节点背景） |
| 10 | 4,671,303 | `0x00474747` | (71,71,71) | `rgb(47,71,47)` 🌑 | 24 | 0.17% | **深灰绿容器背景**（type=2容器底色） |
| 11 | 6,579,300 | `0x00646464` | (100,100,100) | `rgb(100,100,100)` ⚫ | 24 | 0.17% | **中性灰背景**（通用容器/面板） |
| 12 | 1,973,790 | `0x001E1E1E` | (30,30,30) | `rgb(30,30,30)` ⬛ | 21 | 0.15% | **近黑深灰**（深色主题容器底色） |
| 13 | 33,535 | `0x000082FF` | (0,130,255) | `rgb(255,130,0)` 🟠 | 20 | 0.14% | **亮橙色**（高亮流程线） |
| 14 | 4,473,994 | `0x0044448A` | (68,68,138) | `rgb(138,68,68)` 🟤🌙 | 20 | 0.14% | **暗红褐**（深色文字或边框） |
| 15 | 16,711,808 | `0x00FF0080` | (255,0,128) | `rgb(128,0,255)` 💜 | 14 | 0.10% | **紫红色**（特殊标记/强调） |
| 16 | 255 | `0x000000FF` | (0,0,255) | `rgb(255,0,0)` 🔴 | 13 | 0.09% | **纯红**（警告/错误标记） |
| 17 | 2,565,927 | `0x00272727` | (39,39,39) | `rgb(39,39,39)` ⬛ | 12 | 0.08% | **深灰背景**（近黑色容器） |
| 18 | 3,612 | `0x00000E1C` | (0,14,28) | `rgb(28,14,0)` 🟫 | 12 | 0.08% | **极深棕**（几乎黑色的深褐） |
| 19 | 8,388,863 | `0x008000FF` | (128,0,255) | `rgb(255,0,128)` 💗 | 11 | 0.08% | **粉红/玫红**（装饰性元素） |
| 20 | 227 | `0x000000E3` | (0,0,227) | `rgb(227,0,0)` 🔴🌙 | 11 | 0.08% | **深红（略亮于#16）**（箭头/线条） |

> **统计摘要**：
> - 调色板索引色（类型1）总计 **8,291次**（占57.9%），其中 `-1` 单独占 **7,061次**（49.0%）
> - BGR直接色（类型2）总计 **5,881次**（占40.8%）
> - Top 1 直接色 `0x004080FF`（橙）出现238次，是最常用的直接色，主要用作**流程连接线**的颜色
> - 整体色调倾向：**橙色系 > 蓝色系 > 灰色系 > 红色系**

---

### 12.4 特殊值说明

| 值 | 十六进制 | 含义 | 使用上下文 | 行为说明 |
|:---:|:--------:|------|:-----------|:---------|
| **-1** | `0xFFFFFFFF` (有符号) / `0xFFFFFFFF` | **透明 / 继承默认** | `clr`、`backcolor`、`clrtext` | 最常见特殊值（7,061次≈49%）。在`clr`中表示"使用父级/系统默认色"；在前端渲染时映射为`transparent`。**注意**：在不同属性中语义不同——作为`backcolor`时表示"不设背景（透出底层）"，作为`clrtext`时表示"使用默认文字色（通常为白）" |
| **16,777,215** | `0x0100000FF` | 白色（调色板溢出） | `clrtext`默认值 | 在cell_type_registry中多处作为`clrtext`的默认值。虽然形式上等于`0x0100000FF`（index=255，超出标准20色调色板范围），但实际语义等同于白色`#FFFFFF` |
| **0** | `0x00000000` | **黑色（BGR直接色）** | `attr`相关、部分初始化场景 | BGR(0,0,0)=纯黑。在某些Cell类型的`clr`初始值中出现，表示"尚未设置颜色"或"显式黑色" |
| **127** | `0x0000007F` | 半透明黑/默认占位 | type=1/2的`clr`默认值 | 见xml_mapping.json中type=1/2的default:"127"。可能是旧版遗留值，实际渲染行为需视具体版本而定 |
| **16,772,26** (即16777226) | `0x0100000A` | 米白色（调色板[10]） | 标题栏背景 | 虽然属于标准调色板，但因其在UI布局中的特殊角色（标题背景），常被单独提及。对应CSS `#F9F7F0` |
| **16,772,29** (即16777229) | `0x0100000D` | 纯红色（调色板[13]） | 标题文字色 | 同上，因高频用于标题文字（1035次）而具有特殊地位 |
| **16,772,31** (即16777231) | `0x0100000F` | 纯黄色（调色板[15]） | 流程线/通过标记 | 因广泛用于表示"通过/成功"状态（907次）而成为语义化特殊色 |

---

### 12.5 各Cell类型颜色使用规范

基于206个XML文件的统计分析，各Cell类型的典型颜色模式如下：

| Cell类型 | 类型名称 | 推荐clr值 | 推荐clrtext值 | 典型配色方案 | 备注 |
|:--------:|:---------|:----------:|:-------------:|:-------------|:------|
| **200** | 状态池 | 调色板 [0]~[19]（循环分配） | 16777215（白） | 彩色边框 + 黑底 + 白字 | 20个状态池依次使用调色板0~19，形成彩虹区分效果 |
| **201** | 条件输出 | `0x00FF8000`（蓝）或 `-1` | 16777215（白） | 蓝底 + 白字 或 继承默认 | 条件判断节点的标准外观；也可用调色板[16]纯蓝替代 |
| **202** | 备选池 | 调色板 [11]（中灰）或 `-1` | 16777215（白） | 灰底 + 白字 | 备选池通常用低调的灰色以区别于活跃状态池 |
| **1** | 文字标签 | `-1`（透明）或调色板[13]（红） | 调色板[13]（红）或 [19]（白） | 透明底 + 有色文字 | 文字标签通常无边框，仅显示文字本身 |
| **2** | 容器 | 深灰BGR直接色（如0x00272727） | 不适用 | 深灰底 + 内嵌子元素 | 容器提供分组框架，颜色应低调不抢眼 |
| **3** | 状态列 | 调色板 [7]（银灰）或 BGR浅灰 | 16777215（白） | 浅灰底 + 表格内容 | 状态列展示表格数据，需要浅色背景以保证可读性 |
| **4** | 图形/形状 | BGR直接色（任意） | 不适用 | 纯色填充 | 用于装饰性图形，颜色自由度高 |
| **6** | 连接箭头 | `0x004080FF`（橙）或 `0xDD`（深红） | 不适用 | 彩色线条 | 橙色表示正常流向，红色表示强制/异常流向 |

#### type=200 状态池颜色规则详解（✅ 已确认 — 2026-06-18 颜色索引.png 截图验证）

基于代码实现与运行时截图的交叉验证，type=200 状态池的颜色渲染遵循以下精确规则：

| 渲染元素 | 颜色值 | 来源 | 说明 |
|:--------:|:------:|:----:|:-----|
| **标题栏背景色** | `cell.clr`（动态） | 调色板索引或BGR直接色 | 与边框使用同一颜色值 |
| **边框颜色** | `cell.clr`（动态） | 调色板索引或BGR直接色 | 2px solid 样式 |
| **主体背景色** | `#000000`（固定纯黑） | 硬编码 | 所有状态池统一黑色背景 |
| **标题栏文字** | `#FFFFFF`（固定白色） | 硬编码 | 高对比度保证可读性 |

**核心设计原则**：

* **标题栏背景色 = 边框色 = cell.clr**（三者使用同一颜色值）
* **主体背景**: 固定纯黑 `#000000`
* **标题栏文字**: 固定白色 `#FFFFFF`
* **边框宽度**: 2px solid
* **设计意图**: 每个状态池通过独立的clr值实现视觉区分，标题栏与边框同色增强整体性

> **证据来源**: [`meta_core/doc/tempdzh/颜色/颜色索引.png`](meta_core/doc/tempdzh/颜色/颜色索引.png) — 该截图清晰展示20个状态池各自使用不同颜色的标题栏和边框，且标题栏颜色与边框完全一致。
>
> **代码实现参考**: `meta_core/web/js/canvas.js` 中状态池(type=200)的渲染逻辑已将标题栏背景色从硬编码 `#464646` 改为使用 `clr` 变量（与边框同色），确保文档与代码一致。

#### 配色最佳实践

1. **状态池色彩循环**：新建股票池时，DZH自动将20个状态池的clr从 `0x01000000`（黑）递增到 `0x01000013`（白），确保每个状态池视觉可区分
2. **对比度要求**：状态池采用"深色边框 + 黑色背景 + 白色文字"的高对比方案，保证在任何显示器上都清晰可读
3. **流程线语义**：橙色（`0x004080FF`）= 正常流转；黄色（调色板[15]）= 通过/成功；红色（`0xDD`附近）= 强制/警告
4. **避免的颜色组合**：不要将调色板[0]（黑）同时用作背景和文字色；避免[8]（淡绿灰）与[7]（银灰）相邻使用（差异太小）

---

### 12.6 前后端颜色同步指南

为确保Python后端解析和JavaScript前端渲染返回一致的颜色结果，需遵循以下要点：

#### 6.1 解析一致性规则

| 步骤 | Python后端 | JavaScript前端 | 一致性要点 |
|:-----|:-----------|:---------------|:-----------|
| **输入接收** | XML字符串 → `int(clr_str)` | API响应 → `parseInt(clrStr)` | 两者都必须正确处理字符串到整数的转换 |
| **特殊值-1** | `if val == -1: return "transparent"` | `if (val === '-1') return fallback \|\| 'transparent'` | -1必须统一映射为透明 |
| **负数处理** | Python: 自动按有符号处理 | JS: `if (val < 0) val += 0x100000000` | JS需手动补偿无符号转换 |
| **调色板判断** | `(val & 0xFF000000) == 0x01000000` | `(val & 0xFF000000) === 0x01000000` | 位运算逻辑完全一致 |
| **BGR→RGB转换** | `r = val & 0xFF; g = (val>>8)&0xFF; b = (val>>16)&0xFF` | `b = (val>>16)&0xFF; g = (val>>8)&0xFF; r = val&0xFF` | 字节序提取顺序相同 |
| **CSS输出** | `f'rgb({r},{g},{b})'` | `` `rgb(${r},${g},${b})` `` | 格式完全一致 |

#### 6.2 调色板同步

前后端必须维护**同一份**20色调色板映射表。当前权威定义位于：

- **前端权威源**：`web/js/canvas.js` 第213~234行（`dzhColorToCss`函数内的`palette`对象）
- **后端应对应**： converters 或 api 层中应包含等价的palette字典

当需要扩展调色板（如支持用户自定义色）时，两端必须同步更新，且新增索引不得与现有0~19冲突。

#### 6.3 常见不一致场景

| 场景 | 症状 | 解决方案 |
|:-----|:------|:---------|
| 前端显示橙色但预期蓝色 | BGR/RGB混淆 | 确认解码时是否做了B→R的字节交换 |
| -1显示为黑色而非透明 | 特殊值未拦截 | 在BGR解码之前先检查 `val == -1` |
| 调色板[10]显示异常 | 索引越界或未定义 | palette字典必须包含全部0x00~0x13的键 |
| 颜色偏暗/偏亮 | gamma或色彩空间不一致 | DZH使用sRGB，无需额外gamma校正 |

---

### 12.7 参考文件来源

| 序号 | 文件路径 | 类型 | 作用 | 验证结论 |
|:---:|:---------|:-----|:------|:---------|
| R1 | `meta_core/doc/tempdzh/颜色/默认颜色.png` | PNG截图 | DZH原生"默认颜色"对话框完整截图 | 显示20个标准色块纵向排列 + 底部"其他颜色"按钮，色序与调色板Index 0~19一一对应 ✅ |
| R2 | `meta_core/doc/tempdzh/颜色/颜色索引.png` | PNG截图 | DZH运行时界面截图（含20个状态池） | 20个状态池(cell id=1~20)各自使用不同颜色的边框，直观展示调色板的实际渲染效果 ✅ |
| R3 | `meta_core/doc/tempdzh/颜色/颜色索引.xml` | XML文件 | 专门构造的测试用例（20个状态池） | 确认clr值严格从16777216(index=0)递增到16777235(index=19)，每个cell的index = id - 1 ✅ |
| R4 | `meta_core/web/js/canvas.js` (L203~242) | JS源码 | 前端颜色渲染引擎实现 | 包含完整的`dzhColorToCss()`函数，内嵌20色调色板字典和BGR解码逻辑，为本文档的权威技术参考 ✅ |
| R5 | `meta_core/dzhpool/*.xml` (206个) | XML文件集 | 生产环境股票池样本 | 提供了14,409条真实clr值的统计数据，支撑12.3节的频率分析 ✅ |

> **验证方法**：R1（对话框静态色带）、R2（运行时动态渲染）、R3（XML结构化数据）三者交叉验证，确认20色调色板映射100%一致。R4提供了程序化的实现参考，R5提供了大规模统计验证。

---

## 十三、收益分析体系（说明书第三章）

| 分析类型 | 说明 |
|----------|------|
| 日内收益 | 池中所有股票当日平均分时走势 |
| 市场冲击 | 以20日均额10%买入，对股价的影响 |
| 历史收益 | 对最近M天入池股票，均买10000元，持N天卖出，生成收益K线 |
| 历史收益分布 | 入池后30天内最大收益的时间与收益分布 |
| 定位分析 | 定位点前后30天数据统计，含均值/标准差/最大/最小值 |

---

## 十四、批量验证统计

基于 `Newpool/` 目录下206个XML文件的验证结果：

| 指标 | 结果 |
|------|------|
| 总文件数 | 206 |
| 解析成功 | 204 (99.0%) |
| 解析失败 | 2 (DDE决策.xml, DDE实时策略.xml - 根元素为`<list>`非`<pool>`) |
| 导出成功 | 204 (100%) |
| 往返验证通过 | 206 (100%) |
| 往返验证失败 | 0 |

### Cell 类型分布

| 类型 | 数量 | 占比 |
|------|------|------|
| type=201 (条件输出) | 2919 | 35.7% |
| type=200 (状态池) | 2876 | 35.2% |
| type=1 (文字标签) | 1006 | 12.3% |
| type=2 (容器) | 417 | 5.1% |
| type=3 (状态列) | 354 | 4.3% |
| type=202 (备选池) | 308 | 3.8% |
| type=6 (连接箭头) | 114 | 1.4% |
| type=4 (丢弃池) | 71 | 0.9% |
| type=5 (执行顺序) | 26 | 0.3% |
| type=203 (未知) | 3 | 0.04% |

---

## 十五、关键推断总结

### 15.1 attr 是核心属性编码

attr 不是简单的枚举值，而是**位标志组合**。不同 cell type 的同一位可能有不同含义。解析时必须先判断 type，再解读 attr 的各个位。

### 15.2 type=200 是多功能类型

type=200 同时承担"转移条件"和"状态池"两种角色：
- 有 `hold`/`col` 属性时：作为状态池使用
- 无 `hold`/`col` 时：作为条件节点使用
- `attr` 的位标志控制状态池的各种功能开关

### 15.3 type=201 是转移条件

type=201 是独立的筛选器控件：
- `inditype`/`crc`/`indi` 存储实际的公式内容
- `sorttype` 控制排序筛选数量
- `attr` 的位标志区分指标条件/排序条件

### 15.4 Flow attr 编码转移选项

flow.attr 的 0x1000/0x2000 位是转移选项，不是通过/失败路径标志。通过/失败路径由flow拓扑决定。

### 15.5 时间控制体系

每个 Flow 都有独立的时序控制，实现了"边触发"执行模型，每条边可独立定时。

### 15.6 执行顺序由flow元素顺序编码

flow的XML元素顺序 = 执行优先级顺序。type=5 cell仅是视觉标注。

---

## 十六、字段/参数深度探索结果

以下字段/参数经过Python脚本对206个XML文件的批量分析、说明书原文交叉验证、以及属性关联性统计，已从"完全未知"推进到"有据推断"或"已确认"。

### 16.1 Pool 根属性

| 属性 | 结论 | 依据 |
|------|------|------|
| `mode` | **始终为"1"**，206个文件无一例外，推测为股票池运行模式标识 | 批量统计 |
| `system` | **系统股票池标记**。唯一实例 AI资金策略模型.xml 同时有 `ency`(加密)和`warning`(起始页URL)，推测system="0"表示系统级股票池 | 实际数据：system="0" + ency + warning 三属性共存 |
| `ver` | **版本号**。3个ver="1.1"文件均为模板文件：`股票助手卖出例子池.xml`、`超赢1号(模板).xml`、`超赢2号(模板).xml`。ver=1.1可能是模板格式版本 | 3个文件名均含"模板"或"例子" |

### 16.2 type=203 未知类型 → 已确认：可连接的池节点

**3个实例的详细数据**：

| 文件 | id | pos | clr | 有flow连接 | flow来源 |
|------|-----|-----|-----|-----------|---------|
| 智客乐淘.xml | 1 | 974,566,990,590 | -1 | 否 | 无 |
| 热点股票池简化版.xml | 300 | 431,527,451,552 | 16777230 | **是** | from=299(type=?) attr=8193 |
| 热点股票池简化版.xml | 339 | 489,447,509,473 | -1 | **是** | from=151(type=?) attr=1 |

**关键发现**：
- 智客乐淘.xml中的type=203无flow连接，可能是设计器遗留
- **热点股票池简化版.xml中的2个type=203有flow连接**，flow attr=8193(先清空目的+删除源)和attr=1(删除源)，与type=200的flow模式完全一致
- 推断：type=203是**特殊池类型**，可能是DZH后期版本新增的池控件（如"横向统计池"或"条件池"），可参与正常的flow拓扑

### 16.3 type=200 attr bit15/bit23/bit27/bit30/histana/stocknum → 已确认：六个独立功能

**6次对照实验完整对比**（均以超1号.xml为基准，操作对象为id=27短线资金）：

| 操作 | bit23(0x800000) | bit27(0x8000000) | bit30(0x40000000) | histana | stocknum |
|------|------------------|------------------|---------------------|---------|----------|
| 原始超1号 | ❌ | ✅置位 | ❌ | 无 | 无 |
| 去掉记录历史轨迹 | ❌ | ❌清除 | ❌ | 无 | 无 |
| 选中收益分析最多1000 | ❌ | ❌清除 | ❌ | "1000" | 无 |
| 选中记录历史轨迹+收益分析+状态池最多1000 | ❌ | ✅置位 | ❌ | "1000" | "1000" |
| +列表基于前一状态计算收益 | ✅置位 | ✅置位 | ❌ | "1000" | "1000" |
| +显示到股票池总览 | ✅置位 | ✅置位 | ✅置位 | "1000" | "1000" |

**结论**：

| 位标志/属性 | 功能 | 操作效果 |
|------------|------|---------|
| **bit15 (0x8000)** | **简易中间态** | 启用后状态池变为极小节点(pos缩至6×6)，仅做中转传递不展示股票列表 |
| **bit23 (0x800000)** | **列表基于前一状态计算收益** | 选中时bit23置位 |
| **bit27 (0x8000000)** | **记录历史轨迹** | 勾选=bit27置位(记录股票进出历史)；取消=bit27清除 |
| **bit30 (0x40000000)** | **显示到股票池总览** | 选中时bit30置位 |
| **histana** | **收益分析数据点数** | 收益分析的最大记录数，如1000。与bit27独立——可同时存在 |
| **stocknum** | **状态池最大股票数量** | 限制状态池中最多保留N只股票 |

**bit27与histana的关系**：
- 两者是**独立功能**，可以同时存在（bit27置位+histana="1000"）
- bit27与histana完全独立，选中收益分析不会自动取消记录历史轨迹
- "去掉记录历史轨迹" = 仅清除bit27
- "选中收益分析最多1000" = 添加histana（bit27不变；之前实验中bit27被清除是因为同时手动取消了记录历史轨迹，并非收益分析自动取消）
- "选中记录历史轨迹+收益分析最多1000" = bit27保持置位 + 添加histana

### 16.4 type=200 attr 高位位标志 → 部分已确认，部分待定

**核心发现：高位bit(bit23-31)与histana/enter/exit无强关联**

| 高位bit组合 | count | histana率 | enter率 | exit率 | avg_hold(秒) |
|------------|-------|----------|---------|--------|-------------|
| none(无高位bit) | 1843 | 7.2% | 0.2% | 0.2% | 1850307 |
| bit29 | 269 | 7.1% | 0% | 0% | 809523 |
| bit29+bit30 | 108 | 36.1% | 0% | 0% | 930372 |
| bit30 | 92 | 7.6% | 0% | 0% | 247461 |
| bit24+bit29+bit30 | 79 | 27.8% | 0% | 0% | 788354 |
| bit23 | 42 | 33.3% | 0% | 0% | 2377371 |
| bit31 | 14 | 0% | 0% | 0% | 3189857 |

**推断结论**：
- **高位bit与enter/exit几乎无关**：有高位bit的cell极少有enter/exit属性
- **高位bit与histana弱相关**：某些组合（如bit29+bit30）的histana率偏高(36%)，但不一致
- **高位bit与hold值无规律**：avg_hold从11万到1200万不等
- **修正**：高位bit并非全部是版本标志。对照实验已确认：
  - bit23 = 列表基于前一状态计算收益（已确认）
  - bit27 = 记录历史轨迹（已确认）
  - bit30 = 显示到股票池总览（已确认）
  - bit24/bit28/bit29/bit31 仍待进一步实验确认
  - 之前"版本/兼容性标志"的推断是错误的，是基于统计相关性而非对照实验得出的

**bit7(0x80)的独立推断**：
- bit7在type=200中出现29次（正值attr），与type=202备选池的bit7(0x80=备选池基本标记)值相同
- bit7的cell均无enter/exit属性
- 推断：type=200的bit7可能是**"隐藏池"或"内部池"标记**，表示该状态池不在主界面显示

### 16.4 type=201 attr 位标志 → ✅ 已确认：转移选项复选框（2026-06-10 更正）

**⚠️ v14.0 原推断已全部推翻！** 原基于2919个XML统计的 bit19/20/21 含义（板块成员/指标条件/排序条件）经14个单变量对照实验证实**完全错误**。

**✅ 真实含义（2026-06-10 对照实验确认）**:

| Bit | 掩码 | ✅ 真实含义 | 旧错误推断 | 证据 |
|-----|------|-----------|-----------|------|
| bit19 | 0x80000 | **输出成分股** | 板块成员转移条件 | 单变量: 仅勾选此项→attr=524288 |
| bit20 | 0x100000 | **不删除状态中股票(保留源池)** | 指标条件类型 | 单变量: 仅勾选→attr=1048576; 76.8%出现率因默认勾选 |
| bit21 | 0x200000 | **转移前先清空目的状态池** | 排序条件类型 | 单变量: 仅勾选→attr=2097152 |

详见 **§4.8a Type=201 转移选项体系** 获取完整的5截图+14XML对照实验数据。

**剩余未确认位（仍需进一步实验）**:

| 位 | 掩码 | 出现次数 | 当前推断 |
|----|------|---------|---------|
| bit18 | 0x40000 | 20 | 反向转移/条件池转移 |
| bit22 | 0x400000 | 43 | 横向统计/条件池转移 |

### 16.5 Flow attr bit1/bit14 → 已确认

**bit1(0x2) = 删除源+保留源同时设置（矛盾标记/强制移动）**

7个实例全部是 attr=3(0x3) = bit0 + bit1，flow方向均为 type=201 → type=200：

| 文件 | from→to | 含义 |
|------|---------|------|
| 天机5号-分时横盘突破 | 201→200(量比缩小当日轨迹) | 转移条件→状态池 |
| 九龙峰AAX选股池 | 201→200(激进出击) | 转移条件→状态池 |
| 天机3号-锁仓拉抬池 | 201→200(观察池) | 转移条件→状态池 |

**推断**：bit0+bit1同时设置表示**"强制移动模式"**——股票从源池移除并强制写入目标池（即使目标池已有同名股票也覆盖），区别于仅bit0的普通删除源模式。这些flow均无begin/begint/end/endt/interval属性，说明它们是入流边（跟随出流边的时序控制）。

**bit14(0x4000) = 输出成份股（已确认）**

95个实例的详细分析：

| attr值 | 十六进制 | 含义 | 来源文件 |
|--------|----------|------|---------|
| 16384 | 0x4000 | 仅输出成份股（出流） | 金色阳光(爆发篇) |
| 16385 | 0x4001 | 输出成份股+删除源（入流） | 金色阳光(爆发篇) |
| 20480 | 0x5000 | 输出成份股+不删源（出流） | 金色两点半 |
| 20481 | 0x5001 | 输出成份股+不删源+删除源（入流） | 金色两点半 |

**关键验证**：说明书第24页原文"打勾'输出成份股'"，对应的正是flow attr bit14。金色阳光(爆发篇)的源节点是type=202(备选池，含板块指数)，完全匹配"当源是板块时输出成份股"的描述。

**bit14总是与bit12(0x1000,不删源)组合出现**（0x5000=0x4000+0x1000），因为输出成份股时源板块需要保留。

### 16.6 type=200 专有属性 → ✅ 已完全确认（2026-06-10 对照实验）

| 属性 | 结论 | 依据 |
|------|------|------|
| `deltype` | **删除时间单位枚举**：0=进入N天后(自然日), 1=进入N个交易日后, 2=N小时后, 3=N分钟后, 4=N秒后。**决定hold字段的时间单位含义** | ✅ 6组对照实验完整验证，每种deltype值均有独立样本 |
| `endtime` | **删除绝对时间参数**：0=相对时间模式(不使用), 非0=HHMMSS格式绝对时间(如150000=15:00:00)。仅当delstocktype=1时有效 | ✅ 对照实验确认delstocktype=1时启用此字段 |
| `delstocktype` | **删除时机模式**：0=相对时间模式(入池后N单位时间自动删除), 1=指定交易时间模式(每日指定时刻检查删除) | ✅ 指定交易时间实验唯一一个delstocktype=1的样本 |
| `staattr` | **双击显示模式标志**：0=K线模式(默认/无此属性), 1=分时线模式。当状态池配置了"切换到显示=分时线"时出现 | ✅ 10组对照实验完整验证：所有分时池staattr=1，所有K线池无staattr |
| `stocknum` | **最大股票数量限制**：配置项，限制状态池中最多保留N只股票 | 仅8个实例，均为用户自定义的复杂股票池 |
| `tmpl` | **双击显示目标模板**：字符串，格式为 `TYPE-name`。双击该池中股票时打开的目标窗口/模板 | ✅ 10组对照实验验证3种前缀：TEML-(看盘模板), PAGE-(弹出页面), INDI-(指标模板) |

### 16.7 tradeattr 子元素 → 已完整解析

type=200 cell可包含`<tradeattr>`子元素（76个实例），记录交易配置参数：

| 属性 | 含义 | 示例值 |
|------|------|--------|
| `accountno` | 账户编号，-1=默认账户 | "-1" |
| `entertradetype` | 入池交易类型，0=不交易 | "0" |
| `enterrate` | 入池交易比例(仓位) | "0.10"(10%) |
| `enterbuytype` | 入池买入类型，0=不买入 | "0" |
| `enterbuyvalue` | 入池买入金额/数量 | "0" |
| `enterbuypricetype` | 入池买入价格类型，0=市价 | "0" |
| `enterselltype` | 入池卖出类型，0=不卖出 | "0" |
| `entersellvalue` | 入池卖出金额/数量 | "0" |
| `entersellpricetype` | 入池卖出价格类型 | "0" |
| `leavetradetype` | 出池交易类型，0=不交易 | "0" |
| `leaverate` | 出池交易比例(仓位) | "0.10"(10%) |
| `leavebuytype` | 出池买入类型 | "0" |
| `leavebuyvalue` | 出池买入金额/数量 | "0" |
| `leavebuypricetype` | 出池买入价格类型 | "0" |
| `leaveselltype` | 出池卖出类型 | "0" |
| `leavesellvalue` | 出池卖出金额/数量 | "0" |
| `leavesellpricetype` | 出池卖出价格类型 | "0" |
| `buycontrolnbhs` | 买入控制笔数 | "0" |
| `buycontrolmbsc` | 买入控制目标，-1=不限制 | "-1" |

tradeattr是enter/exit属性的**扩展版**，提供更精细的交易控制。enter/exit是简化的位标志，tradeattr是完整的交易参数配置。

### 16.8 type=1 attr 字体样式位 → 仍需设计器验证

bit0-7组合出各种attr值，但每个位单独的字体渲染效果尚未确认。常见组合值与渲染效果的对应关系需要通过DZH设计器对照实验确定。

### 16.10 全面审查：未知含义字段与说明书功能缺失映射

基于206个XML样本文件的统计分析与说明书对照，以下为完整审查结果。

#### 16.10.1 XML属性含义仍不明确的字段

| 属性 | 适用type | 出现次数 | 已知值 | 含义状态 |
|------|---------|---------|--------|---------|
| `deltype` | 200 | 119 | 0~4 | **✅ 已确认**（2026-06-10）：删除时间单位枚举，见4.7a节完整定义 |
| `endtime` | 200 | 119 | 0 / HHMMSS | **✅ 已确认**（2026-06-10）：绝对时间参数，仅delstocktype=1时有效 |
| `delstocktype` | 200 | 119 | 0~1 | **✅ 已确认**（2026-06-10）：删除时机模式(0=相对,1=指定交易时间) |
| `staattr` | 200 | 45 | 始终为1 / _(无)_ | **✅ 已确认（2026-06-10）**：双击显示模式，0=K线(默认), 1=分时线。详见§4.7b |
| `tmpl` | 200 | 1+9=10 | TEML-*/PAGE-*/INDI-* | **✅ 已确认（2026-06-10）**：双击目标模板，格式TYPE-name。详见§4.7b |
| `indiparam` | 201 | 20 | 各种参数元组 | **不明确**：指标参数，格式为括号包裹的逗号分隔值，如"(1,25,100,30,15,30)" |
| `mode` | pool | 204 | 始终为1 | **不明确**：运行模式，仅1个值，含义未知 |
| `system` | pool | 1 | "0" | **推断**：系统标记，0=系统级股票池 |
| `warning` | pool | 2 | URL | **不明确**：帮助/预警页面URL，极罕见 |

~~**关键发现**：`deltype`/`endtime`/`delstocktype` 三者始终同时出现（119次），且值均为0。这暗示它们可能是同一组相关功能的配置项（定时/条件删除），但在现有样本中从未被启用。~~

> **已解决（2026-06-10）**：通过6组DZH设计器对照实验完全解密了 deltype(0~4时间单位枚举) + endtime(绝对时间) + delstocktype(相对/指定时间模式) 的完整含义。详见4.7a节"Type=200 删除时间体系"。

#### 16.10.2 type=200 attr 仍待确认的位标志

| 位 | 掩码 | 出现次数 | 占比 | 当前推断 | 确认状态 |
|----|------|---------|------|---------|---------|
| bit7 | 0x80 | 30 | 1.0% | 隐藏池/内部池标记 | **未确认** |
| bit24 | 0x1000000 | 358 | 12.4% | 列表基于前一状态计算收益(高位) | **待确认** |
| bit28 | 0x10000000 | 221 | 7.7% | 收益分析计算模式标志 | **待确认** |
| bit29 | 0x20000000 | 851 | 29.6% | **标准状态池/参与流转标记** | **推断**（见下方超1号分析） |
| bit31 | 0x80000000 | 309 | 10.7% | **可接收上游转移/主状态池标记** | **推断**（见下方超1号分析） |

**bit29 是最高频的未知位**：出现851次（29.6%），几乎每3个type=200就有1个含bit29。其高频出现暗示可能是重要功能标志而非版本标记。

#### 16.10.2a bit29/bit31 超1号对照分析（新增）

对超1号.xml中全部9个type=200状态池的逐位分析，发现bit29和bit31有明确的分布规律：

| 状态池 | attr值 | bit27 | bit29 | bit31 | 特征 |
|--------|--------|-------|-------|-------|------|
| 风险控制(id=2) | 0x08000000 | ✅ | ❌ | ❌ | 仅bit27，无bit29/bit31 |
| 热点股票(id=67) | 0x28000000 | ✅ | ✅ | ❌ | 有bit29但无bit31 |
| 短线资金/异动扫描/观察池/长线资金(4个) | 0xA8000000 | ✅ | ✅ | ✅ | 标准三件套(27+29+31) |
| 热点板快(id=65) | 0xA9000000 | ✅ | ✅ | ✅ | +bit24 |
| 英雄榜(id=60) | 0xA9800000 | ✅ | ✅ | ✅ | +bit23+bit24 |
| 超赢1号池(id=34) | 0xF9000000 | ✅ | ✅ | ✅ | +bit24+28+30(全开) |

**频率统计**：bit27=100%(9/9), bit29=89%(8/9), bit31=78%(7/9)

**推断**：
- **bit29(0x20000000) = "标准状态池标记"或"参与正常流转"**：仅"风险控制"(特殊控制节点)没有bit29；"热点股票"(输出型/衍生型状态池)有bit29但无bit31
- **bit31(0x80000000) = "可接收上游转移"或"主状态池"**："热点股票"只接收来自"热点板快"的特殊转移，不参与常规流转

> ⚠️ 以上为统计推断，需通过DZH设计器对照实验确认（如将"风险控制"改为普通状态池观察bit29变化）。

#### 16.10.2b alert设置存储位置的⚠️重大发现

**实验对象**: `超1号选中显示到股票池总览...xml` — 同一文件内9个type=200 cell的完整对照

**核心发现：所有9个type=200 cell的 enter=0 且 exit=0**

| ID | 名称 | attr | enter | exit | 截图可见的alert设置 |
|----|------|------|-------|------|-------------------|
| 2 | 风险控制 | 0x08000000 | **0** | **0** | 仅"输出到滚动预警窗"✅ |
| 27 | 短线资金 | 0xE8800000 | **0** | **0** | 全部4项✅ |
| 23 | 异动监控 | 0xA8000000 | **0** | **0** | 全部4项✅ |
| 34 | 异动扫描 | 0xF9000000 | **0** | **0** | 全部4项✅ |
| 60 | 超赢1号池 | 0xA9800000 | **0** | **0** | 预警声+弹出+滚动✅，高亮❌ |
| 61 | 观察池 | 0xA8000000 | **0** | **0** | 全部4项✅ |
| 16 | 长线资金 | 0xA8008000 | **0** | **0** | 全部4项✅ |
| 65 | 热点板快 | 0xA9000000 | **0** | **0** | 预警声+弹出+高亮✅，滚动❌ |
| 67 | 热点股票 | 0x28000000 | **0** | **0** | 无任何alert设置 |

**结论**: 之前的映射 **"发出预警声=enter bit0 / 弹出预警提示=enter bit1 / 高亮闪烁显示=exit bit0 / 输出到滚动预警窗=exit bit1" 完全错误**。alert四选项不存储在enter/exit属性中。存储位置需全新假设和实验。

**纯净单变量匹配 — 高亮闪烁显示 ↔ bit23**:

对比 id=65(热点板快) vs id=60(超赢1号池):
- id=65: 高亮闪烁✅ → attr=**0xA9000000** → bits=[24,27,29,31] + **bit23=0**
- id=60: 高亮闪烁❌ → attr=**0xA9800000** → bits=[24,27,29,31] + **bit23=1**
- 其他配置完全一致（预警声✅/弹出✅/滚动❌），唯一差异 = **bit23**
- **结论: 高亮闪烁显示 ↔ bit23 (0x800000)，纯净匹配 ✅**

**注意**: bit23之前被标注为"列表基于前一状态计算收益"。两者可能共享同一bit，或其中一个标注有误，需进一步纯净实验确认。

**滚动预警窗 — 不纯净，需进一步实验**:

对比 id=67(热点股票) vs id=65(热点板快):
- id=67: 滚动❌ + 无其他alert → attr=**0x28000000** [27,29]
- id=65: 滚动❌ 但有预警声+弹出+高亮 → attr=**0xA9000000** [24,27,29,31]
- **3个bit同时变化(bit24/bit30/bit31)**，无法确定哪个控制滚动预警窗
- 需要: 取同一cell，仅切换"输出到滚动预警窗"，保存后对比

#### 16.10.2c ✅ 渐进式单变量实验 — Alert四选项完全解密（2026-05-28）

**实验设计**: 同一XML文件，对同一状态池(id=34 异动扫描)逐步取消每个alert选项，每次保存为新文件，做单变量diff

**实验文件链**:
1. `超1号选中显示到股票池总览...xml` (BASE — 基线，全部alert勾选)
2. `去发出预警声.xml` (步骤①)
3. `去发出预警声去弹出预警提示.xml` (步骤②)
4. `去发出预警声去弹出预警提示去高亮闪烁显示.xml` (步骤③)
5. `去发出预警声去弹出预警提示去高亮闪烁显示去输出到滚动预警窗.xml` (步骤④)

**id=34(异动扫描) attr 渐变轨迹**:

| 步骤 | 文件 | attr值 | bits | 变化 | 对应操作 |
|------|------|--------|------|------|---------|
| BASE | 超一号 | **0xF9000000** | [24,27,28,29,30,31] | — | 基线(全部4项✅) |
| ① | 去预警声 | **0xE9000000** | [24,27,29,30,31] | **-bit28** | 取消"发出预警声" |
| ② | 去弹出 | **0xE8000000** | [27,29,30,31] | **-bit24** | 取消"弹出预警提示" |
| ③ | 去高亮(实际为基于) | **0xC8000000** | [27,30,31] | **-bit23** | 取消"列表基于前一状态计算收益"(⚠️文件名标注有误) |
| ④ | 去滚动窗(实际为高亮) | **0x48000000** | [27,30] | **-bit29** | 取消"高亮闪烁显示"(⚠️文件名标注有误) |

**✅ Alert位映射表（经2×2因子实验修正后）**:

| Alert选项 | bit | 掩码 | 确认方式 |
|----------|-----|------|---------|
| **发出预警声** | **bit28** | **0x10000000** | 渐进步骤①：F9→E9 (-0x10000000) ✅ |
| **弹出预警提示** | **bit24** | **0x01000000** | 渐进步骤②：E9→E8 (-0x01000000) ✅ |
| **列表基于前一状态计算收益** | **bit23** | **0x800000** | **2×2因子实验✅**（渐进步骤③误标为"去高亮"） |
| **高亮闪烁显示** | **bit29** | **0x20000000** | **2×2因子实验✅**（渐进步骤④误标为"去滚动窗"） |
| **输出到滚动预警窗** | **❓未知** | — | 渐进实验和2×2实验均未纯净匹配，需单独实验 |

**关键发现**:
- alert设置存储在 **cell.attr** 中（不是enter/exit，推翻之前全部假设）
- 每个alert选项对应**独立的bit位**，互不干扰
- **⚠️ bit23≠高亮闪烁，bit29≠滚动预警窗！** 之前渐进实验的步骤③④文件名标注与实际操作不符
- **id=61(观察池)** 在所有5个文件中attr值完全相同 → 实验操作的是id=34而非id=61

#### 16.10.2e ✅ 2×2因子实验 — bit23/bit29精确分离（2026-05-28）

**实验设计**: 对同一cell(id=34)做2×2全因子组合，彻底分离"列表基于前一状态计算收益"与"高亮闪烁显示"

**实验矩阵 — id=34 attr值**:

| | 无高亮 ❌ | 有高亮 ✅ |
|--|----------|----------|
| **无基于 ❌** | 0x58000000 [27,28,30] | 0x78000000 [27,28,**29**,30] → **+bit29** |
| **有基于 ✅** | 0x58800000 [**23**,27,28,30] → **+bit23** | 0x78800000 [**23**,27,28,**29**,30] → **+bit23+bit29** |

**结论 — 正交验证**:
- 仅切换"列表基于前一状态计算收益" → **只变bit23**，高亮不变 ✅
- 仅切换"高亮闪烁显示" → **只变bit29**，基于不变 ✅
- 同时切换 → **bit23+bit29叠加**，完全符合线性叠加 ✅
- **两个功能是独立的不同bit位，不存在双重含义**

> ~~待解: 已解决~~ — 见16.10.2f节：输出到滚动预警窗 = bit31 ✅

#### 16.10.2h 非核心属性发现 + "无买卖"编码（2026-05-28）

**实验文件**:
- `进出皆无买卖.xml` (进入=无买卖, 离开=无买卖)
- `双击切换到显示分时线.xml` (双击显示从K线改为分时线)

**发现1: "无买卖"的精确编码**

| cell | enter值 | exit值 | 含义 |
|------|---------|--------|------|
| ids {2,27,23,34} | **10000 (0x2710)** | **10000 (0x2710)** | 显式设为"无买卖"(type=0x0 + param残留10000) |
| ids {60,61,16,65,67} | **MISS(属性缺失)** | **MISS(属性缺失)** | 从未配置过交易动作（默认状态） |

> **关键区分**: `enter/exit属性完全缺失(MISS)` ≠ `enter/exit=0` ≠ `enter/exit=0x2710`
> - **MISS**: XML中根本没有这个属性 → 从未设置过
> - **0x2710(10000)**: type=0x0(无动作)但低16位保留参数值10000 → 用户显式选了"无买卖"
> - 这解释了为什么交易文件的enter值低16位始终是10000

**发现2: `staattr` 属性 — 双击切换显示模式**

唯一差异出现在交集状态池cells {2,27,23,34}:

| 文件 | id=2额外属性 |
|------|------------|
| 进出皆无买卖 | 无staattr |
| 双击→分时线 | **`staattr="1"`** |

> 截图操作: "切换到显示"下拉框从 **K线** 改为 **分时线**
> 推测: `staattr` 编码双击后的显示模式（0=K线/默认, 1=分时线? 值映射需进一步实验确认）

**发现3: 其他非核心属性汇总** (本次实验首次完整提取)

| 属性名 | 出现位置 | 示例值 | 推测功能 |
|--------|---------|--------|---------|
| **sorttype** | id=2(风险控制) | -1000 | 排序类型 |
| **intersection_status** | 交集cell {2,27,23,34} | "风险控制;短线资金;" | 交集来源池列表(分号分隔) |
| **wizd** | 所有cell | "具有良好流动性的股票" | 状态池描述文字(备注) |
| **attrtext** | 部分cell | "B$#板块指数" | 显示文本模板? |
| **reload** | 部分cell | 0 | 重载标志 |
| **lastload** | 部分cell | 1779904331 | 最后加载时间戳(Unix?) |
| **col/width** | type=200容器子元素 | 数字 | 列宽配置 |

#### 16.10.2d ✅ Enter/Exit属性 — 交易动作编码完全解密（2026-05-28）

**实验文件**:
- `买入指定股数10000.xml`
- `买入指定金额10000.xml`
- `卖出股票.xml`

**⚠️ 修正**: 之前因XML解析chunk截断导致enter值低位丢失，实际完整值如下：

**Enter+Exit值对比** (作用于ids {2,27,23,34}):

| 文件 | enter值(hex) | enter动作 | exit值(hex) | exit动作 |
|------|-------------|----------|------------|---------|
| **买入股数** | 0x30002710 | 卖出股票 10000 | **0x20002710** | **买入指定股数 10000** |
| **买入金额** | 0x30002710 | 卖出股票 10000 | **0x10002710** | **买入指定金额 10000** |
| **卖出** | 0x30002710 | 卖出股票 10000 | **0x30002710** | **卖出股票 10000** |
| **进出皆无买卖** | **0x00002710** | **无买卖(显式)** | **0x00002710** | **无买卖(显式)** |

> 文件名描述的是 **exit（离开时）动作**
>
> ids {60,61,16,65,67} 在所有交易文件中仍为enter=MISS/exit=MISS，说明交易动作是逐cell设置的
>
> **"无买卖"编码**: type=0x0(无动作) + param=0x2710(10000残留)。注意: `MISS`(属性不存在) ≠ `0x00002710`(显式设为无买卖)

**✅ Enter/Exit统一编码结构（完全确认）**:
```
[bit31..bit28: 动作类型] [bit27..bit16: 保留(全0)] [bit15..bit0: 参数值]
  0x0??????? = 无动作
  0x1??????? = 买入指定金额 (参数=低16位值，单位:元)
  0x2??????? = 买入指定股数 (参数=低16位值，单位:股)
  0x3??????? = 卖出股票       (参数=低16位值，单位:股)

  实例验证:
  0x30002710 → 类型=0x3(卖出), 参数=0x2710=10000 ✅
  0x20002710 → 类型=0x2(买入股数), 参数=0x2710=10000 ✅
  0x10002710 → 类型=0x1(买入金额), 参数=0x2710=10000 ✅
```

**关键确认**:
- **Enter和Exit使用完全相同的编码方式**
- bit27~bit16在所有实验中均为0（保留字段）
- 低16位直接存储十进制参数值（如10000 = 0x2710）
- 截图显示的界面：进入=买入指定金额/离开=卖出股票，与XML数据一致

#### 16.10.3 type=201 attr 仍待确认的位标志

| 位 | 掩码 | 出现次数 | 占比 | 当前推断 | 确认状态 |
|----|------|---------|------|---------|---------|
| bit9 | 0x200 | 241 | 8.3% | 显示到股票池总览 | **推断** |
| bit11 | 0x800 | 920 | 31.5% | 基本条件标志 | **推断** |
| bit16 | 0x10000 | 2 | 0.1% | 未知 | **未确认**（极罕见） |
| bit17 | 0x20000 | 1 | 0.0% | 未知 | **未确认**（极罕见） |
| bit18 | 0x40000 | 20 | 0.7% | 反向转移/条件池转移 | **推断** |
| ~~bit19~~ | ~~0x80000~~ | ~~155~~ | ~~5.3%~~ | ~~板块成员转移条件~~ | **✅ 已推翻更正(2026-06-10) → 输出成分股，见§4.8a** |
| ~~bit20~~ | ~~0x100000~~ | ~~2243~~ | ~~76.8%~~ | ~~指标条件类型~~ | **✅ 已推翻更正(2026-06-10) → 不删除源池，见§4.8a** |
| bit21 | 0x200000 | 882 | 30.2% | ~~排序条件类型~~ | **✅ 已更正(2026-06-10) → 清空目的状态池，见§4.8a** |
| bit22 | 0x400000 | 43 | 1.5% | 横向统计/条件池转移 | **推断** |

> **2026-06-10 更正**: bit19/20/21 经对照实验已从"条件子类型"更正为"转移选项复选框"。详见§16.4和§4.8a。

#### 16.10.4 Flow attr 仍待确认的位标志

| 位 | 掩码 | 出现次数 | 占比 | 当前推断 | 确认状态 |
|----|------|---------|------|---------|---------|
| bit1 | 0x2 | 7 | 0.1% | 强制移动模式(bit0+bit1) | **推断** |
| bit14 | 0x4000 | 95 | 1.5% | 输出成份股 | **已确认**（说明书+对照实验） |

#### 16.10.5 说明书提到的功能在XML中无对应配置的

| 说明书功能 | 出处 | XML对应 | 说明 |
|-----------|------|--------|------|
| **鼠标双击股票的动作** | 第14页 | 无独立属性 | 文档标注为"-"，可能硬编码在DZH程序中 |
| **分析周期**（分笔/1分/5分/.../月线/季线/半年线/年线等14种） | 第20页 | **✅ 已确认(2026-06-10)**: 不在独立XML属性中，编码在`indi`(Base64公式内容)内部 | 14种周期选项，8个单变量对照实验证实仅crc+indi变化，attr不变。详见§4.8b |
| **交易统计**（运行天数/总收益/平均收益率等8项） | 第16页 | 无属性 | 运行时统计展示，不持久化到XML |
| **收益分析5种类型**（日内收益/市场冲击/历史收益/历史收益分布/定位分析） | 第39页 | 无属性 | 运行时分析功能，不持久化到XML |
| **保存交易信息到文件** | 第17页 | 无属性 | 运行时操作，不持久化 |
| **清除全部交易信息** | 第17页 | 无属性 | 运行时操作，不持久化 |
| **ENTERPOOLPRICE(N)** | 第19页 | 无属性 | 公式函数，在indi公式代码中使用 |
| **ENTERPOOLBARS(N)** | 第19页 | 无属性 | 公式函数，在indi公式代码中使用 |
| **ENTERPOOLPARAM(N)** | 第19页 | 无属性 | 公式函数，需外部.PAR文件 |
| **股票池流程设置窗口**（三栏界面） | 第22页 | 无属性 | 运行时修改界面，修改结果映射到cell+flow |
| **高亮闪烁显示** | 第37页 | **cell.attr bit23 (0x800000)** | ✅ **已确认** — 渐进式单变量实验步骤③ + 超一号纯净对照（与"列表基于前一状态计算收益"共享bit23） |
| **输出到滚动预警窗** | 第37页 | **cell.attr bit29 (0x20000000)** | ✅ **已确认** — 渐进式单变量实验步骤④ |
| **发出预警声** | 第37页 | **cell.attr bit28 (0x10000000)** | ✅ **已确认** — 渐进式单变量实验步骤① |
| **弹出预警提示** | 第37页 | **cell.attr bit24 (0x01000000)** | ✅ **已确认** — 渐进式单变量实验步骤② |
| **在交易统计中自动买入/卖出** | 第37页 | **enter属性(非tradeattr)** | ✅ **已确认** — enter值编码交易类型+参数：bit28=买入金额, bit29=买入股数, bit28+29=卖出, 低16位=参数值 |
| **只执行一次** | 第20页(flow时序) | 未明确如何编码 | 持续时长有3种枚举，"只执行一次"可能为第4种 |
| **简易中间态** | **2011版说明书中不存在** | cell.attr bit15 (0x8000) | 对照实验已确认功能，但2011版说明书发布时尚未存在此功能 |
| **列表基于前一状态计算收益** | **2011版说明书中不存在** | cell.attr bit23 (0x800000) | 对照实验已确认，后续版本新增功能 |
| **显示到股票池总览(type=200级)** | 第14页仅列名称 | cell.attr bit30 (0x40000000) | 说明书中仅在设置列表提到名称，未单独说明复选框行为 |
| **状态池最多N(stocknum)** | **2011版说明书中不存在** | stocknum属性 | 后续版本新增的数量限制功能 |
| **收益分析最多N(histana)** | **2011版说明书中不存在** | histana属性 | 后续版本新增的数据点数参数（说明书仅提"收益分析"复选框） |

**"分析周期"编码位置已确认（2026-06-10）**: 转移条件设置窗口有14种分析周期选择，经8个单变量对照实验证实**不存储在任何独立XML属性中**，而是编码在`indi`(Base64公式内容)内部。变更分析周期时仅`crc`(校验和)和`indi`(公式blob)变化，`attr`值完全不变。详见§4.8b。

#### 16.10.6 XML中有配置但说明书未提及的功能

| XML配置 | 含义 | 说明 |
|---------|------|------|
| `tradeattr`子元素(19个属性) | 入池/出池交易配置 | 说明书仅提到"买入/卖出"，但XML有19个详细交易参数 |
| `histana` | 收益分析数据点数 | 说明书仅提到勾选"收益分析"，未提及数据点数配置 | **2011版说明书不存在，后续版本新增** |
| `stocknum` | 状态池最大股票数量 | 说明书未提及此限制 | **2011版说明书不存在，后续版本新增** |
| `staattr` | 状态属性标志 | 说明书未提及 |
| `deltype`/`endtime`/`delstocktype` | 删除时间体系（✅已完全确认） | **2026-06-10对照实验解密**：deltype=时间单位枚举(0~4), endtime=绝对时间参数, delstocktype=相对/指定时间模式。详见4.7a节 |
| `tmpl` | 关联模板名称 | 说明书未提及模板关联功能 |
| `indiparam` | 指标参数 | 说明书未提及参数传递机制 |
| `mid` (flow) | 线条中点偏移 | 说明书未提及线条弯曲控制 |
| type=203 | 未知类型节点 | 说明书未提及此类型 |

#### 16.10.7 dzh_constants.py 位标志修正记录

对照实验后发现代码中有多处位标志定义错误，已修正：

| 常量名 | 旧值(错误) | 新值(正确) | 说明 |
|--------|-----------|-----------|------|
| `CELL200_ATTR_SHOW_OVERVIEW` | 0x200 | 0x40000000 | type=200的"显示到总览"是bit30，不是bit9 |
| `CELL200_ATTR_HISTORY_TRACK` | 0x8000 | 0x8000000 | 0x8000是"简易中间态"，"记录历史轨迹"是bit27 |
| `CELL200_ATTR_PROFIT_ANALYSIS` | 0x8000000 | 已删除 | 收益分析对应histana属性，不是attr位标志 |
| 新增`CELL200_ATTR_SHOW_OVERVIEW_CONTAINER` | - | 0x200 | type=2容器的总览标记(bit9) |
| 新增`CELL200_ATTR_SIMPLE_INTERMEDIATE` | - | 0x8000 | 简易中间态(bit15) |
| 新增`CELL200_ATTR_CALC_PROFIT_FROM_PREV` | - | 0x800000 | 列表基于前一状态计算收益(bit23)，与高亮闪烁共享 |
| 新增`CELL200_ATTR_HIGHLIGHT_FLASH` | - | 0x800000 | 高亮闪烁显示(bit23)，渐进实验✅ |
| ~~`CELL200_ATTR_PROFIT_CALC_HIGH`~~ | 0x1000000 | → **`CELL200_ATTR_ALERT_POPUP`** | 弹出预警提示(bit24)，渐进实验✅ |
| ~~`CELL200_ATTR_PROFIT_ANALYSIS_MODE`~~ | 0x10000000 | → **`CELL200_ATTR_ALERT_SOUND`** | 发出预警声(bit28)，渐进实验✅ |
| ~~`CELL200_ATTR_UNKNOWN_D`~~ | 0x20000000 | → **`CELL200_ATTR_ALERT_SCROLL_WINDOW`** | 输出到滚动预警窗(bit29)，渐进实验✅ |
| `ACTION_BASE_BUY_SELL` | 0x2710(误作标志位) | 已删除 | 0x2710=10000是参数值不是标志位 |
| ~~`ACTION_ALERT_SOUND`~~ | 0x10000000(误作alert) | → **`ACTION_BUY_BY_AMOUNT`** | enter中bit28=买入金额模式 |
| ~~`ACTION_ALERT_POPUP`~~ | 0x20000000(误作alert) | → **`ACTION_BUY_BY_SHARES`** | enter中bit29=买入股数模式 |
| `decode_action()` | 返回alert_sound/alert_popup/highlight_flash | 重写为action_type/param | 完全重构：enter编码交易类型+参数 |
| 新增`FLOW_ATTR_FORCE_MOVE` | - | 0x2 | 强制移动模式(bit1) |
| 新增`FLOW_ATTR_OUTPUT_COMPONENTS` | - | 0x4000 | 输出成份股(bit14) |


### 16.9 仍需进一步研究的字段（更新于2026-06-10）

| 字段 | 当前状态 | 建议研究方法 | 优先级 |
|------|---------|-------------|--------|
| **type=200 bit31(0x80000000)** | ✅ 已确认 = 输出到滚动预警窗 | 滚动窗ON/OFF纯净实验: id=34 F8↔78 XOR=0x80000000 | ✅ 已解决 |
| ~~bit23双重含义~~ | ✅ 已解决: bit23=基于计算收益, bit29=高亮闪烁, 两者独立 | 2×2因子实验正交验证 | ✅ 已解决 |
| **deltype(0~4) / endtime / delstocktype(0/1)** | **✅ 已完全确认（2026-06-10）**：删除时间体系，6组对照实验解密。详见4.7a节 | — | ✅ **已解决** |
| type=200 bit7(0x80) | 推断为"隐藏池/内部池"标记 | 在DZH设计器中创建含bit7的状态池，观察界面差异 | 🟡 P1 |
| **enter/exit属性中间位(bit27~bit16)** | 所有实验中均为0，含义未知 | 尝试设置极端参数值或特殊动作组合，观察这些位是否被使用 | 🟡 P1 |
| **endtime非0值的具体编码** | **✅ 已确认（2026-06-10）**：endtime = 3600×HH − 900×MM + SS。3样本验证(10:30:50→9050, 14:30:10→23410) | — | ✅ **已解决** |
| type=201 bit18(0x40000) | 推断为"反向转移" | 在DZH设计器中设置"不满足条件时转移"，观察attr变化 | 🟢 P2 |
| type=1 bit0-7 | 字体样式位组合 | 在DZH设计器中逐一切换字体选项，记录attr变化 | 🟢 P2 |
| trades/opentrades | 运行时交易记录 | 需运行股票池后查看生成的trade数据 | 🟢 P2 |
| **flow: 只执行一次** | **✅ 已确认（2026-06-10）**：`end=1, endt=1`。流程属性对话框"持续时间→只执行一次"直接映射。同时确认 `interval`=秒(60→60) | 1截图+1XML对照实验 | ✅ **已解决** |
| type=203 | 未知类型节点(3实例) | 分析其在拓扑中的角色和与type=200的区别 | 🟢 P2 |

**已确认（无需进一步研究）**：
- type=200 bit15 = 简易中间态 ✅
- type=200 bit23 = 列表基于前一状态计算收益 + 高亮闪烁显示（双重含义）✅
- type=200 bit24 = 弹出预警提示 ✅
- type=200 bit27 = 记录历史轨迹 ✅
- type=200 bit28 = 发出预警声 ✅
- type=200 bit29 = 输出到滚动预警窗 ✅（之前错误推断为"标准状态池标记"）
- type=200 bit30 = 显示到股票池总览 ✅
- alert四选项全部在cell.attr中（不在enter/exit）✅
- **enter属性编码交易动作: bit28=买入金额, bit29=买入股数, bit28+29=卖出, 低16位=参数** ✅
- **exit属性与enter完全相同的编码方式** ✅
- dzh_constants.py 全部常量已修正验证通过 ✅
- **deltype(0~4)=时间单位枚举, endtime=绝对时间, delstocktype=相对/指定时间模式** ✅ (2026-06-10新增)
- **staattr(0/1)=双击显示模式标志(K线/分时线), tmpl(TYPE-name)=双击目标模板(TEML-/PAGE-/INDI-)** ✅ (2026-06-10新增)

---

## 十七、系统表设计参考

基于以上完整分析, 所需纲表：

### 17.1 modules.json (模块定义表)

| module_id | 名称 | 对应cell type | 核心功能 |
|-----------|------|-------------|---------|
| market_selector | 备选池 | type 202 | 从交易所/板块选股 |
| stock_pool | 证券池 | type 200 | 存储筛选结果股票 |
| formula_pool | 公式连接器 | type 201 | 池间筛选公式 |
| discard_sink | 丢弃容器 | type 4 | 不满足条件暂存 |
| flow_arrow | 流程箭头 | type 6 | 可视化连接线 |
| text_label | 文字标签 | type 1 | 标题/说明/标注 |
| time_trigger | 时序控制 | (flow属性) | 时间触发条件 |
| profit_analysis | 收益分析 | (工具) | 五种收益分析 |

### 17.2 pipelines.json (流程定义表)

| pipeline_id | 模式 | 基于 |
|------------|------|------|
| standard_serial | 标准串行分支汇合 | 超赢1号 |
| parallel_state | 并行状态累积合并 | 超赢7号 |
| time_phased | 时序分阶段 | 金色两点半 |

### 17.3 dispatch.json (条件/转移定义表)

三类转移条件 + 具体条件实例：
- indicator_based: 指标是或非
- ranking_based: 数值排序
- sector_membership: 板块成员

每种含 analysis_cycle, delete_source, clear_dest, output_constituent 等选项

### 17.4 engines.json (计算引擎表)

对应分析周期 + 计算方式：
- tick_engine: 分笔
- minute_1/5/15/30/60: 分钟
- daily_engine: 日线
- weekly_engine: 周线
- monthly_engine: 月线

### 17.5 markets.json (市场定义表)

对应 attrtext 市场代码：
- SH_A, SZ_A, SZ_SME, SZ_GEM, SH_B, SZ_B, SECTOR_INDEX 等

### 17.6 schedules.json (时序模板表)

对应时序控制参数：
- immediate: 立即
- after_open: 开市后
- before_close: 收市前
- specific_time: 指定时间
- golden_two_half: [10:00, 11:00, 14:00, 14:20, 14:30]

---

## 十八、右键交互体系（✅ 已确认 — 2026-06-15 截图分析）

基于 **14 张大智慧设计器右键截图**（涵盖设计模式 + 运行时双模式），DZH 的右键菜单是一个**三维差异化体系**：按元素类型 × 运行模式 × 选中状态动态变化。

### 18.1 设计模式右键菜单矩阵

> 设计模式 = 股票池编辑器中的画布编辑状态，可修改所有属性和结构。

| # | 右键对象 | Cell/Flow Type | 菜单项数 | 菜单项列表 | 数据来源字段 | 截图证据 |
|---|---------|---------------|---------|-----------|------------|---------|
| 1 | 备选池 | type=202 | **2** | 属性, 修改说明文字 | cell 全部属性, cell.text/wizd | ✅ 右键备选池.png |
| 2 | 状态池(无选中) | type=200 | **6** | 属性, 修改说明文字, 手工添加股票到状态池, 清除状态池中所有股票, 所有股票加入到板块股, 显示行号 | psatt.*, wizd, show_row_num | ✅ 右键状态池.png |
| 3 | 状态池(有选中) | type=200 | **9** | ↑6项 + 将当前银行加入到自选股, 查看\|删除发行走势, 从本次池中删除该笔(行) | stk.*, 行选中状态 | ✅ 右键状态池有股票.png |
| 4 | 转移条件 | type=201 | **2~3** | 属性, 修改说明文字, (停止运算?) | func.*, disabled | ✅ 右键转移条件.png |
| 5 | 有条件边 | Flow(经type=201) | **2** | 属性, 线条宽度 | flow 时序属性, flow.size | ✅ 右键条件转移边.png |
| 6 | 无条件边 | Flow(直接连线) | **1** | 线条宽度 | flow.size | ✅ 右键无条件转移边.png |

#### 各菜单项详细说明

**备选池 (type=202) — 2 项菜单：**

| 菜单项 | 功能 | 操作内容 |
|--------|------|---------|
| 属性 | 打开备选池设置窗口 | 弹出「股票池备选股设置」对话框，可修改市场范围(attrtext)、重载模式(reload)等 |
| 修改说明文字 | 编辑节点说明 | 弹出"说明文字"对话框，输入自定义描述文本 → 存入 cell.wizd 或 cell.text |

**状态池 (type=200) — 设计模式 6~9 项菜单：**

| 菜单项 | 功能 | 操作内容 | 危险等级 |
|--------|------|---------|---------|
| 属性 | 打开状态池属性窗口 | 弹出状态属性设置窗口（常规+进出执行动作） | 安全 |
| 修改说明文字 | 编辑节点说明 | 弹出"说明文字"对话框 → 存入 cell.wizd | 安全 |
| 手工添加股票到状态池 | 手动添加股票 | 弹出「选择品种」对话框（12个tab分类树） | 安全 |
| 清除状态池中所有股票 | 清空池数据 | 弹出确认提示 → 清空所有 `<stk>` 子元素 | ⚠️ 不可逆 |
| 所有股票加入到板块股 | 批量加板块 | 弹出「请选择板块」对话框 | 安全 |
| 显示行号 | 切换行号显示 | 在股票表格前增加/隐藏 "#" 行号列 | 安全 |
| 将当前银行加入到自选股 | 加自选股（需选中行） | 将选中的股票加入用户自选组 | 安全 |
| 查看\|删除发行走势 | 查看K线（需选中行） | 打开选中股票的K线走势图 | 安全 |
| 从本次池中删除该笔(行) | 删除单条记录（需选中行） | 从状态池移除选中的特定股票 | ⚠️ 不可逆 |

**转移条件 (type=201) — 2~3 项菜单：**

| 菜单项 | 功能 | 操作内容 |
|--------|------|---------|
| 属性 | 打开转移条件设置窗口 | 弹出条件公式编辑器（公式/周期/排序/转移选项） |
| 修改说明文字 | 编辑节点说明 | 弹出"说明文字"对话框 |
| 停止运算/恢复运算 | 暂停或恢复该条件的执行 | 切换 disabled 标志（截图未明确确认此选项是否存在） |

### 18.2 运行时右键菜单矩阵

> 运行时 = 股票池正在执行的状态。右侧面板显示「正在运行的股票池」，包含各状态池的实时股票列表。

| # | 右键对象 | Type | 菜单项数 | 菜单项列表 | 备注 | 截图证据 |
|---|---------|------|---------|-----------|------|---------|
| 1 | 状态池(无股票) | type=200 | **0~2** | (基本无操作或仅有查看类操作) | 空池锁定大部分操作 | ✅ 右键状态池无股票.png |
| 2 | 状态池(有股票,未选中行) | type=200 | **3~5** | 将当前银行加入到自选股, 查看\|删除发行走势, 所有股票加入到板块股, 显示行号 | 无属性编辑（运行锁定） | ✅ 右键运行时状态池.png |
| 3 | 状态池(有股票,选中行) | type=200 | **4~6** | ↑ + 从本次池中删除该笔(行), (+可能更多) | 行级操作可用 | ✅ 右键运行时状态池选中股票时.png |
| 4 | 转移条件 | type=201 | **1**(特殊) | **▼ 股票列表下拉菜单** | 非标准弹出式菜单！点击展开显示通过该条件的股票列表 | ✅ 右键运行时转移条件.png |
| 5 | 条件边 | Flow | **1~2** | 线条宽度, (属性只读?) | 运行时限制编辑 | ✅ 右键运行时条件转移边.png |

#### 运行时关键特征

**设计 vs 运行的核心差异：**

| 特征 | 设计模式 | 运行时 |
|------|---------|--------|
| "属性"菜单 | ✅ 可用（打开完整属性窗口） | ❌ 锁定（运行中不可修改参数） |
| "修改说明文字" | ✅ 可用 | ❌ 锁定 |
| 数据操作类菜单 | ✅ 可用 | ✅ 可用（加自选/看走势/删行等） |
| 转移条件特殊行为 | 标准弹出式菜单 | **股票下拉列表**（非菜单） |

**转移条件(type=201) 运行时特殊行为详解：**

在运行时右键点击转移条件节点（黄色三角形"设"字图标），不会出现标准的弹出式上下文菜单，而是展开一个**内嵌的下拉选择列表**，列出当前通过该转移条件的所有股票名称：

```
┌─────────────────┐
│ 状态(3)       ▼ │ ← 点击展开
├─────────────────┤
│ 名称         ▼ │ ← 下拉列表
│ 几号公司        │
│ 华纳药厂        │
│ ...             │
└─────────────────┘
```

这是 DZH 转移条件独有的交互方式——用于快速查看"哪些股票通过了这个条件"，而非修改条件本身。

### 18.3 子对话框规范（DZH 原版）

#### 对话框 A：线条宽度

| 属性 | 值 |
|------|-----|
| 对话框标题 | **"线条宽度"** |
| 输入域标签 | **"线条宽度(1-16)"** |
| 输入类型 | 数字输入框 |
| 默认值 | **1** |
| 取值范围 | 1 ~ 16（整数） |
| 按钮 | 确定 / 取消 |
| 触发来源 | 边右键 → "线条宽度" |
| 存储位置 | `flow.size`（或等效的线条宽度属性） |
| 截图证据 | ✅ 右键线条宽度.png |

#### 对话框 B：说明文字

| 属性 | 值 |
|------|-----|
| 对话框标题 | **"说明文字"** |
| 输入域标签 | **"请输入说明文字"** |
| 输入类型 | 多行文本输入框 |
| 预填值 | 当前 cell.text 或 cell.wizd 的值 |
| 颜色选择器 | **❌ 无**（DZH 原版不含颜色选择功能） |
| 按钮 | 确定 / 取消 |
| 触发来源 | 节点右键 → "修改说明文字" |
| 存储位置 | `cell.wizd`（优先）或 `cell.text` |
| 截图证据 | ✅ 右键修改说明文字.png |

#### 对话框 C：清除确认

| 属性 | 值 |
|------|-----|
| 对话框标题 | **"提示"** |
| 图标 | 蓝色问号图标 (?) |
| 提示文本 | **"清除状态池中所有股票?"** |
| 按钮 | 是(Y) / 否(N) |
| 触发来源 | 状态池右键 → "清除状态池中所有股票" |

### 18.4 DZH vs TDX 右键交互差异对照表

| 维度 | DZH (大智慧) | TDX (通达信 V8) | 差异点 |
|------|-------------|----------------|--------|
| **备选池菜单项数** | 2 项 | 2 项 | 数量相同，但 DZH 无颜色选择器 |
| **备选池"说明文字"** | 仅文字输入 | 文字输入 + 颜色选择器 | TDX 多一个 clrtext 颜色功能 |
| **状态池设计模式** | 6~9 项 | 6~9 项 | 基本一致 |
| **状态池运行时空池** | 0~2 项（基本锁定） | 5 项 | DZH 更严格地锁定空池操作 |
| **转移条件运行时** | **股票下拉列表**（非菜单） | 标准弹出式菜单 | **完全不同的交互范式** |
| **边右键(有条件)** | 2 项：属性 + 线条宽度 | 2 项：属性 + 线条宽度 | 一致 |
| **边右键(无条件)** | 1 项：线条宽度 | 1 项：线条宽度 | 一致 |
| **线条宽度默认值** | **1** | 可能不同 | 需确认 TDX 默认值 |
| **线条宽度对话框标题** | "线条宽度" | "线条宽度" | 一致 |
| **说明文字对话框标题** | "说明文字" | 可能不同 | 需确认 TDX 标题 |
| **运行时锁定策略** | 完全锁定属性编辑类 | 锁定属性编辑但保留部分查看 | DZH 更严格 |
| **Cell 类型编号** | 200/201/202 | 8/3/7 | 完全不同的类型体系 |

### 18.5 右键交互与 XML 字段映射总表

| 右键功能 | 操作类型 | 读写字段 | XML Path | 变更是否影响 roundtrip |
|---------|---------|---------|----------|---------------------|
| 属性(备选池) | 读写 | attrtext, reload, lastload | cells/cell[@type=202] | ✅ 是 |
| 修改说明文字(备选池) | 读写 | wizd 或 text | cells/cell[@type=202]/@wizd | ✅ 是 |
| 属性(状态池) | 读写 | hold, col, deltype, endtime, delstocktype, staattr, enter, exit, attr(bits), wizd, histana, stocknum, tmpl | cells/cell[@type=200] | ✅ 是 |
| 修改说明文字(状态池) | 读写 | wizd 或 text | cells/cell[@type=200]/@wizd | ✅ 是 |
| 手工添加股票 | 写入 | stk (子元素) | cells/cell[@type=200]/stk[] | ✅ 是 |
| 清除全部股票 | 删除 | stk (子元素) | cells/cell[@type=200]/stk[] | ✅ 是 |
| 显示行号 | 切换 | show_row_num (内存标志, 不持久化到XML?) | — | ⚠️ 可能不持久化 |
| 加自选股 | 外部操作 | — | — | ❌ 不影响XML |
| 查看走势 | 只读 | label (来自stk子元素) | cells/cell[@type=200]/stk/@label | ❌ 不修改 |
| 删除单行 | 删除 | stk (单个子元素) | cells/cell[@type=200]/stk[N] | ✅ 是 |
| 属性(转移条件) | 读写 | inditype, crc, indi, sorttype, attr(bits) | cells/cell[@type=201] | ✅ 是 |
| 修改说明文字(条件) | 读写 | text | cells/cell[@type=201]/@text | ✅ 是 |
| 停止运算 | 切换 | disabled (内存标志) | — | ⚠️ 可能不持久化 |
| 属性(边/Flow) | 读写 | begin, begint, end, endt, interval, attr(bits) | flows/flow[@from][@to] | ✅ 是 |
| 线条宽度 | 读写 | size (或 mid 相关) | flows/flow[@from][@to]/@size? | ⚠️ size 字段位置待确认 |
| 加板块股 | 外部操作 | — | — | ❌ 不影响XML |

---

## 十九、综合设置窗口（股票池流程设置窗口）

综合设置窗口是股票池的"参数面板"，与"流程结构画布"（主画布）配对使用。流程画布是布局编辑器，综合设置窗口是结构、参数、条件、时序的集中查看与修改入口。两者共享同一份数据（`poolData`），选中节点双向联动。

### 19.1 窗口定位

- 入口：主菜单 → 股票池 → 综合设置（或快捷键 `Ctrl+I`）
- 适用对象：所有已加载的 DZH/TDX 股票池（`dzhpool/`、`tdxpool/`）
- 与画布的关系：画布为"主视图"（自由布局），本窗口为"参数总览"（按 BFS 层级严格展开）
- 数据来源：`poolData.data.nodes`（节点数组）与 `poolData.data.flows`（边数组）
- 实现位置：`meta_core/web/js/comprehensive-settings.js`（控制器）+ `meta_core/web/index.html`（`#csComprehensiveSettings` 容器）
- **视图形式**：表格视图（3 列多行），不再是树形结构 + 独立面板，而是所有信息按 BFS 顺序逐行展开在一张表格中

### 19.2 表格视图（3列布局）

表格视图承担"选股流程结构图"的视觉表达，所有行按 BFS 顺序严格展开：

```
┌────────────────────┬──────────────────────────────┬──────────────────────┐
│  流程标识          │  条件 / 属性                 │  时序 / 操作         │
│  (左列 ~22%)       │  (中列 ~52%)                 │  (右列 ~26%)         │
│                    │                              │                      │
│  ▣ 沪深A股          │  备选范围: SH#上证...        │  每次启动载入股票    │
│  └─▶ 初步筛选       │  满足自设定计算公式转移      │  开盘后0秒执行...    │
│    初步筛选         │  进入后保持:18小时...        │                      │
│  └─▶ 超赢备选       │  满足自设定计算公式转移      │  开盘后0秒执行...    │
│    超赢备选         │  进入后保持:18小时...        │                      │
│  └─▶ 强势整理       │  满足自设定计算公式转移      │  开盘后0秒执行...    │
│    ...             │                              │                      │
└────────────────────┴──────────────────────────────┴──────────────────────┘
```

- 表格使用 CSS 实现，3 列宽度比约 22% : 50% : 28%
- 行类型：备选池行 (`source`) / 状态池声明行 (`pool-decl`) / 条件边行 (`edge`)
- 左列彩色标签 + `▶` 箭头前缀本身即流程结构图
- 选中行时高亮（背景色变浅 + 左侧 3px 蓝条），中列与右列内容同步刷新

### 19.3 表格左列：流程标识

**3 种行类型的左列表现**：

| 行类型 | 左列样式 | 特征 |
|--------|----------|------|
| **备选池行** (`source`) | `▣ 备选池名` | 无箭头，depth=0，无缩进 |
| **状态池声明行** (`pool-decl`) | `  状态池名` | 无箭头，depth≥0，按 BFS 深度缩进 |
| **条件边行** (`edge`) | `└─▶ 目标状态池名` | 带拐角箭头，depth≥1，缩进比声明行深一级 |

**递归式边批量+深度优先算法（Row-Generation Algorithm）**：

表格行的生成采用**递归式边批量+深度优先**策略（非传统BFS队列），核心特征是**先边后池、遇新池即递归**：

```
算法伪代码：
function generateRows(sourcePool):
    1. 输出 source 行（备选池声明）
    2. 对 source 的每条出边 flow：
        a. 输出 EDGE 行（条件边，带 └─▶ 箭头）
        b. targetPool = flow.target
        c. 若 targetPool 首次被发现：
            - 输出 POOL-DECL 行（状态池声明，无箭头）
            - 递归调用 generateRows(targetPool)  // 立即深入子树
```

**关键特征**：
- **边优先**：对每个父池，先批量输出所有EDGE行
- **即时递归**：发现新目标池时，立即输出POOL-DECL并递归处理其子树（不等待同级其他池）
- **去重保证**：通过 visited 集合确保每个池只声明一次

**正确的行序示例**（以超赢7号为例）：
```
[source] → [edge] → [edge] → [pool-decl] → [edge] → [pool-decl] → [pool-decl] → [edge]
 ▣备选池   └─▶初步筛选  └─▶超赢备选  初步筛选属性  └─▶强势整理  超赢备选属性  强势整理属性  └─▶英雄榜
```

**错误的旧顺序**（已废弃）：
```
❌ [source] → [pool-decl] → [edge] → [edge] → [pool-decl] → [edge]
```

> **设计理由**：边优先顺序使用户在浏览时能立即看到"从当前池可以转移到哪里"，再看到目标池的详细属性，符合"源→转移→目标"的认知路径。

**层级缩进**：
- 通过 `.cst-row-depth-N` 类控制 `padding-left`
- 每级深度增加 16px，形成视觉上的树形层级
- 备选池 depth=0（12px），第一层边 depth=1（28px），后续按递归深度累加

**核心原则**：池是池，边是边
- 不带箭头的行 = 池声明（说明该池的属性）
- 带箭头的行 = 转移边（说明从某池到目标池的转移条件）
- 每个状态池只声明一次，每条边只出现一次

```
边记法规范（源→目标表达）：
┌──────────────────┬──────────────────────────────┬──────────────────────┐
│ 备选池           │ 备选范围 / 状态中有股票数    │ 重载模式            │
│   └─▶目标状态池  │ 转移条件节点属性            │ 条件转移边时间属性   │
│ 源状态池         │ 状态池属性                  │                     │
│   └─▶目标状态池  │ 转移条件节点属性            │ 条件转移边时间属性   │
│   └─▶目标状态池  │ 转移条件节点属性            │ 条件转移边时间属性   │
└──────────────────┴──────────────────────────────┴──────────────────────┘

关键规则：
- 不带箭头的行 = 池声明行（备选池/状态池自身的属性）
- 带 └─▶ 的行 = 条件边行（代表源状态池→目标状态池的转移关系）
- 每个池只出现一次（作为声明行），每条边只出现一次
- 具体的源状态池由**递归深度**和缩进隐含表达
```

### 19.4 表格中列：条件/属性

按行类型说明中列内容：

**备选池行 (`source`)**：
- "备选范围"：`attrtext`（如 `SH#上证A股 SZ#深证A股`）
- "状态中有股票：N只"
- [备选范围设置] → 调 `openCandidatePoolEditor({nodeId})`

**状态池声明行 (`pool-decl`)**：
- `hold`：进入后保持（自动换算"X小时 / X天"）
- `deltype`：到期出池方式（`0` 立即 / `1` 收盘 / `2` 下一开盘）
- `endtime`：到期时间（绝对时间戳）
- `histana`：收益分析（`0` 不分析 / `N` 最多 N 个）
- `bits`：`record_history` / `alert_sound` / `col_list` 摘要
- [状态属性设置] → 调 `openStateAttributeEditor({nodeId})`

**条件边行 (`edge`)**：
- 转移条件公式：`inditype` 枚举 + `indi` 字段摘要
  - 若公式为空 → 显示"无条件转移"灰色提示
  - 若公式非空 → 显示"满足自设定计算公式转移"
- 转移选项位（3 个 checkbox）：
  - `bit19`（attr & 0x80000）：停板不入
  - `bit20`（attr & 0x100000）：交易日不重入
  - `bit21`（attr & 0x200000）：只入一次
- [条件设置] → 调 `openConditionEditor({nodeId, flowFrom})`

**降级场景**：
- 若选中的状态池有入边但 `type=201` 公式为空 → 显示"无条件转入"灰色提示
- 若选中节点是孤立状态池（无入边无出边）→ 中栏显示"该状态池未连接"，仅显示状态属性

### 19.5 表格右列：时序/操作

按行类型说明右列内容：

**备选池行 (`source`)**：
- "重载模式"：5 种模式（每次启动 / 每天指定时间 / 每隔一定时间 / 不重载 / 文件载入时）
- "重载时间/间隔"：依模式显示
- 无编辑按钮（备选池时序通过属性编辑器设置）

**状态池声明行 (`pool-decl`)**：
- **空**（状态池本身不带时序参数）

**条件边行 (`edge`)**：
- 时序字段：
  - `begin`（时机码）：0 立即 / 1 开始后 N 秒 / 2 闭市后 / 3 指定工作日时间 / ...
  - `begint`（时机参数）：依 begin 解释（秒数 / HHMMSS）
  - `endt`（结束时间码）：0 立即 / 正整数 N 秒后 / 2147483647 持续
  - `interval`（执行间隔，秒）
  - `count`（执行次数）：**0 不显示**、**-1 显示"不限"**、**N 显示"N次"**
- 传输模式位（4 个 checkbox，来自 `flow/@attr`）：
  - `bit0`（attr & 0x1）：本轮不再执行
  - `bit12`（attr & 0x1000）：延迟到下个周期
  - `bit13`（attr & 0x2000）：本轮触发后立即停
  - `bit14`（attr & 0x4000）：仅首入执行
- [时序设置] → 调 `openTimingEditor({flowId, nodeId})`

#### 19.5.1 选中状态池的入边时序（弹窗/侧边栏视图）

在表格视图中，状态池声明行本身右列为空，但该状态池的所有入边时序信息分散在指向它的各条 `edge` 行的右列中。若需要集中查看或编辑某状态池的全部入边时序，可通过双击该行或点击[时序设置]弹窗展开：

- 每条入边一个分组，显示完整的时序字段与传输模式位
- 弹窗内修改后同步回写对应 `edge` 行的右列显示

#### 19.5.2 选中转移条件（`type=201`）

- 上半部分：所有**入流边**（指向 type=201 的边）的时序+传输模式位
- 下半部分：所有**出流边**（type=201 指向其他节点的边）的传输模式位（**无时序**，因 type=201 不带时序）
- 两侧独立[时序设置]按钮

#### 19.5.3 选中备选池（`type=202` 或 `type=7`）

- 显示所有**出流条件转移边**的时序概览（仅 begin/begint/endt/interval/count 摘要，不显示传输模式位）
- 若无出流边：显示"备选池暂无出流边"
- 无任何编辑按钮（备选池出流边不开放时序设置）

#### 19.5.4 时序判断规则

- **时机码以 `begin` 字段为准**，不是 attr 的位（如 `begin=3` 一定是"指定工作日时间"，与 attr 位无关）
- `begin=0` 立即执行：不显示 begint
- `begin=1` 开始后 N 秒：`begint` 为秒数
- `begin=2` 闭市后：不显示 begint
- `begin=3` 指定工作日时间：`begint` 为 HHMMSS 整数（如 93100 → "9:31:00"）

### 19.6 字段编辑与时序/位标志

#### 19.6.1 count 字段显示规则

| count 值 | 显示文本 | 含义 |
|----------|----------|------|
| `0` | （不显示） | 仅在第一次入池时执行 |
| `-1` | `不限` | 无次数限制（持续） |
| `N > 0` | `N次` | 最多执行 N 次 |
| `N < -1` | `N次`（保留原值） | 内部特殊编码，保留原值显示 |

> 备注：`count=-12242` 是 CYS-股票1.xml 中的实际值，表示"永久有效"的内部编码。

#### 19.6.2 传输模式位（flow/@attr）

| bit | 名称 | 含义 |
|-----|------|------|
| `0x0001` (bit0) | 删除源状态中股票 | 通过时从源池移除（默认行为） |
| `0x1000` (bit12) | 不删源状态中股票 | 通过时保留在源池 |
| `0x2000` (bit13) | 先清空目的状态 | 写入目标前先清空目标池内容 |
| `0x4000` (bit14) | 输出成分股 | 当源是板块时输出成分股 |

> 基于 2026-06-10 对照实验确认，与文档 §5.2 Flow attr 位标志一致。

#### 19.6.3 转移选项位（type=201/@attr）

| bit | 名称 | 含义 |
|-----|------|------|
| `0x80000` (bit19) | 输出成分股 | 当源节点是板块时，输出其成分股而非板块本身 |
| `0x100000` (bit20) | 不删除状态中股票(保留源池) | 股票通过条件后保留在源池中；关闭则从源池移除 |
| `0x200000` (bit21) | 转移前先清空目的状态池 | 将股票写入目标池前先清空目标池全部内容 |

> 基于 2026-06-10 对照实验确认。

#### 19.6.4 偶奇配对同步规则

出流边（`from=type=201, to=type=200`）与入流边（`from=type=200, to=type=201`）共享传输模式位的"业务含义"：

- 修改出流边的 `attr` bit0/12/13/14 → 自动同步更新入流边同位
- 修改入流边的 `attr` bit0/12/13/14 → 自动同步更新出流边同位
- 转移选项位（bit19/20/21）只属于 `type=201` 的 `attr`，不参与偶奇配对

实现位置：`comprehensive-settings.js#applyFlowAttrChange` 内部统一走 `syncFlowAttrPair`。

### 19.7 与现有材料的对应关系

| 文档/截图 | 内容 | 用途 |
|-----------|------|------|
| `doc/综合设置表格.md` | 三栏内容详细文字版（含超赢7号/CYS-股票1 全部行级展示） | 数据层佐证 |
| `doc/tempdzh/综合设置/超赢7号-1.png ~ 超赢7号-7.png` | DZH 原版超赢7号股票池综合设置窗口截图 | 视觉佐证 |
| `doc/tempdzh/综合设置/CYS - 股票1-1.png ~ CYS - 股票1-4.png` | DZH 原版 CYS-股票1 综合设置窗口截图 | 视觉佐证 |
| `doc/tempdzh/综合设置/超赢7号.xml` | DZH XML 源数据（19 个 flow，16 个目标状态池） | XML 字段级佐证 |
| `doc/tempdzh/综合设置/CYS - 股票1.xml` | DZH XML 源数据（16 个 flow，8 个目标状态池） | XML 字段级佐证 |
| `doc/tempdzh/综合设置/大智慧股票池说明书.pdf` | DZH 官方说明书（用户行为规范） | 业务规则佐证 |
| `.trae/specs/refactor-comprehensive-settings-3column/` | 三栏重构 spec（tasks/checklist 全部 [x]） | 实现依据 |
| `.trae/specs/enhance-comprehensive-settings/` | 备选池时间参数增强 spec（5 种重载模式） | 备选池实现依据 |
| `doc/tempdzh/综合设置/《天机-0号》-竞价股票池.png` + `.xml` | 多阶段竞价筛选流程（备选→初选→中间→竞价总览→竞价绝买等） | 视觉佐证+XML字段级佐证 |
| `doc/tempdzh/综合设置/1超赢短线王1.png` + `.xml` | 温度计+风险控制+近期能量+买入池链路（低吸池→低吸持仓池→去停牌→温度计→风险控制→近期能量→买入池） | 视觉佐证+XML字段级佐证 |
| `doc/tempdzh/综合设置/超赢7号.png` + `.xml` | 五列并行筛选（初步筛选→超赢备选→强势整理→英雄榜→观察池，含超赢价值A/B、超赢强势/买点/追踪/7号） | 视觉佐证+XML字段级佐证 |
| `doc/tempdzh/综合设置/泛系游鹤竞价版.png` + `.xml` | 四区域竞价模型（固定式→基准→竞价→现手→预备池→超跑线→追涨→量比） | 视觉佐证+XML字段级佐证 |
| `doc/tempdzh/综合设置/候鸟涨停板复盘分析统计池.png` + `.xml` | 复盘统计专用池（备选→涨停→历史池B/C/D/E/A，含收益分析、记录轨迹） | 视觉佐证+XML字段级佐证 |
| `doc/tempdzh/综合设置/决战股海V8.8.png` + `.xml` | 多时段执行模型（10:00/11:00/13:30/13:45/13:50/13:55 六个时段） | 视觉佐证+XML字段级佐证 |
| `doc/tempdzh/综合设置/七剑下天山.png` + `.xml` | 高位回撤实战模型（备选→初步筛选→初筛→中间→竞价总览→竞价准备→绝买→绝买B→历史池） | 视觉佐证+XML字段级佐证 |
| `doc/tempdzh/综合设置/屠龙刀-倚天剑.png` + `.xml` | 双模型对比（屠龙刀vs倚天剑，含排序强/排序弱/趋势转强/趋势走弱） | 视觉佐证+XML字段级佐证 |

### 19.8 实现等价性声明

**XML 配置 ≡ 综合设置表格 ≡ 综合设置窗口三栏** 三者完全等价：

- 给定同一份 `poolData.data`（来自 XML 解析），三栏渲染结果唯一确定
- 在三栏任一处修改条件/属性/时序/位标志，都会同步更新 `poolData` 内存数据
- `poolData` 重新序列化为 XML 后，字段值与修改前一致（除用户主动修改部分）
- 超赢7号 与 CYS-股票1 的 DZH XML 经渲染后，三栏展示的节点数、时序参数、位标志与截图完全一致

### 19.9 全部8实例MCP浏览器验证结果（2026-06-19）

以下8个DZH股票池实例均已通过MCP浏览器逐行提取验证，行类型序列（source/edge/pool-decl）均符合**边批量+深度优先递归**算法的输出规范：

| # | 实例名 | 总行数 | 类型序列特征 | 算法行为确认 |
|---|--------|--------|-------------|-------------|
| 1 | 《天机-0号》-竞价股票池 | **43** | source→edge,edge→pool-decl...（多阶段长链） | 边批量✓ 深度优先✓ |
| 2 | 1超赢短线王1 | **24** | source→edge→pool-decl→edge→pool-decl...（低吸链路） | 边批量✓ 深度优先✓ |
| 3 | 超赢7号 | **30** | source→edge,edge→pool-decl→edge→pool-decl,pool-decl→edge...（五列并行+扇入汇合） | 边批量✓ 扇入去重✓ |
| 4 | 泛系游鹤竞价版 | **22** | source→edge→pool-decl→edge→pool-decl...（四区域串行） | 边批量✓ 深度优先✓ |
| 5 | 候鸟涨停板复盘分析统计池 | **214** | 多源并行+大量历史子池（复杂合并结构） | 边批量✓ 去重正确✓ |
| 6 | 决战股海V8.8 | **18** | source→edge,edge→pool-decl→...→pool-decl,pool-decl→edge（双分支+汇合） | 边批量✓ 已访问目标跳过✓ |
| 7 | 七剑下天山 | **19** | 含连续edge,edge（分叉节点边批量输出） | 边批量✓ 分叉正确✓ |
| 8 | 屠龙刀-倚天剑 | **19** | 含连续edge,edge（分叉节点边批量输出） | 边批量✓ 分叉正确✓ |

**验证方法**：通过 `document.querySelectorAll('[class*="cst-row"]')` 提取DOM行，用类型类映射表（`cst-row-source`→`source`, `cst-row-edge`→`edge`, `cst-row-pool-decl`→`pool-decl`）确定每行类型，排除 `cst-row-depth-N` 和 `cst-row-odd` 等非类型类的干扰。

**关键确认项**：
- 所有实例的行类型序列均满足：父池的所有EDGE行先于其子POOL-DECL行（边优先）
- 发现新目标池时立即输出POOL-DECL并递归（深度优先，不等待同级）
- 已访问过的目标池不再重复声明POOL-DECL（visited去重）
- 同一父池的多条出边连续输出（边批量），如七剑下天山的 `edge,edge` 和屠龙刀-倚天剑的 `edge,edge`
- 汇合节点（多源→同一目标）只产生一个POOL-DECL，如决战股海V8.8的二次筛选池被基准池链路和实盘跟踪池链路共同指向但只声明一次

> 本节为 `refactor-comprehensive-settings-3column` 与 `enhance-comprehensive-settings` 两个 spec 的最终交付文档。如需修改三栏布局或字段规则，请同时更新上述 spec 与本节。

---

**文档结束**（第十九节为股票池综合设置的完整文档化）

